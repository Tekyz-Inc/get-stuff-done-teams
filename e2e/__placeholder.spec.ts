import { test } from '@playwright/test';

// Placeholder spec — viewer specs live under e2e/viewer/*.spec.ts (M50 D2).
// This file exists so `playwright test --list` succeeds in environments where
// the viewer specs are not yet runnable (e.g., Playwright not yet installed).
test.skip('placeholder', () => {});
