# Domain: pipeline-integration

## Purpose
CI/CD examples and documentation for headless mode integration.

## File Ownership
- `docs/infrastructure.md` — headless mode documentation section
- `.github/workflows/gsd-t-verify.yml` — example GitHub Actions workflow (docs-only, no actual CI)
- `docs/ci-examples/` — example workflow files

## Scope
- GitHub Actions example workflow
- GitLab CI example
- Documentation: headless commands, exit codes, CI usage patterns
- Update docs/infrastructure.md with headless section

## Out of Scope
- Actual CI infrastructure (examples/docs only)
- Modifying actual repo CI (if any)

## Tasks
1. Create CI example files (GitHub Actions + GitLab)
2. Update docs/infrastructure.md with headless mode section
