# Domain: dashboard

## Responsibility
Build the browser-based visualization dashboard (`gsd-t-dashboard.html`) that connects to the SSE server, renders agent hierarchy using React Flow + Dagre via CDN, and displays live event overlay with outcome color coding.

## Owned Files/Directories
- `scripts/gsd-t-dashboard.html` — new dashboard HTML/JS (create)

## NOT Owned (do not modify)
- `scripts/gsd-t-dashboard-server.js` — owned by server domain
- `commands/gsd-t-visualize.md` — owned by command domain
- `bin/gsd-t.js` — owned by command domain
- `scripts/gsd-t-dashboard-mockup.html` — reference only, do not modify
- All reference files (README.md, GSD-T-README.md, etc.) — owned by command domain
