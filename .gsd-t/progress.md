# GSD-T Progress

## Project: GSD-T Framework (@tekyzinc/gsd-t)
## Status: M40 PLANNED — External Task Orchestrator + Streaming Watcher UI
## Date: 2026-04-19
## Version: 3.13.16 (targeting 3.14.10 at M40 complete)

## Current Milestone

**M40 — External Task Orchestrator + Streaming Watcher UI** (DEFINED)

### Goal
Build an external JS orchestrator that drives in-session Claude one task per spawn — short-lived, fresh context per task, never long enough to compact. Pair it with a dumb local streaming watcher UI that renders all workers' stream-json output as a continuous claude.ai-style feed at zero Claude token cost.

### Core problem it solves
M8-sized work (~40 tasks across 8 domains) exceeds one Claude Code session's context budget. In-session risks mid-execute compaction that loses cross-file reasoning chains. The unattended supervisor is 5–10× slower and gives only per-iter (5-min) visibility. Both fail on the same workload.

### Success criteria (measurable)
1. **Speed parity or better**: orchestrator wall-clock ≤ in-session wall-clock on a single-domain benchmark (D0 go/no-go gate). Ideal: faster due to process-level parallelism.
2. **No compaction**: no worker session exceeds one task; compaction is architecturally impossible during an M40 run.
3. **Live streaming UI**: `localhost:7842` renders stream-json from all active workers with claude.ai-style layout, task-boundary banners, and durable JSONL backlog. Zero Claude token cost in the UI process.
4. **Parallelism**: per-wave `Promise.all` over parallel-safe tasks, default 3 concurrent workers, max 15 (Team Mode contract §15). Each worker is a separate OS process with its own context window.
5. **Recovery**: if orchestrator crashes mid-run, resume from durable JSONL + progress.md state.

### Scope
**In scope**: orchestrator-core (task queue + claude-p spawn + completion detection + kill/respawn + wave join), task-brief-builder (2–5KB self-contained per-task briefs), completion-protocol contract (commit on expected branch + progress.md entry + test exit), stream-feed-server (ws + JSONL persistence), stream-feed-ui (HTML/JS claude.ai-style renderer), recovery-and-resume.

**Explicitly NOT in scope**: retiring `gsd-t-unattended` (stays for overnight/idle), rewriting `gsd-t-execute` subagent flow (unchanged — M40 is a new orchestrator, not a replacement), mobile UI, remote/hosted watcher (localhost only), multi-project orchestration (single project per orchestrator run).

### Go/no-go gate (D0, built first)
Benchmark orchestrator vs in-session on the same domain. If orchestrator is not ≥ in-session, kill M40 before building the UI (D4/D5). No sunk-cost commitment. User requirement: *"It's not worth doing if it doesn't run at least as fast[er]."*

### Version target
v3.14.10 (minor bump — new major feature, not a fix).

## Milestones

| # | Milestone | Status | Version | Domains |
|---|-----------|--------|---------|---------|
| M40 | External Task Orchestrator + Streaming Watcher UI | PLANNED | 3.14.10 (target) | 7 domains, 28 tasks in 4 waves: W0 = 15 tasks (5 sub-groups including D0 gate) + W2 = 1 task + W3 = 11 tasks + W4 = 4 tasks |
| M39 | Fast Unattended + Universal Watch-Progress Tree | COMPLETE | 3.13.10 | 3 domains: D2 progress-watch, D3 parallel-exec, D4 cache-warm-pacing |
| M38 | Headless-by-Default + Meter Reduction | COMPLETE | 3.12.10 | 5 domains in 2 waves: Wave 1 sequential (m38-headless-spawn-default → m38-meter-reduction); Wave 2 parallel (m38-unattended-event-stream + m38-router-conversational + m38-cleanup-and-docs) |
| M37 | Universal Context Auto-Pause | COMPLETE | 3.11.10 | Archived (1 domain: m37-auto-pause; context-meter-contract v1.2.0; 5 command files updated; 1228/1228 tests) |
| M36 | Cross-Platform Unattended Supervisor Loop | COMPLETE | 3.10.10 | Archived (6 domains) |
| M35 | No Silent Degradation + Surgical Model Escalation + Token Telemetry | COMPLETE | 2.76.10 | Archived (7 domains; 38 tasks) |
| M34 | Context Meter | COMPLETE | 2.75.10 | Archived |

## Completed Milestones

| Milestone | Version | Completed | Tag | Summary |
|-----------|---------|-----------|-----|---------|
| M39 Fast Unattended + Universal Watch-Progress Tree | 3.13.10 | 2026-04-18 | v3.13.10 | 3 domains (D2 progress-watch + D3 parallel-exec + D4 cache-warm-pacing); 1240/1240 tests; Red Team GRUDGING PASS; plus 6 follow-on patches v3.13.11–3.13.16 |
| M38 Headless-by-Default + Meter Reduction | 3.12.10 | 2026-04-17 | v3.12.10 | Headless spawn default across 7 commands; context meter reduced to single-band; 7 commands deleted; event stream JSONL; smart router intent classifier; 5 contracts folded; 1176/1177 tests |
| M37 Universal Context Auto-Pause | 3.11.10 | 2026-04-16 | v3.11.10 | Strengthened context meter additionalContext from suggestion to MANDATORY STOP instruction; context-meter-contract v1.2.0; CLAUDE-global Universal Auto-Pause Rule section; Step 0.2 in 5 loop commands; 1228/1228 tests |
| M36 Cross-Platform Unattended Supervisor Loop | 3.10.10 | 2026-04-15 | v3.10.10 | Detached supervisor relay for 24h+ unattended milestone execution; 3 new slash commands (unattended/unattended-watch/unattended-stop); ScheduleWakeup(270s) in-session watch loop; /clear+/resume auto-reattach via Step 0; cross-platform (macOS/Linux/Windows except sleep-prevention); safety rails (gutter/blocker/timeout); 1226/1226 tests |
| M35 Runway-Protected Execution | 2.76.10 | 2026-04-15 | v2.76.10 | Removed silent quality degradation; surgical per-phase model selection; pre-flight runway estimator with headless auto-spawn; 18-field per-spawn token telemetry; detect-only optimization backlog; 985/985 tests |
| M34 Context Meter | 2.75.10 | 2026-04-14 | v2.75.10 | Real context-window measurement via Anthropic count_tokens API; replaces task-counter proxy entirely; 941/941 tests |

Older milestones (M33 and earlier) archived under `.gsd-t/milestones/` — see directory listing for the full index.

## Blockers
<!-- No active blockers -->

## Decision Log

> Prior decision log entries preserved in `.gsd-t/milestones/*/progress.md` — see archive snapshots (most recently `M39-fast-unattended-watch-progress-2026-04-18/progress.md`) for pre-M39 history.

- 2026-04-20 13:25: [benchmark-gate] **PASS** — M40 D0 speed-benchmark kill-switch cleared. Operator run (`node bin/gsd-t-benchmark-orchestrator.js --keep-tmp --runs 1`): orchestrator **226451ms** vs in-session **315567ms** (ratio 0.72×, threshold 1.05×). Both sides compliant per the new `auditTaskCommits` gate: orchestrator 22 commits across 20 unique task IDs (discipline=compliant), in-session 28 commits across 20 unique task IDs (discipline=compliant). Full verdict detail: `orchestrator 226451ms ≤ in-session 315567ms × 1.05 (331345ms) — Waves 2+3+4 unlocked`. Summary of the three hypotheses from the 13:12 fix: (a) in-session prompt did NOT still bulk-commit — the strict per-task discipline landed and stuck (28 task commits ≥ 20 expected, unique IDs = 20); (b) API concurrency throttling DID show up (per-task orch duration 35–80s on the 8-way parallel wave vs ~15s sequential, confirmed by the kept tmp dir state.json from the earlier Hk2Kcd run), but the orchestrator's wave-level parallelism beats that cost — 3 waves of parallel work still finish materially faster than 20 serial tasks; (c) no evidence of the CPU-contention theory the user correctly ruled out (20-core M5 Pro, 18 reported by node). Outcome: **M40 is architecturally green**. The sunk-cost kill-switch does not fire. Waves 2 (D1-T6), 3 (D1-T7 + D4-T1..5 + D5-T1..5), and 4 (D6-T1..4) are now unblocked and can begin. Files produced this run: `.gsd-t/benchmark-results.json` (schema v1, full env + audit fields), `docs/m40-benchmark-report.md` (operator-facing verdict). Preserved tmp dirs for forensics: orch `m40-bench-orch-1-TlhZp9`, in-session `m40-bench-insession-1-zWACkE`. Next action: execute Wave 2 — D1-T6 (full worker refinement: task-boundary frames, wave-boundary frames, SIGINT handler, integration test covering SIGINT-mid-wave + second-fail-halt + 3-wave success). After Wave 2, Wave 3 runs D1-T7 + D4-T1..5 + D5-T1..5 in parallel; after Wave 3, Wave 4 runs D6-T1..4. Total remaining: 16 tasks across 4 domains (D1 ×2, D4 ×5, D5 ×5, D6 ×4).
- 2026-04-20 13:12: [debug] M40 D0 benchmark baseline integrity fix. First 20-task re-run FAILed 1.85× (orch 215s vs insession 117s) — inspection of preserved tmp dirs revealed the in-session side **cheated the benchmark**: git log shows ONE bulk commit (`1212b5b Complete all 20 benchmark tasks across 4 domains`) for all 20 tasks, no Decision Log entries, no per-task Done Signal compliance. Meanwhile orchestrator made 20 correct per-task commits (visible in state.json: all 20 tasks `done`, 0 retries, 0 `missing`, canonical IDs, one commit each). The original in-session prompt ("for each task…commit on main") gave Claude enough latitude to batch the whole workload. Comparing 20 committed-per-task runs to 1 bulk-committed run is apples-to-oranges and any resulting timing delta is meaningless — user correctly flagged this and chose Option A (enforce parity) over Option B (redefine the gate). **Fix** (bin/gsd-t-benchmark-orchestrator.js): rewrote in-session prompt with mandatory per-task discipline (read completion-signal-contract once, strict wave order, ONE commit per task minimum, write-files-for-N+1 only after committing N, no subagents, no git push, stop when finished). Added `auditTaskCommits(workloadDir)` helper that greps `git log --pretty=%s` for `bench-dN-tN:` prefixes and counts unique task IDs; returns `{totalCommits, taskCommitCount, uniqueTaskIds, expectedTaskCount, discipline: 'compliant'|'noncompliant'}`. Both runOrchestratorSide and runInsessionSide now return a `commitAudit` field. `computeVerdict` grew a new verdict branch: **INVALID** — when either side's audit is noncompliant, the verdict is INVALID (not FAIL) with detail "commit-discipline audit failed … one side did not produce one-commit-per-task; timing comparison is meaningless until both sides obey the same rules." Report/stdout verdict now show per-run commit counts (`215803ms / 0 / 20`). Tests: +2 verdict tests (INVALID path, compliant-both-sides PASS preservation); existing PASS/FAIL tests keep working because absent `commitAudit` treats as compliant (preserves back-compat for synthetic injected timings). Full suite: **1351/1351** (was 1349, +2). Note on the still-real timing delta (assuming baseline now complies): on first re-run, orchestrator per-task duration was 35-80s vs ~17s single-worker (from the 4-task fixture run). User (20-core M5) correctly ruled out CPU contention; the likely cause is **per-account Anthropic API concurrency throttling** — 8 simultaneous claude-p sessions on one auth hit queuing at the API layer. That's a real M40 cost, not a measurement artifact, and we'll see its true magnitude once the baseline stops cheating. Next operator action: re-run `node bin/gsd-t-benchmark-orchestrator.js --keep-tmp --runs 1` — if verdict is INVALID (in-session still ignoring per-task rule), prompt needs more teeth. If compliant-but-slow (INVALID → PASS/FAIL transition), we measure the real delta and decide based on that.
- 2026-04-20 12:55: [debug] M40 D0 benchmark fixture scale-up — 4 tasks/2 waves → 20 tasks/3 waves/4 domains. Root cause of the 1.20× FAIL (orch 83137ms vs insession 69300ms) on the re-run after the three-bug fix: the 4-task fixture is too small. With 2 tasks per wave and 2 waves, the orchestrator pays 2 cold-starts (one per wave, ~10-15s each) against the in-session side's 1 amortized startup. On a 4-task run, per-spawn overhead dominates orchestration value. Decision: scale the benchmark to match the real M40 use case (M8-sized, 20+ tasks across multiple domains with 3 waves). New shape: 4 domains (`bench-d1`…`bench-d4`), each with 5 tasks in the same pattern (W0: 2 generators, W1: 2 derivers, W2: 1 aggregator). Wave totals: W0=8 parallel, W1=8 parallel, W2=4 parallel. Cross-domain tasks touch disjoint files (`out/d{1,2,3,4}-*.txt`, `test/bench/d{1,2,3,4}-*.test.js`) so parallel-safe across domains. Benchmark driver updated: `--max-parallel 2` → `--max-parallel 8` (exercise the pool), `--worker-timeout 60000` → `180000` (cold-start + real work), orchestrator-side spawn timeout 600s → 900s, in-session spawn timeout 600s → 900s (20 × ~15s serial ≈ 5min + headroom). Files: `test/fixtures/m40-benchmark-workload/.gsd-t/domains/{bench-d1,bench-d2,bench-d3,bench-d4}/{scope,constraints,tasks}.md` (9 new files; bench-d1 updated for new prefix scheme), `test/fixtures/m40-benchmark-workload/README.md` (workload-shape table + expected ~3× speedup rationale), `bin/gsd-t-benchmark-orchestrator.js` (--max-parallel 8, timeouts bumped), `test/m40-benchmark-workload-fixture.test.js` (updated for 20/3/4 shape, 4 domains exist-check loop). Full suite: **1349/1349 pass** (unchanged total — fixture tests cover same 7 cases with wider assertions). Expected operator re-run: orch ~90s (3 waves × ~30s slowest-in-wave), insession ~300s (20 serial). Gate should PASS at ~0.3× — orchestrator roughly 3× faster. **NOT** recording `[benchmark-gate] PASS/FAIL` — awaiting operator re-run with live auth.
- 2026-04-20 02:45: [debug] M40 three-bug completion-check fix (diagnostic re-run root cause). First operator benchmark verdict was FAIL with all 3 orch runs exit=1 and 1.46× slowdown. After the stdout-capture fix (2026-04-20 02:05) and a `--keep-tmp --runs 1` diagnostic run, inspection of the preserved tmp dir's `.gsd-t/orchestrator/state.json` + `git log` surfaced three layered bugs in the completion-check plumbing, none related to speed. **Bug 1** (task ID format mismatch): queue emits `bench-d1:T1` form; workers commit with `bench-d1-t1` form (per task-brief expectations); assertCompletion's `taskIdRe` used the colonized form literally, so it never matched the actual commit message. Fix: `bin/gsd-t-orchestrator.js` computes `canonicalTaskId` once (`task.id.includes(':T')` → `${domain}-t${N}`), threads it through the brief AND a `taskForWorker = {...task, canonicalId}`; `bin/gsd-t-orchestrator-worker.cjs` now passes `task.canonicalId || task.id` into `assertCompletion`. **Bug 2** (ownedPatterns never plumbed through): `task.ownedPatterns` was always `[]`; completion-check's `matchesAny` returns true when patterns empty, so every uncommitted file counted as uncommitted_owned_changes — instant fail. Fix: `bin/gsd-t-orchestrator-queue.cjs` new `parseFilesField` function extracts backtick-quoted paths from a task's `**Files**:` line (strips `(NEW)`/`(UPDATED)` suffixes, rewrites trailing-slash to `/**` globs, comma-split fallback); `parseTasksFile` now populates `task.ownedPatterns` from that field. 5 new queue tests added (`test/m40-orchestrator-queue.test.js`: parseFilesField backtick extraction, trailing-slash glob, comma-split fallback, NONE/N/A → [], parseTasksFile ownedPatterns population + absent-Files → []). **Bug 3** (fixture byproducts counted as uncommitted): orchestrator writes `.gsd-t/events/YYYY-MM-DD.jsonl`, `.gsd-t/orchestrator/state.json`, context-meter state, and heartbeats inside the workload dir during a run. All appear as uncommitted files; with Bug 2 fixed via real ownedPatterns, they now correctly stay outside a task's owned patterns — but the workload itself needed a `.gitignore` so a clean fixture doesn't fail its own completion check, and needed a real `package.json` + `test/run.js` so worker test runs don't invent one. Fix: `test/fixtures/m40-benchmark-workload/.gitignore` (excludes `.gsd-t/events/`, `.gsd-t/orchestrator/`, `.gsd-t/.context-meter-state.json`, `.gsd-t/context-meter.log`, `.gsd-t/heartbeat-*.jsonl`, `node_modules/`); `package.json` with `"test": "node test/run.js"`; `test/run.js` minimal tolerant runner (iterates `test/bench/*.test.js`, tolerates missing dir / empty set — workload is pre-wave-0 valid). **Fixture tests extended**: `test/m40-benchmark-workload-fixture.test.js` +3 existence assertions (package.json, test/run.js, .gitignore), +1 `.gitignore` content test, +1 ownedPatterns plumbing test. Full suite: **1349/1349** (was 1340, +9). Next operator action: re-run `node bin/gsd-t-benchmark-orchestrator.js --keep-tmp --runs 1` for trustworthy verdict; if orch state.json now shows `status: done` with no `missing` entries per task, the completion-check path is verified and a full `--runs 3` run can produce the real PASS/FAIL gate decision.
- 2026-04-20 02:05: [debug] benchmark driver diagnostic gap — first operator run verdict FAIL but stderr captures empty; cannot tell WHY orchestrator runs exited 1. First verdict data — orch median 90844ms vs insession 62011ms, all 3 orch runs exit=1, all 3 insession runs exit=0 — so the speed delta (1.46x slower) is real but confounded by orchestrator hard-failing before workload completes. Root cause: `runOrchestratorSide`/`runInsessionSide` captured only `res.stderr`; orchestrator logs `[wave_halt]` via `logger.log` which goes to stdout, so the diagnostic signal was silently dropped. Fix (commit TBD): new `captureOutput()` helper labels and concatenates stdout + stderr into the `stderr` field of each run record (last 4KB). New `--keep-tmp` CLI flag preserves per-run tmp dirs (previously auto-wiped) so operators can inspect `.gsd-t/orchestrator/state.json`, `.gsd-t/events/*.jsonl`, and `git log` of a failing run. `runBenchmark` now threads `keepTmp` through to both side runners; each run record gains a `tmpDir` field (null when keepTmp=false). Tests: +4 coverage (parseCliArgs --keep-tmp, captureOutput regression, keepTmp=true preserves + keepTmp=false cleans). Full suite 1340/1340 (was 1336). NOT recording `[benchmark-gate] FAIL` in progress.md — data is untrustworthy until stdout capture lands. Next operator action: re-run `node bin/gsd-t-benchmark-orchestrator.js --keep-tmp --runs 1` for one diagnostic run; inspect the preserved tmp dir + new `stderr` field in benchmark-results.json to identify the real failure mode (assertCompletion strictness vs worker timeout vs legit overhead).
- 2026-04-19 22:30: [execute] M40 Wave 0 — D0 Tasks 2 + 3 landed (benchmark driver + smoke wrapper). **D0-T2** (`bin/gsd-t-benchmark-orchestrator.js`): CLI `gsd-t benchmark-orchestrator --runs N [--report-path p] [--results-path p] [--fixture-dir p] [--mock-claude p] [--project-dir p]`. Per run: `fs.mkdtempSync` tmp dir → `copyDirSync` fixture → `git init -b main` + baseline commit → spawns `bin/gsd-t-orchestrator.js --milestone M40-bench --project-dir <tmp> --max-parallel 2 --worker-timeout 60000` (orchestrator side) and a single `claude -p --dangerously-skip-permissions --output-format stream-json --verbose --model sonnet` with a sequential-execute prompt as input (in-session side). Captures `Date.now()` deltas per run; collects node version, platform, arch, CPU count, free/total RAM via `collectEnv()`. Writes `.gsd-t/benchmark-results.json` (schema v1: generatedAt, runs, fixtureDir, env, per-run timings + exitCode, summary, verdict) and `docs/m40-benchmark-report.md` (verdict line, per-run table, methodology). Verdict: PASS when `median(orch) ≤ median(insession) × 1.05` AND all runs exit 0; else FAIL with `M40 HALT RECOMMENDED`. Module exports injection points for unit tests — `orchestratorImpl` / `insessionImpl` / `spawnImpl` all overridable. Tolerance `PASS_TOLERANCE = 1.05` exported. **D0-T3** (`test/m40-speed-benchmark.test.js`): 13 unit tests — `parseCliArgs` (defaults + validation), `median` (odd/even/empty), `computeVerdict` (PASS, FAIL-slow, FAIL-any-exit-nonzero), `prepareRun`+`cleanupRun` (fixture copy + git init + tmp cleanup), `renderReportMd` (verdict+rows+methodology), `runBenchmark` smoke (PASS path writes JSON+MD), `runBenchmark` FAIL path, missing-fixture rejection, `runOrchestratorSide` spawnImpl assertions, `runInsessionSide` spawnImpl assertions. Zero real `claude -p` spawns in the test — all injected. Full 3-run verdict remains an operator action (user invokes `gsd-t benchmark-orchestrator --runs 3` with real auth). **Wiring**: `bin/gsd-t.js` help now lists `benchmark-orchestrator`; the subcommand case spawnSyncs the driver with forwarded args (mirrors `orchestrate`). Full suite: **1336/1336** (was 1323, +13). E2E: N/A (CLI package). Fixture + drivers are all in place — next operator action: run `gsd-t benchmark-orchestrator --runs 3` on a live claude-auth machine. On PASS → unblock Waves 2/3/4 (D1-T6 full worker, D4 stream-feed-server, D5 stream-feed-ui, D6 recovery). On FAIL → halt M40, revisit architecture.
- 2026-04-18 13:15: [verify] M39 Fast Unattended + Universal Watch-Progress Tree → VERIFIED. Retrospective book-keeping verify: M39 code shipped across v3.13.10 (Wave 1 baseline with Red Team GRUDGING PASS) + six follow-on patches v3.13.11–v3.13.16 covering unattended supervisor triple-fix, debug-ledger tolerance, sweep self-protection by package-name identity, narrow gitignore, and `/gsd-t-unattended` overnight-only positioning. Tests: 1240/1240 unit pass (net +12 vs 1228 at M37 close). E2E: N/A (CLI package, no playwright.config.*). All 4 success criteria met: (1) parallel-within-iter Team Mode prompt shipped in `_spawnWorker` with §15 v1.3.0, (2) universal watch-progress tree renders under every `--watch` via `bin/watch-progress.js` + 189 shims across 17 commands + `watch-progress-contract.md` v1.0.0, (3) cache-warm pacing DEFAULT_WORKER_TIMEOUT_MS=270000 + `--worker-timeout` flag + §16 v1.3.0, (4) bee-poc speedup inherited on v3.13.11+ after supervisor relaunch. No CRITICAL/HIGH findings; no remediation required. Status: IN PROGRESS → VERIFIED. Next: auto-invoke /gsd-t-complete-milestone.
- 2026-04-17 21:30: [debug] v3.13.10→3.13.11 — unattended supervisor reliability triple-fix (bee-poc 15-min hang fallout). Bug 1 (P0): supervisor watchdog visibility — `runMainLoop` now writes a deterministic `[worker_timeout] iter=N budget=Nms elapsed=Nms` line to `run.log` when the spawnSync timeout fires (exit 124), so operators tailing the log can see when the 270 s watchdog (§16) kicks in. State.json still gets a fresh lastTick + lastExit=124 post-timeout. Note: the real root cause of bee-poc's hang was that v3.13.10 (which had the 270 s timeout) was never published to npm — bee-poc was running against installed v3.12.15 with the 1-hour timeout. Shipping v3.13.11 propagates the fix. Bug 2 (P0): worker cwd invariant — `_spawnWorker` prompt now carries an explicit `# CWD Invariant` section instructing the worker to (a) compare `$(pwd)` to `$GSD_T_PROJECT_DIR` as the first Bash call and (b) scope any `cd` inside a subshell. Closes the "Shell cwd was reset" silent-wrong-repo failure mode. Bug 3 (P2): IS_STALE determinism — Step 2 of `commands/gsd-t-unattended-watch.md` now computes `const IS_STALE = tickAgeMs !== null && tickAgeMs > 540000;` and emits it via `out('IS_STALE', …)`. Step 6a reads the flag verbatim instead of re-interpreting the threshold — fixes Haiku's occasional "⚠️  stale" at 6-min tick age (358 s). Boundary cases locked in: 539 s=false, 540 s=false (strict >), 541 s=true. Reproduction tests: `test/unattended-triple-fix-v3-13-11.test.js` (8 assertions across 8 it-blocks). Files: `bin/gsd-t-unattended.cjs`, `commands/gsd-t-unattended-watch.md`, `test/unattended-triple-fix-v3-13-11.test.js` (NEW), `package.json`, `CHANGELOG.md`. Tests: Unit 1235/1235 pass (was 1227, +8 new). E2E: N/A.
- 2026-04-17 13:33: [execute] M39 Wave 1 — all 3 domains landed + Red Team GRUDGING PASS. D2 progress-watch: `scripts/gsd-t-watch-state.js` CLI, `bin/watch-progress.js` tree builder + renderer, `.gsd-t/contracts/watch-progress-contract.md` v1.0.0, 189 shims across 17 workflow commands, append-below-banner integration in 3 watch printers. D3 parallel-exec: `_spawnWorker` prompt teaches Team Mode (intra-wave ≤15 parallel subagents, inter-wave sequential), contract §15 v1.3.0. D4 cache-warm-pacing: `DEFAULT_WORKER_TIMEOUT_MS=270000` (was 3600000) + `--worker-timeout` CLI flag parsed + config.workerTimeoutMs merge, contract §16 v1.3.0. Red Team (opus) FAIL→fix→GRUDGING PASS: fixed BUG-1 through BUG-8. Tests: 1227/1227 pass (1186 baseline + 41 new tests across 6 new test files).
- 2026-04-17 20:00: [milestone-planned] M39 PLANNED — D2: 12 tasks (progress-watch renderer + 17 command shims), D3: 4 tasks (worker Team Mode prompt + contract §15), D4: 3 tasks (270s worker timeout + contract §16). Total: 19 tasks, all in Wave 1 parallel.
- 2026-04-17 19:45: [milestone-partitioned] M39 PARTITIONED into 3 domains, all in Wave 1 (parallel). D2 d2-progress-watch, D3 d3-parallel-exec, D4 d4-cache-warm-pacing. Shared files: `bin/gsd-t-unattended.cjs` and `unattended-supervisor-contract.md` — additive, no coordination required.
- 2026-04-17 19:30: [milestone-defined] M39 — Fast Unattended + Universal Watch-Progress Tree committed to progress.md. Target version 3.13.10. 3 domains: D2 progress-watch, D3 parallel-exec, D4 cache-warm-pacing. Rationale: bee-poc pid 69481 has been grinding unattended on v3.12.13 for 45+ min on a milestone that in-session finishes in 10–15 min — root cause is worker prompt at `bin/gsd-t-unattended.cjs::_spawnWorker` not teaching Team Mode. Ready for partition.
- 2026-04-18 15:00: [complete-milestone] M39 Fast Unattended + Universal Watch-Progress Tree COMPLETED — v3.13.16 (shipping tag v3.13.10 + patches v3.13.11–v3.13.16). Archived to `.gsd-t/milestones/M39-fast-unattended-watch-progress-2026-04-18/`. Tests: 1240/1240 unit pass. E2E: N/A. [goal-backward-pass] 4/4 success criteria verified. [distillation] No repeating patterns from event stream. [patch-lifecycle] No promotions/deprecations. Token optimizer: no new recommendations.
- 2026-04-19 19:50: [execute] M40 Wave 0 — D0 Task 1 landed (benchmark workload fixture). `test/fixtures/m40-benchmark-workload/` contains a self-contained `.gsd-t/` with one domain (`bench-d1`) and 4 tasks: W0-T1/T2 generate `out/{a,b}.txt` in parallel (independent writes), W1-T3/T4 derive uppercased `out/{c,d}.txt` each reading exactly one wave-0 artifact. Exercises both the parallel pool and the strict wave barrier. Carries its own `completion-signal-contract.md` copy so `buildTaskBrief` builds briefs without reaching into the parent repo — confirms the brief builder is project-scoped. README documents workload shape, invariants, and why 4 tasks / 2 waves is the right size (enough to exercise the orchestrator, small enough that orchestration overhead dominates model-throughput noise). 5/5 smoke tests pass (test/m40-benchmark-workload-fixture.test.js): layout present, `tasks.md` parses cleanly via D1 queue (4 tasks / 2 waves, no forward cross-wave deps), `buildTaskBrief` produces ≤10 KB briefs for all 4 tasks with Task + Done Signal sections. Full suite: **1323/1323** (was 1318, +5). **D0-T2 (benchmark driver) is next** but is intentionally operator-run — the driver spawns real `claude -p` processes to compare orchestrator vs in-session wall-clock, which requires live Claude auth and 3×N×~30 s of real workload per run. The driver skeleton + smoke-test wrapper (D0-T2 + D0-T3) can be built in-session; the PASS/FAIL verdict (`BENCHMARK: PASS|FAIL` → written to `docs/m40-benchmark-report.md`) comes from an operator running `gsd-t benchmark-orchestrator --runs 3`. If the verdict FAILs, the M40 plan is to halt before D4/D5.
- 2026-04-19 19:30: [execute] M40 Wave 0 — D1 Tasks 4 + 5 landed (main orchestrator CLI + `gsd-t orchestrate` subcommand). **D1-T4** (`bin/gsd-t-orchestrator.js`): exports `runOrchestrator({projectDir, milestone, maxParallel?, workerTimeoutMs?, logger?, spawnImpl?, runWorkerImpl?})`, `runWaveTasks`, `parseCliArgs`, `atomicWriteJson`. CLI: `gsd-t orchestrate --milestone <id> [--max-parallel N] [--worker-timeout ms] [--project-dir path] [--resume]`. Iterates waves via `groupByWave`; within wave uses a concurrency-capped worker pool (default 3, ceiling 15 per Team Mode §15); strict barrier — wave N+1 never launches until all wave N tasks resolve. Retry policy per completion-signal-contract v1.0.0 §Retry Policy: first FAIL → single retry with "previous attempt failed" preamble; second FAIL → halt wave, mark state.json `failed`, exit non-zero (per wave-join-contract v1.0.0 §Failure Semantics). State file at `.gsd-t/orchestrator/state.json` written atomically (tmp + rename) after every task transition; captures milestone, totalTasks, currentWave, per-task {status, startedAt, endedAt, exitCode, durationMs, missing, retryCount}, overall status `running|done|failed`. Emits task lifecycle events to `.gsd-t/events/YYYY-MM-DD.jsonl` reusing the existing event schema: `orchestrator_start`, `wave_start`, `task_start`, `task_done|task_failed`, `wave_done|wave_failed`, `orchestrator_halt|orchestrator_done`. Injects `buildTaskBrief` per spawn; brief built fresh for retry attempts. Test injections: `spawnImpl` (into worker) + `runWorkerImpl` (into orchestrator — lets integration tests stub completion logic without real git commits). **D1-T5** (`bin/gsd-t.js`): added `orchestrate` subcommand case that spawnSyncs `bin/gsd-t-orchestrator.js` with forwarded args; `help` output lists `orchestrate`; existing dispatches (install, status, headless, unattended, etc.) untouched. Smoke tests: `gsd-t help` shows orchestrate; `gsd-t orchestrate --help` prints flag summary; `gsd-t status` still prints current project context. 7/7 integration tests pass (test/m40-orchestrator-main.test.js) including parseCliArgs, empty-project status, single-wave drive-to-done, strict wave barrier ordering (wave 1 waits for all wave 0 dispatches), double-fail halt with failed state.json, events JSONL written. Full suite: **1318/1318** (was 1311, +7). D0-T1 (benchmark fixture) now unblocked — next up. After D0-T1/T2 land, D0-T3 is **THE GATE**: benchmark orchestrator vs in-session; FAIL → halt M40 before D4/D5.
- 2026-04-19 19:00: [execute] M40 Wave 0 — D2 Task 3 public `buildTaskBrief` API landed. `bin/gsd-t-task-brief.js` exports `buildTaskBrief({milestone, domain, taskId, projectDir, expectedBranch?, maxBytes?}) → string`. Reads `.gsd-t/domains/{domain}/{scope,constraints,tasks}.md`, parses the task entry by number + extracts its `Contract refs:` list, reads only those named contracts (excerpted verbatim from `.gsd-t/contracts/`), reads `completion-signal-contract.md` Done Signal section verbatim for the completion-spec slot, runs stack detection (package.json deps → react/typescript/nextjs/vue/playwright/node-api/tailwind/prisma; Python via requirements.txt / pyproject.toml) and loads matching `templates/stacks/*.md` + universal `_*.md` rules, composes via `renderTemplate`, passes through `compactToTarget`. Deterministic by design — pure fs reads + sorted stack filenames. `extractTask` and `extractSection` helpers exported for unit reuse. 12/12 unit tests pass (test/m40-task-brief.test.js) including determinism (byte-identical on 2 calls), missing-contract throws, missing-domain throws, tight-budget forces stack-rule drop while preserving Task+Done Signal+CWD Invariant, and a self-test against the actual GSD-T repo for D2-T1 (<10KB). Full suite: 1311/1311 (was 1299, +12). **D1-T4 UNBLOCKED** (main orchestrator CLI wave-join loop — next). After D1-T4+T5 land, the D0 benchmark (D0-T1/T2/T3 **GATE**) runs: if FAIL → halt M40 before D4/D5 ever start.
- 2026-04-19 18:30: [execute] M40 Wave 0 — D2 Tasks 1 + 2 landed (task brief template + compactor) + worker test stabilization. **D2-T1** (`bin/gsd-t-task-brief-template.cjs`): exports `renderPreamble({projectName, milestone, domain, taskId, expectedBranch, projectDir}) → string`, `renderCwdInvariant(projectDir) → string`, `renderTemplate({preamble, taskStatement, scope, constraints, contractExcerpts?, stackRules?, completionSpec, cwdInvariant}) → string`. Frozen section headers (`## Task`, `## Scope`, `## Constraints`, `## Contracts`, `## Stack Rules`, `## Done Signal`, `## CWD Invariant`) per task-brief-contract.md v1.0.0. Contract + stack sections are optional — rendered only when non-empty. 9/9 unit tests pass (test/m40-task-brief-template.test.js). **D2-T2** (`bin/gsd-t-task-brief-compactor.cjs`): exports `compactToTarget(sections, maxBytes) → sections'` and `TaskBriefTooLarge` error. Drop order: `stackRules` → `contractExcerpts` → `constraintsExtra` (droppable). Non-droppable set frozen: `preamble`, `taskStatement`, `scope`, `constraintsMustFollow`, `completionSpec`, `cwdInvariant` — if these alone exceed `maxBytes`, compactor throws `TaskBriefTooLarge` with breakdown. 9/9 unit tests pass (test/m40-task-brief-compactor.test.js). **Test stabilization**: `test/m40-orchestrator-worker.test.js` refactored — happy/fail/invalid-json/env-check tests now use `spawnImpl` EventEmitter mocks; timeout test also converted to mock (kill('SIGTERM') → `exit(null, 'SIGTERM')` 10ms later) eliminating real-sleep dependency. Worker test file now runs in 305ms (was ~1.2s); removed unused `writeMockClaude` helper and `child_process.spawn` import. Full suite: **1299/1299 × 3 consecutive runs clean** — the previously-flaky `telemetry-env-propagation.test.js:178` also stabilized (less fork contention from shell mocks). D1-T4 (main CLI wave-join loop) still blocked on D2-T3 (`buildTaskBrief` public composer). Next: D2-T3 composer → D0-T1 benchmark fixtures → D0-T2 driver → **D0-T3 GATE verdict**.
- 2026-04-19 17:55: [execute] M40 Wave 0c — D1 Task 3 worker lifecycle (minimal). `bin/gsd-t-orchestrator-worker.cjs` exports `runWorker({task, brief, config, onFrame, env, spawnImpl}) → Promise<{result, exitCode, durationMs, timedOut, stderr}>`. Spawns `claude -p --dangerously-skip-permissions --output-format stream-json --verbose --model {task.model||sonnet}` with cwd=`config.projectDir` and env `GSD_T_PROJECT_DIR`. Writes brief to stdin. Pipes stdout line-by-line through JSON.parse; valid frames → `onFrame(frame)`; unparseable lines → `onFrame({type:"raw", line})`. Emits synthetic `task-boundary start` before first frame + `done|failed` after exit (per stream-json-sink-contract v1.0.0). Enforces `config.workerTimeoutMs`: SIGTERM, wait 5s, SIGKILL; writes deterministic `[worker_timeout] task=... budget=...ms elapsed=...ms` via config.logger. Post-exit calls `assertCompletion` from D3-T2 with `ownedPatterns` from `task.ownedPatterns`. Timeout path prepends `worker_exited_via_timeout`; nonzero exit prepends `worker_exit_nonzero`; spawn error returns `missing:["spawn_error"]`. Test-double: `GSD_T_CLAUDE_BIN` env override lets unit tests swap a shell-script mock (happy/fail/hang/invalid-json/env-check). 9/9 unit tests pass (test/m40-orchestrator-worker.test.js). Full suite: 1281/1281 (was 1272, +9). D1 Task 4 (main CLI wave-join loop) blocked on D2-T3 (buildTaskBrief); D2-T1 is next unblocked.
- 2026-04-19 17:50: [execute] M40 Wave 0b — D1 Task 2 task queue + wave partitioning. `bin/gsd-t-orchestrator-queue.cjs` exports `listDomainTaskFiles`, `parseTasksFile`, `parseDependencies`, `readAllTasks`, `groupByWave(tasks) → Map<waveNum, Task[]>` sorted ascending, `validateNoForwardDeps(tasks)` (per wave-join-contract v1.0.0 §Between-Wave Ordering). Tasks without `**Wave**: N` default to wave 0 (backward-compat safety). Dep parser handles `NONE`/`N/A`/empty, same-domain `Task N` → `domain:TN`, and cross-domain `BLOCKED BY d3-completion-protocol Task 2` → `d3-completion-protocol:T2`. Smoke test on real M40 tree: 30 tasks across 7 domains, Wave 0=14, Wave 2=1, Wave 3=11, Wave 4=4, dep validation passes. 13/13 unit tests pass (test/m40-orchestrator-queue.test.js). Full suite: 1272/1272 (was 1259, +13). D1 Task 3 (worker lifecycle — consumes `assertCompletion` from D3-T2) next.
- 2026-04-19 18:55: [execute] M40 Wave 0a — D1 Task 1 config loader. `bin/gsd-t-orchestrator-config.cjs` exports `loadConfig({projectDir, cliFlags, env})` with strict precedence default < `.gsd-t/orchestrator.config.json` < CLI < env (per wave-join-contract v1.0.0). Defaults: maxParallel=3, workerTimeoutMs=270000, retryOnFail=true, haltOnSecondFail=true. Rejects maxParallel > 15 (Team Mode §15 ceiling), maxParallel < 1, workerTimeoutMs < 1000, non-integer env values, corrupt config file. 11/11 unit tests pass (test/m40-orchestrator-config.test.js). Full suite: 1259/1259. D1 Tasks 2-3 (queue, worker) next.
- 2026-04-19 18:30: [execute] M40 Wave 0a — D3 Task 1 contract freeze. `completion-signal-contract.md` v1.0.0 reviewed and frozen: 5 Done Signal conditions all testable from orchestrator side without worker cooperation, retry policy explicit (first fail → retry with "previous attempt failed" preamble, second fail → halt wave, timeout = FAILED retry-eligible), `assertCompletion({taskId, projectDir, expectedBranch, taskStart, skipTest, ownedPatterns})` → `{ok, missing, details}` API matches D1/D2/D6 consumers, owned patterns sourced from tasks.md (not scope.md prose) — eliminates ambiguity on "uncommitted changes in owned scope". Downstream D1/D2 unblocked to cite the frozen contract. D3 Task 2 (assertCompletion impl) next.
- 2026-04-19 18:00: [milestone-planned] M40 PLANNED — 28 tasks across 7 domains. Wave 0 (foundation+gate): 15 tasks in 5 sub-groups (0a independent starts: D3-T1, D1-T1, D2-T1, D0-T1; 0b same-domain unlock; 0c cross-domain integration; 0d orchestrator CLI ready; 0e THE GATE = D0-T2 benchmark driver → D0-T3 verdict). Wave 2 (after D0 PASS): D1-T6 full worker. Wave 3 (stream feed): D4 Tasks 1-5 + D5 Tasks 1-5 + D1-T7 stream wiring. Wave 4 (recovery): D6 Tasks 1-4. REQ traceability appended to `docs/requirements.md` with 5 M40 REQs (REQ-M40-01 through REQ-M40-05), all mapped to tasks. Scope-validation: no task >5 files, no cross-domain dep >3 per task. No duplicate operations across domains (D1/D2/D3 roles are orthogonal; D4/D5 split by transport vs rendering). Tests baseline 1240/1240 pass. Ready for Wave 0 execute.
- 2026-04-19 17:45: [milestone-partitioned] M40 PARTITIONED into 7 domains across 5 waves. D0 speed-benchmark (KILL-SWITCH GATE — Wave 0), D1 orchestrator-core, D2 task-brief-builder, D3 completion-protocol (contract-only, lands first), D4 stream-feed-server, D5 stream-feed-ui, D6 recovery-and-resume. Wave 0 assembles D3 contract + D1/D2 minimal slices + D0 benchmark run — if benchmark FAIL, milestone halts before D4/D5 ever start (per user: "It's not worth doing if it doesn't run at least as fast[er]"). 4 new contracts written: task-brief-contract.md v1.0.0, completion-signal-contract.md v1.0.0, stream-json-sink-contract.md v1.0.0, wave-join-contract.md v1.0.0. Existing code dispositions locked in: `bin/gsd-t-unattended.cjs` INSPECT, existing events JSONL USE, `scripts/gsd-t-agent-dashboard*` (untracked, 424+1043 LOC) INSPECT-then-decide for D4/D5 promotion-or-rewrite, `bin/model-selector.js` + `bin/token-budget.cjs` NOT USED inside workers (one-shot workers don't need context meter), `commands/gsd-t-execute.md` UNCHANGED, unattended supervisor UNCHANGED. Five ambiguity-locks recorded (claude.ai-style = visual conventions not clone; streaming = replay+tail; zero-token = no LLM at all; parallelism = child_process.spawn; recovery = operator-initiated not auto). Baseline tests: 1240/1240 pass. Domains ready for `/gsd-t-plan` to fill tasks.md files.
- 2026-04-19 11:00: [milestone-defined] M40 — External Task Orchestrator + Streaming Watcher UI committed to progress.md. Target v3.14.10 (minor bump, new major feature). Rationale: M8-sized work exceeds single-session context; in-session compaction loses cross-file reasoning; unattended is 5–10× slower with 5-min visibility gap. Solution: external node orchestrator spawns short-lived `claude -p` workers (one task per spawn = no compaction possible), pipes stream-json to a dumb local watcher UI (`localhost:7842`) that renders claude.ai-style continuous feed at zero Claude token cost. Per-wave Promise.all parallelism (default 3, max 15 per Team Mode §15) — true process-level, not subagent-level. D0 speed-benchmark gate built first: if orchestrator ≯ in-session on same domain, kill M40 before D4/D5 UI work. Seven proposed domains (D0–D6) — partition will validate/adjust. Ready for partition.
