# GSD-T Progress

## Project: GSD-T Framework (@tekyzinc/gsd-t)
## Status: ✅ ACTIVE — last shipped v5.18.10 (2026-09-03): **M115 Test-Plan-First Requirements Interrogation COMPLETE** — `/gsd-t-test-plan` enumerates every test case a milestone's requirements imply BEFORE build (rules E1-E8), surfaces unfillable rows as requirements gaps, batches questions into one round, halts on non-convergence, binds acceptance criteria to plan rows, and re-enumerates after the fact. Blind replay against the TimeTracking rate-ledger answer key run CLEAN (answer key held out) — the same gaps found. Seven verify runs; one shared plan reader. Prior history in `.gsd-t/milestones/m115-test-plan-first-2026-09-03/progress.md`. Doc-First Enforcement remains PARKED (pseudocode committed). Next: `/cpua`, then the next milestone.
## Date: 2026-09-03 18:30 PDT
## Version: 5.18.10

## Current Milestone

None — ready for next milestone.

## Completed Milestones

| Milestone | Version | Completed | Tag | Summary |
| M100 Universal Trace + Audit Logging | 4.20.10 | 2026-07-08 19:40 PDT | v4.20.10 | Trace + audit logging as GSD-T framework defaults. 5 file-disjoint domains (d1 storage-scaffolder+human-approval-PAUSE / d2 trace-module+distiller / d3 structural verify-envelope gate / d4 audit-module (fresh SQLite, append-only triggers+sentinel+self-heal, admin query) / d5 CLAUDE.md rules+migration+UMI pilot); d2/d4 share NO file → no-collapse mechanized. Symmetric trace opt-out added (stateless CLIs); GSD-T self-opts-out of both. Plan-hardening pre-mortem converged 7→4→3→2→0 (16 built-but-broken/unwired defects caught before code). Verify triad 3 fix-cycles: Red Team found+FIXED 2 buyer-PII leaks (emitTrace only scanned data; EMAIL_RE missed delimiter-adjacent emails), a DB-store gate no-op (closed flat-file allowlist skipped SQLite stores), in-app immutability bypasses, and a phone-regex false-positive that blocked builds. VERIFIED-WITH-WARNINGS: audit immutability is trigger-based DEFENSE-IN-DEPTH, NOT cryptographic tamper-proofing vs a hostile process with direct .db-file write access (fundamental SQLite limit; scoped OUT per audit-logging-contract §KNOWN LIMITATION; tamper-evidence hash-chain → backlog #48). UMI-Automation greenfield pilot committed on its own main (TS bootstrapped, trace.ts+audit.ts wired, plan-grounded categories/actions). Promotes backlog #44+#45. Suite 2764/0/13-skip. Built on branch m100-define-trace-audit-logging (behind main's stories work — merge needed). |
| M115 Test-Plan-First Requirements Interrogation | 5.18.10 | 2026-09-03 18:30 PDT | v5.18.10 | `/gsd-t-test-plan`: enumerate before build (E1-E8), gaps → one question round, halt on non-convergence, plan-row traceability, `--after` mode; blind replay run clean; one shared plan reader after 7 verify runs |
| M99 Graph Observability & Consolidation | 4.14.10 | 2026-06-30 14:13 PDT | v4.14.10 | All graph artifacts (`graph.db`+WAL/SHM, `index.scip`, `index-python.scip`, telemetry `logs/`) consolidated under `.gsd-t/graphDB/` via one resolver (`bin/gsd-t-graph-store-resolver.cjs`, zero raw `graph.db`/`.scip` literals survive outside it) + a copy-verify-swap migration shim (WAL-checkpoint(TRUNCATE) before copy → self-contained `.db`, sidecars-renamed-before-`.db`, idempotent, interruption-safe, real-root-only, never-orphan; fires first-touch + during `update-all`). The graph-vs-grep DECISION is observable: a toggleable (`GSDT_GRAPH_TELEMETRY`), size/entry-capped+rotated, fail-open append-only ledger at `graphDB/logs/graph-events-NNN.jsonl` records every graph query (verb/outcome/tier/latency/stale-reindex/consumer), every grep-intercept decision (structural→graph-replaced vs text→passthrough), every read-intercept decision, and each of 6 workflows' wiring mode (WIRED/fallback-announced/disabled) — surfaced via `gsd-t graph metrics` (hit-vs-passthrough ratio, fallback-rate, p50/p95 latency, tier mix, per-consumer/verb, `fallbackAnnouncedDespiteHit` north-star). **3 file-disjoint domains / 2 waves** (D1 resolver+migration+sink serial gate; D2 decision-logging ∥ D3 rollup). The orthogonal-triad verify reached the triad across rounds 3+4 and caught + FIXED **3 real HIGH bugs** — all with regression tests: (1) migration silently dropped uncheckpointed WAL rows on interruption (reproduced 4000→2000) → wal_checkpoint+sidecars-first; (2) `gsd-t graph metrics` crashed on prototype-pollution consumer/verb labels (`__proto__`/`constructor`) → `Object.create(null)` accumulators; (3) SC#1 falsified — SCIP indexes written to `.gsd-t/` not `graphDB/` (the no-raw-literals test only grepped `graph.db`, missed `.scip`) → `resolveScipPath` + mkdir-before-write. Plus a casing bug (scan emitted lowercase `"wired"` vs rollup `"WIRED"` → WIRED:0). Two environmental verify false-blocks also fixed: m44 live-repo test hang (slow-gated) + verify-gate `tsc`-on-scip-stub false-FAIL (run `tsc` only when the binary is installed). Suite green (2600+). Pre-mortem earlier caught the milestone-KILLING `:107` invisible-fallback (the discovery loop wasn't in the rewire list) before a line shipped. Shipped after user opted to skip a final Red Team re-pass once the 3 HIGHs were fixed+tested. |
| M93 Brevity Guard | 4.9.11 | 2026-06-23 13:12 PDT | v4.9.11 | Conciseness ENFORCED, not requested — the user asked for concise/jargon-free replies for weeks and prose rules kept getting ignored (a "no process narration" rule existed and was violated). GSD-T only obeys deterministic GATES, so the fix is a gate. **3 file-disjoint domains, 1 wave.** (1) `scripts/gsd-t-brevity-guard.js` — a BLOCKING `Stop` hook: QUESTION (text reply) → block 2+ stacked process-narration sentences before the answer, or a bare unglossed jargon code; ACTION (turn changed code) → allow intent-first (user can short-circuit a wrong direction); **FAIL-OPEN always** (never gags legit work). (2) Reader Contract in CLAUDE-global + qa/red-team/pre-mortem/blind-adversary prompts (the default). (3) `bin/gsd-t-jargon-lint.cjs` — flag unglossed jargon in docs (the file surface the hook can't reach). Installer + PROJECT_BIN_TOOLS for propagation; **wired LIVE into `~/.claude/settings.json` (active now)**. **Verify: VERIFIED-WITH-WARNINGS** — QA PASS (47/47, 0 shallow, 4 mutations caught incl fail-open); Red Team GRUDGING-PASS (opus) — exhaustively attacked fail-open, it HELD; 2 findings FIXED in-verify (jargon over-flag of common acronyms; loop-guard truthy-hardened) + a detector gap caught at integrate (ack-lead + stacked narration scored 0 → fixed). Dated banner kept (user request). Suite **2230 / 2226 pass / 0 fail / 4 skip**. |
| M92 Understand-Before-Build, paradigm half (#44a) | 4.9.10 | 2026-06-23 11:38 PDT | v4.9.10 | The cheap, NO-GRAPH paradigm half of the BinVoice over-scoping fix (#44b graph half = separate gated milestone, carries the M20–M21 prior-graph autopsy). **3 file-disjoint domains, 1 wave, single session.** Root cause: GSD-T was a purely ADDITIVE gate pipeline — plan too high → every gate inflates; the verdict couldn't even SAY "smaller." **3 moves:** (1) M90 §2 arch-trigger gets a cheaper-first **look→smallest→spike→defer** ladder (`look`=new DEFAULT, `spike` DEMOTED; R-ARCH-4/5/6 + backward-compat envelope preserved; spike-feasibility decider stays backlog #42 — deliberately out of scope, the brief's thesis is the cheap "look" rung resolves most cases). (2) **verdict can say "smaller"** — `bin/gsd-t-shrink-metric.cjs` (deterministic `git diff --numstat` → `{netLoc, leaner}`, MEASURED) + ADDITIVE `shrink` dimension on VERDICT_SCHEMA (enum untouched). (3) **default inverted** — milestone/quick lead with smallest-altitude, ceremony opt-in. Doctrine contract §2.2 updated. **Verify: VERIFIED-WITH-WARNINGS** — QA PASS (3 mutations caught, §2.2 compliant); Red Team FAIL→**1 HIGH FIXED in-verify** (git-arg injection in `shrink-metric --range`: `--output=<path>` overwrote arbitrary files via LLM-derived range; dash-leading reject + `--` + 3 security tests; exploit verified closed) on opus (Fable unavailable→tier-substituted). Suite **2183 / 2179 pass / 0 fail / 4 skip**. Closes #44a. |
| M91 PseudoCode Source-of-Truth (M87+M88) | 4.8.10 | 2026-06-22 18:48 PDT | v4.8.10 | Merged M87 (already plan-cleared) + M88 (4 downstream gates). **8 file-disjoint domains, 3 waves, single-session build.** The intention-first `PseudoCode-[Title].md` behavior map (two altitudes: approach → detailed) is the milestone source-of-truth, enforced by 5 deterministic gates — every one meeting the bar (deterministic code, ZERO LLM, structural-not-substring, killing test vs byte-verbatim binvoice fixtures, fail-closed): **Wave 1** `bin/gsd-t-guard-map.cjs` `[RULE]` guard-map verify gate (FAIL-blocking before triad, §7 doc+map discovery, distinct logged skips) — the KILL GATE, A1 22/22. **Wave 2** section-citation traceability (`gsd-t-traceability-gate.cjs` +307, slug-as-slug + `--domains` scoping fix + path-containment) · two-altitude flow (`gsd-t-milestone.md` + `gsd-t-phase.workflow.js`, competition-altitude shift) + keep-or-supersede prompt · `templates/PseudoCode-spec.md` + doc-ripple A4 region-scoped lint. **Wave 3 (M88)** sign-off `isDefined` (`gsd-t-milestone-state.cjs`) · build→map derivation (`gsd-t-guard-map-derive.cjs`) · triad-consumption seam (`gsd-t-rule-consume.cjs` + qa/red-team ingest directives) · divergence-grammar round-trip (`gsd-t-divergence-grammar.cjs`). Count-drift caught at build (PayPal 13→11 rules, byte-verbatim fixture wins). **Verify: VERIFIED-WITH-WARNINGS** — QA PASS (mutation-tested 3 gates, 0 shallow, fixtures verified), Red Team GRUDGING-PASS on opus (Fable unavailable → tier-substituted not skipped); MEDIUM-1 path-traversal FIXED in-verify (`containedChild` + 3 tests); fence-awareness MEDIUM/LOW → backlog #45 (all fail-closed). Suite **2131 / 2127 pass / 0 fail / 4 skip**. Contract `pseudocode-source-of-truth-contract.md` v1.1.5 STABLE. M87 + M88 both CLOSED. |
| M90 The Unproven-Assumption Doctrine | 4.7.10 | 2026-06-22 16:03 PDT | v4.7.10 | 4 file-disjoint domains, 3 waves. Doctrine = **externalize + force, never introspect** (discuss-sourced: self-introspection is unreliable, worse on RLHF models — arxiv 2306.13063 / 2310.01798). **Three deterministic mechanisms:** (1) `bin/gsd-t-research-gate.cjs` factual classifier — premise-CORRECTED (the partition's "vendor list causes silent-miss" was FALSE on disk; vendor list KEPT as an external→web upgrade) + NEW time-anchored override (R-FACT-3); (2) `bin/gsd-t-loop-ledger.cjs` non-convergence halt — symptom-keyed (R-LOOP-1, catches binvoice whack-a-mole), full-reset on genuine re-examination, detection≠resolution, per-milestone scoped, plain-object-validated, atomic write; (3) `bin/gsd-t-architectural-trigger.cjs` divergence + extend-existing trigger (EXPERIMENTAL+MEASURED; R-FAIL-2 response-mode enforcement honestly DECLARED interface-only — no live producer this milestone, backlog #42). Wired fail-closed into debug/execute/quick/phase/verify (§4 R-FAIL-1/2/3). Doctrine contract v1.0.0 STABLE absorbing auto-research v1.3.3 + §6 guard map. **Process — the doctrine proved on itself:** plan-hardening BLOCK×3 caught M90 building D3 on an unproven premise (re-verified on disk, re-scoped); verify ran **9 Red Team cycles** that hardened the gate-wiring lifecycle to ground (hollow-gate → fail-open → no-producer → 4 lifecycle flaws → vacuous-gate → array-type-confusion → cross-milestone-brick → symptom-keying), each strictly narrower, code-review nits-only the last 4. User decisions A (interface-only), detection≠resolution, per-milestone-scoping, symptom-keying. Completed straight-line per user directive after 9 rounds. Suite **1998 / 1994 pass / 0 fail / 4 skip**. Deferred (documented): backlog #42 (live spike producer + sink fail-closed), #43 (AWS/S3 classifier edge), 3 code-review nits. |
| M86 Model Profiles | 4.5.10 | 2026-06-10 22:28 PDT | v4.5.10 | standard/pro/premium tier-spend switch over M85 STAGE_TIERS — per-project config + gsd-t model-profile CLI + invoke-time args injection into all 10 invokers + wave forwarding; drift lint unwraps ?? forms (8 negatives); profile surfaced named everywhere. 4 domains (auto-competition winner), 1 wave, 1 day. Plan-hardening 7→5→0; verify = 3 Red Team fix cycles (prototype-key bypass, live self-propagation clobber → installer source-repo guard, config-blind resolve form). Live frame evidence: standard zero-fable, override-beats-profile, premium red-team-fable. Suite 1598/0. Contracts: tier-policy v1.1.0, profile-config v1.0.0 STABLE (new). VERIFIED-WITH-WARNINGS (publish-ordering + pro census deferral). |
| M85 Model-Tier Policy + Fable 5 | 4.4.10 | 2026-06-09 18:04 PDT | v4.4.10 | 4 file-disjoint domains, single-day single-session build on Fable-5-as-lead. **Headline**: `bin/gsd-t-model-tier-policy.cjs` — single source of truth for tier assignments (frozen MODEL_IDS + STAGE_TIERS, requiresThinkingOmitted predicate incl. live bracket-suffix form, resolve() + CLI envelope, `gsd-t model-tier-policy` dispatcher + dual bin-propagation). Live-bug fix: parallel alias map was `opus→claude-opus-4-7`; now require()s the module, zero bare literals; cache-warm `--model` flag fix (env pin measured silently ignored). 5 Fable assignments (M84 probes, judge, M83 pre-mortem, Red Team, debug cycle-2 ternary; producers HELD opus). Drift lint 44/44 (label-keyed, negative + real-file + debug-ternary meta-tests). **MEASURED A/B**: Fable single-draft TIED judged 3-Opus competition at 42% cost ($4.74 vs $11.24; order-reversed referees split → position bias > gap; n=1). AC(c) ledger: 5/6 fable lines live (partition-probe banks at first M86 partition). **Process**: 4 pre-mortem cycles (13 findings; 2 real defects found by measurement) + explicit human gate adjudication (TD-294 gate-quiescence registered, TD-295 narrative-vs-verdict, TD-296 stale brief). Verify round 1 FAIL (Red Team HIGH: unwired resolver surface) → fix-cycle 1 → round 2 VERIFIED-WITH-WARNINGS, warnings closed. Suite 1462/0 (+~70 M85 tests), E2E 3/4+placeholder. Contracts: model-tier-policy v1.0.0 STABLE (new), model-selection v1.0.0→v1.1.0. Bonus: settled L1–L5 retro-agent design (CONTEXT.md) from the measurement run. |
|-----------|---------|-----------|-----|---------|
| M58 Test Data Cleanup Gate | 3.28.10 | 2026-05-27 | v3.28.10 | 2 file-disjoint domains, single-day in-session build. **Origin**: GSD-T-Board v0.1.10 Verify left 2442 orphaned `E2E_TEST_*` / `E2E_DRAG_*` ideas live in the production data store — verify passed because GSD-T had no convention for tracking or purging test data inserted during the suite. **D1 `gsd-t test-data`** (`bin/gsd-t-test-data-ledger.cjs`): append-only JSONL ledger + 3 built-in adapters (`bin/gsd-t-test-data-adapters/{localstorage-key-prefix,file-json-array,sqlite-table-where}.cjs`) + CLI dispatch via `bin/gsd-t.js`. Defense-in-depth: every adapter refuses to delete a record whose id doesn't start with the ledger row's `taggedPrefix`. **D2 `withTestData()` Playwright fixture** (`templates/test-helpers/test-data-fixture.ts` + README): tag composition `{PREFIX}_{runId}_{counter}`, reads `GSD_T_VERIFY_RUN_ID`, opt-in `purgePerTest`. **Integrate**: `commands/gsd-t-verify.md` new Step 4.5 (FAIL-blocking, runs `gsd-t test-data --purge --run "$GSD_T_VERIFY_RUN_ID"` after E2E + before VERDICT) + verify-report `Test Data Cleanup` line + doc-ripple (CLAUDE-global, ~/.claude/CLAUDE.md, README, help, .gitignore, CHANGELOG). **SCs 7/7 PASS**: ledger records 5 ✅; purge removes 5, reports `purged.length===5` ✅; planted adapter throw → `errors[]` → FAIL ✅; clean E2E → `purged=5 skipped=0 errors=0` ✅; **2649/2649 unit tests (baseline 2587, zero regressions)** ✅; Red Team **6/6 attacks defended** ✅; doc-ripple complete ✅. Contracts: `test-data-ledger-contract.md` v1.0.0 STABLE + `test-data-tagging-contract.md` v1.0.0 STABLE. |
| M57 CI-Parity Verify Gate | 3.27.10 | 2026-05-19 | v3.27.10 | 2 file-disjoint domains, in-session serial rebuild. Origin: TimeTracking v1.10.12 post-mortem (verify reported VERIFIED+tagged while Cloud Build failed — new `hooks/` dir absent from Dockerfile COPY + noImplicitAny passed warm-cache local tsc). **D1 `gsd-t build-coverage`** (commit 1755da5): structural CI parse — Dockerfile COPY/ADD source-arg (incl. relative `--from=` = coverage, absolute = image fs), cloudbuild `args`-positional, workflow `run`/`working-directory`-positional via block-scalar-aware YAML walker; never substring; node_modules never coverage; 19/19 tests incl. one per FROZEN bug* falsification fixture. **D2 `gsd-t ci-parity`** (9f0ca13): detection precedence + containment-safe `clearBuildCaches` (predicate `resolved.startsWith(root+path.sep) && resolved!==root` — refuses outside-AND-equal-to projectRoot + prefix-collision; `refusedPaths[]`; mandatory-path cache-clear closes BUG-2); 18 tests + SC2 self-skip-no-docker. **Integrate** (b7c3f42): `gsd-t.js` dispatch + GLOBAL_BIN propagation; `gsd-t-verify` Step 2.6 FAIL-blocking gate; doc-ripple CLAUDE-global/README/help. **Process**: first design (substring + unguarded cache-clear) FAILED Red Team across 5 non-converging cycles + 5 CRITICAL Destructive-Action-Guard violations; autonomous chain halted (Prime Rule stop #2); detached fan-out harness false-completed twice (STABLE contracts over un-rewritten code); rebuilt in-session serially — corrected structural design converged first attempt. Suite 2587 pass / 0 fail. Contracts cli-build-coverage v2.0.0 + ci-parity v2.0.0 STABLE. Memory: feedback_coverage_check_structural_not_substring, feedback_destructive_path_ops_containment, feedback_detached_fanout_false_completion. Pair-flag backlog #25 (gsd-t-bench eval set). |
| M54 Live Activity Visibility | 3.24.10 | 2026-05-07 | v3.24.10 | 2 file-disjoint domains, 8 tasks total, single-day in-session build. **D1 server-and-detector** (5 tasks): `bin/live-activity-report.cjs` (new, 615 LOC, pure read-only, zero deps) — `computeLiveActivities({projectDir,now?})` detects 4 kinds (bash run_in_background, Monitor tool_use without stop, tool_use >30s, spawn-plan endedAt:null); 3 falsifiers (F1 terminator event, F2 process.kill PID ESRCH, F3 source-file mtime >60s); cross-stream dedup by tool_use_id then (kind,label,startedAt) tuple; UNION of `.gsd-t/events/<today>.jsonl` + `~/.claude/projects/<slug>/<sid>.jsonl`. 3 dashboard handlers added to `scripts/gsd-t-dashboard-server.js` (additive — handleLiveActivity 5s cache, handleLiveActivityTail with isValidActivityId path-traversal guard, handleLiveActivityStream SSE 15s heartbeat). 1-line `GLOBAL_BIN_TOOLS` edit in `bin/gsd-t.js`; module hot-patched to `~/.claude/bin/`; `gsd-t doctor` reports "All 2 global bin tools installed". `live-activity-contract.md` flipped v0.1.0 PROPOSED → v1.0.0 STABLE. **D2 rail-and-spec** (3 tasks): additive `<section id="rail-live-activity">` + CSS `@keyframes accent-pulse` (.la-pulsing class) + `wireLiveActivity()` IIFE in `scripts/gsd-t-transcript.html` (5s polling, appendActivity/removeActivity/updateDuration/loadTailUrl/stopPulse, 3 pulse-stop conditions: click/absence/30s); 2 live-journey specs under `e2e/live-journeys/` (single-bash + multikind, both self-skip when no live dashboard); manifest +2 entries. All 3 checkpoints PUBLISHED. Red Team GRUDGING PASS: 5/5 broken patches caught (dedupe-disabled, PID-stub-true, mtime-fallback-removed, pulse-never-clears, tool_use_id-collision-unhandled). Suite **2262/2262 unit pass** (baseline 2233 + 29 M54 D1 new); Playwright **39 pass + 23 self-skip** in 2.6s. `gsd-t check-coverage` reports `OK: 21 listeners, 16 specs` exit 0. Goal-Backward PASS (12 REQs, 0 placeholders). |
| M52 Rigorous User-Journey Coverage + Anti-Drift Test Quality | 3.23.10 | 2026-05-06 | v3.23.10 | 2 file-disjoint domains, 10 tasks total (5 D1 + 5 D2), single-day in-session build. D1 journey-coverage tooling: `bin/journey-coverage.cjs` (308 lines, regex listener detector, zero parser deps, sub-100ms) + `bin/journey-coverage-cli.cjs` (`gsd-t check-coverage` CLI, exit 0/4/2) + `scripts/hooks/pre-commit-journey-coverage` (mode 0755, marker-block idiom) + `bin/gsd-t.js` wiring (+46 lines: `installJourneyCoverageHook`, `check-coverage` dispatch, `--install-journey-hook` doctor flag, auto-install in init) + `journey-coverage-contract.md` v1.0.0 STABLE. D2 journey specs + fixtures: 12 inaugural `e2e/journeys/*.spec.ts` covering all 20 detected viewer listeners — every assertion verifies user-visible state change (zero `toBeVisible`/`toBeAttached` shallow assertions); `.gsd-t/journey-manifest.json` 12 entries; 3 real-data NDJSON fixtures + `replay-helpers.ts`; Red Team "Test Pass-Through — Journey Edition" additive category in `templates/prompts/red-team-subagent.md`. Red Team GRUDGING PASS: 5/5 broken viewer patches caught; hook end-to-end exercised (block-then-unblock). Suite: 2195/2195 unit + 35/35 E2E + 1 skip preserved. `gsd-t check-coverage` reports `OK: 20 listeners, 12 specs` (exit 0). |
| M50 Universal Playwright Bootstrap + Deterministic UI Enforcement | 3.22.10 | 2026-05-06 | v3.22.10 | 2 domains file-disjoint, single-day in-session build. Three executable enforcement layers: (1) `bin/playwright-bootstrap.cjs` + `bin/ui-detection.cjs` wired into init/update-all/doctor/setup-playwright; (2) spawn-gate in `bin/headless-auto-spawn.cjs` (auto-install on hasUI && !hasPlaywright, blocked-needs-human on fail); (3) `scripts/hooks/pre-commit-playwright-gate` (opt-in commit gate). Also ships `playwright.config.ts` + 5 E2E viewer specs (M48 Bug 1-4 regressions + M49 banner). 62 new unit tests; E2E 8/9 pass. Goal-Backward PASS. Zero regressions. |
| M47 Focused Visualizer Redesign | 3.21.10 | 2026-05-06 | v3.21.10 | 2 domains file-disjoint, single-day in-session build. D1 viewer-redesign (7 tasks): split-pane HTML + 3-section rail (Main/Live/Completed≤100) + dual SSE + mouse/keyboard splitter + 4 sessionStorage persistence keys + right-rail collapse + reactive Live→Completed transition. D2 server-helpers (5 tasks): 30s-window `status` field on `listInSessionTranscripts` + new `GET /api/main-session` (path-traversal guarded, no-cache) + contract v1.3.0 + 9 regression tests. Suite 2058/2060 (M47 +13/+13, 2 pre-existing flakes preserved). Goal-Backward PASS, Red Team grudging pass (0 bugs across 13 vectors). |
| M46 Unattended Iter-Parallel + Worker Fan-Out Completion | 3.19.00 | 2026-04-23 | v3.19.00 | 2 domains file-disjoint, same-day build. D1 iter-parallel scaffold + contract v1.0.0 + 12 unit tests + proof 3.35×; D2 worker sub-dispatch production path + contract v2.0.0 → v2.1.0 + 6 unit tests + proof 5.96×. Closes audit surfaces 2A + 2B. Production default gated serial pending concurrent-safe work-stealing rewrite (backlog #24). Full suite 1946/1946 pass, zero regressions. |
| M45 Conversation-Stream Observability | 3.18.14 | 2026-04-23 | v3.18.14 | 2 domains, parallel Wave 1. D1 viewer-route-fix; D2 in-session-conversation-capture with contract v1.0.0. Red Team caught session_id path-traversal (fix + regression test). |
| M44 Cross-Domain & Cross-Task Parallelism | 3.18.10 | 2026-04-23 | v3.18.10 | 8 of 9 domains. Task-graph, `gsd-t parallel` CLI, disjointness prover, economics estimator, spawn-plan visibility, per-CW attribution. |
| M43 Token Attribution & Always-Headless Inversion | 3.17.10 | 2026-04-21 | v3.17.10 | 6 domains across 3 waves. Part A universal token attribution; Part B always-headless channel separation. |
| M42 Live Spawn Transcript Viewer | 3.16.10 | 2026-04-20 | v3.16.10 | 3 domains. Claude-Code-style per-spawn transcript on `:7433`. |
| M41 Universal Token Capture Across GSD-T | 3.15.10 | 2026-04-20 | v3.15.10 | 5 domains. `bin/gsd-t-token-capture.cjs` + lint enforcement. |
| M40 External Task Orchestrator + Streaming Watcher UI | 3.14.10 | 2026-04-20 | v3.14.10 | 7 domains. Operator 0.72× wall-clock. |
| M39 Fast Unattended + Universal Watch-Progress Tree | 3.13.10 | 2026-04-18 | v3.13.10 | 3 domains + 6 patches. |
| M38 Headless-by-Default + Meter Reduction | 3.12.10 | 2026-04-17 | v3.12.10 | Default headless across 7 commands. |
| M37 Universal Context Auto-Pause | 3.11.10 | 2026-04-16 | v3.11.10 | MANDATORY STOP across 5 commands. |
| M36 Cross-Platform Unattended Supervisor Loop | 3.10.10 | 2026-04-15 | v3.10.10 | Detached supervisor relay. |
| M35 Runway-Protected Execution | 2.76.10 | 2026-04-15 | v2.76.10 | Surgical model selection. |
| M34 Context Meter | 2.75.10 | 2026-04-14 | v2.75.10 | Real CW measurement. |

Older milestones (M33 and earlier) archived under `.gsd-t/milestones/` — see directory listing for the full index.

## Blockers

<!-- No active blockers -->

## Decision Log
- 2026-09-03 18:50 PDT: [release] **v5.18.10 published and propagated (32 projects) — M115 shipped.** First `update-all` after the tarball install copied NOTHING ("33 already current") and the global package was found back on 5.17.14 afterwards: `~/.claude/.gsd-t-version` still said 5.17.14 because the home install (`gsd-t install`: commands + version file) had not been refreshed. Order that works, now in `commands/cpua.md`: tarball install → on-disk check → `gsd-t install` → version-file check → `update-all` → on-disk re-check + `ls` a NEW file in a real project. Verified: TimeTracking + binvoice carry `gsd-t-testplan-{rows,lint,halt}.cjs`; `~/.claude/commands/gsd-t-test-plan.md` installed.

> Prior decision log entries preserved in `.gsd-t/milestones/*/progress.md` — see `m115-test-plan-first-2026-09-03/progress.md` for pre-M115 history (279 entries trimmed 2026-09-03).

- 2026-09-03 18:30 PDT: [success] Milestone "M115 Test-Plan-First Requirements Interrogation" completed — `/gsd-t-test-plan` + enumerator protocol (E1-E8) + mold + evidence classifier + testplan-lint + testplan-halt + plan-row traceability + the shared plan reader + front-door wiring; blind replay run clean (answer key held out); 7 verify runs, Red Team GRUDGING-PASS, every finding fixed with a regression test (120 milestone tests, suite 3515/0). Archived to `.gsd-t/milestones/m115-test-plan-first-2026-09-03/`. v5.18.10 (minor: feature milestone).
- 2026-09-03 18:05 PDT: [m115][verify] **Verify run 7: VERIFIED-WITH-WARNINGS — Red Team GRUDGING-PASS (15 attack vectors, nothing found), QA clean (3512/0, E2E 3/3 live specs, no shallow tests), review 2 important + 4 nits — all fixed before completion.** Important: the graph-use gate's one `|| ''` made an UNTIMESTAMPED wired claim attributable to every query ever logged (fail-open) — such a claim now gets nothing; the verify workflow named an unreadable plan FILE's cause as "undefined" (per-doc reason lives in results[].reason) — now formatted per doc. Nits: migrate-logging reuses the envelope checker's manifest reader (a twin had crept back in); the lint runs row checks over every Table section even when the order check failed; the demo-video model list is one model; declared logging-manifest paths must stay inside the project. 3 more tests. Suite green, fallbacks 0. → complete-milestone.
- 2026-09-03 17:45 PDT: [m115][verify-fix] **Verify run 6: Red Team FAIL (2 HIGH) + review (3 important, 3 nits) — the classifier and the loop identity, fixed once each.** HIGH: `GAPX:` was a gap to the lint and halt (`startsWith("GAP")`) and a sourced citation to the gate (`GAP\b`), so it cleared an acceptance criterion — `rowState` in the shared reader is now the ONE classifier (exact tokens, case-insensitive; a cell that looks like a marker but is not one is `malformed`: lint `unknown-source-marker`, gate never clears, halt counts open). HIGH: a blank-Seq gap row was dropped by the halt and the round cap never saw it — blank Seq is a lint violation (`blank-seq`) and OPEN to the halt. Review: the halt counted CALLS not rounds (re-checking round 2 fired the repeat cap) and two plans with the same open set shared one ledger signature (the ledger drops `surface` from the key on purpose) — the doc path is now part of the assertion and a per-project `.gsd-t/testplan-halt-rounds.json` (gitignored, corrupt → halt) makes a re-check of the same round idempotent; duplicate row identities collapsed last-write-wins in the gate (a later sourced row overwrote an earlier GAP) — duplicates are their own never-clearing state and a lint violation; the `## Table:` heading pattern is one shared regex; orphan JSDoc removed. 5 more regression tests (24 in `test/m115-verify-fixes.test.js`). Suite green, fallbacks 0. Verify run 7.
- 2026-09-03 17:20 PDT: [m115][verify-fix] **Verify run 5: Red Team FAIL (1 HIGH, 1 MEDIUM) + review (2 important, 4 nits) — the last three readers of a section that were not fence-aware, now are.** HIGH: `parseDecidedGroup` read a self-answered row's required entry from inside a tilde-fenced block — rendered as code, invisible as a decision, yet the "stays visible" rule passed; MEDIUM: `decidedGroupIsExplicitlyEmpty` counted a fenced "None"; review: `findTableHeader` took a fenced pipe line as the header and then skipped every real row check (a blank-source row passed unreported); the halt message printed `(undefined)` for every open row (`r.reason` vs `r.source`). All four read through `unfencedLines` / `isFenceToggle` from the shared reader; halt names the source; dead `splitRow` / `testPlanTitleFromFilename` removed; the lint's header definition is the shared `HEADER_CELLS`; the demo-video model comment matches the code. 4 more regression tests (19 in `test/m115-verify-fixes.test.js`). Suite green, fallbacks 0. Verify run 6.
- 2026-09-03 17:05 PDT: [m115][verify-fix] **Verify run 4: Red Team FAIL (1 HIGH, 2 MEDIUM) + review (3 important, 2 nits) — root cause is ONE job done three ways; fixed once.** Three tools read a test plan's sections and rows independently: the lint tracked backtick fences but not tilde fences (HIGH — a `~~~`-fenced fake heading satisfied a required section), the halt tool tracked no fences (a fenced `## Table:` became a table), and all three accepted a row with EXTRA cells and then read the wrong cell as column 6 — an extra-cell GAP row passed the lint, CLEARED an acceptance criterion in the traceability gate, and was invisible to the halt. New `bin/gsd-t-testplan-rows.cjs` is the one reader (both fence styles, exact six-cell width, state from cell 6 alone); lint → violation on wrong width, gate → a malformed row is its own state and never clears, halt → a malformed row counts as OPEN (the halting direction). Also: a headline task cleared by a plan row no longer trips `headline-without-test` (the plan IS the test) but still needs an implementing path; a second `## Decided without you` heading is flagged; the demo-video multi-model failover (dead since the quota halt) removed. The rows module joins BOTH bin registries, and `test/verify-gate-tools-propagated.test.js` now asserts every shipped tool's local `require()` siblings also ship (a tool without its sibling is dead in every project). 7 more regression tests (15 total in `test/m115-verify-fixes.test.js`).
- 2026-09-03 16:40 PDT: [m115][verify-fix][td-299] **Verify run 3 failed at the fallback gate on four phantom findings in code nobody touched.** The fence-detecting regex I added to `loadTestPlanRowIdentities` (`/^\s*(```|~~~)/`) contains three backticks; the fallback scanner's string stripper read them as an unterminated template literal and mis-tokenised the rest of the file, so four pre-existing `fieldValueRaw(...) || ""` lines in M83's `assessTask` surfaced as new findings. Regex rewritten with `\x60{3}` (no literal backtick); gate 0 unapproved; logged TD-299 (scanner must recognise regex literals). Re-verifying (run 4).
- 2026-09-03 16:30 PDT: [m115][verify-fix] **Verify run 2: deterministic gates all PASS, QA clean (3488/0, no shallow tests), Red Team FAIL (1 HIGH, 2 MEDIUM, 1 LOW), code review 3 important + 3 nits — all seven fixed directly, 8 regression tests, re-verifying.** HIGH: a `GAP` row in a test plan cleared an acceptance criterion through the new `**Plan-Row**` binding — the exact dead-deliverable shape the traceability gate exists to catch; row identities now carry their state and only `sourced` / `DECIDED-WITHOUT-YOU` rows clear. MEDIUM: a doc title with path segments could escape `.gsd-t/test-plans/` — single-segment titles only, resolved path must stay inside the directory. MEDIUM (= review important #3): the verify workflow treated EVERY exit-64 from testplan-lint as "no plan directory" — an unreadable directory (EACCES) was silently skipped; only ENOENT is "absent", everything else is `testplan-discovery-error` and HALTS. LOW: a `##` inside a fenced block closed the table scope — fences tracked. Review important #1: the lint's section-order walker stalled on any heading the mold did not name (the contract-MANDATED `## HALT` section) and then reported every later required section missing — it now skips unknown headings and checks relative order. Important #2: a valueless `--round` became `Number(true)` = 1 and passed the positive-integer check, defeating the three-round cap — digit strings only. Nits: the test-plan gate is announced as "Test-Plan Gate" (was a second "Guard-Map Gate"); `parseOpenRows` counts rows only under `## Table:` (gaps restated under `## Open gaps` were counted twice); the Decided-group "None" check is positional, not a substring scan. `test/m115-verify-fixes.test.js` 8/8.
- 2026-09-03 16:10 PDT: [fix][td-297] **Verify halted at the M89 research-claim gate on three markers that were never M115's.** `git log -S` shows they entered progress.md in 22b51f0 (my TD-395 round-2 commit): the plan run's research pipeline had appended them minutes earlier — a `[GUESSED]` claim about UMI-Automation (M100, private, un-citable) and two "cited" blocks about SQLite WAL and tree-sitter/SCIP (M94-M99) whose fact-line keys did not match their marker keys. The pipeline re-researches stale claims it finds in Decision-Log prose and writes into whichever artifact the phase touched (TD-297), and the §7 gate scans the whole project, so a completed milestone's leftovers block every later verify. Removed the appended block (86 lines). Commit d2fa895 carried only the TD-297 addendum; this one carries the removal.
- 2026-09-03 15:58 PDT: [fix] **The eight demo-video fallbacks are gone — halts with the remedy, not approvals.** David: "They all sound like fallbacks to me. Are any required? Will I end up with the video I want?" — three of five handed him a different video (trim left pauses in; batch→per-line reintroduced voice drift; quota→model switch changed the voice), one was never diagnosed (trim discarded auto-editor's stderr), one was a retry-then-halt the guard misread. Now: ffmpeg-full broken → halt naming `brew reinstall ffmpeg-full` (4 scripts); trim → auto-editor's exit + stderr surfaced, empty output halts; batch unsplittable → halt (per-line renderer deleted, real fix = deterministic split, backlog #54); daily quota → halt "rerun tomorrow"; transient-error retries and the re-render gate KEPT (they end in a halt). `gsd-t-testplan-lint` gate-error path made an explicit exit-64 halt. Fallback gate: 8 → 0 unapproved; nothing approved by name.
- 2026-09-03 10:15 PDT: [m115][execute] **`front-door-wiring` (Wave 3) complete — A8 front door wired: router slug, CLI dispatch, both registries, verify gate.** `commands/gsd-t-test-plan.md` written as an in-session command following the `/gsd-t-architect` pattern (before-mode + `--after` mode), not a `Workflow({scriptPath})` phase runner — **no domain in M115 owns a `gsd-t-test-plan.workflow.js`; every sibling scope.md confirms the milestone's design is deterministic gates + subagent protocols invoked directly**, so inventing a workflow script to satisfy the task text's generic convention would have shipped a dead `scriptPath` claim, exactly the A8 failure shape this domain exists to prevent. A4 one-round batching stated explicitly, drip named as banned. **A8 test written and confirmed RED first** (`test/m115-a8-front-door-test-plan.test.js`, 4/15 assertions failing pre-wiring on the missing router slug, registry entries, and verify gate) — then `commands/gsd.md` (valid-slugs list + a scope-disambiguation bullet against `plan`) and `bin/gsd-t.js` (`testplan-lint`/`testplan-halt` dispatch cases on the `pseudocode-style` pattern; both tools added to `GLOBAL_BIN_TOOLS` **and** `PROJECT_BIN_TOOLS` — the propagation-gap class shipped dead four times before in this repo) turned it green. **Verify-gate wiring (M115-D5-T4, PM-1) deliberately does NOT copy the guard-map gate's discovery error-handling**: that gate's `.catch(... skips: [{reason:"discovery-error"}] ...)` turns a broken discovery into a silent pass; this gate uses deterministic glob discovery via `runCli` (no LLM discovery agent) and treats `runCli`'s own error shape (`exitCode:-1`) or an unparseable envelope as an immediate FAIL named `testplan-discovery-error`, never a skip. Four outcomes kept distinct and verified against `gsd-t-testplan-lint.cjs --dir`'s REAL output shapes for all 6 cases (dir absent → 64/`reason`; dir exists but empty → 0/`docsChecked:0`; malformed → 4 with violations correlated by `doc` from the top-level `violations` array, since per-result entries carry no violations of their own; clean → 0; agent error; unparseable stdout) — both empty-dir shapes correctly collapse to the same `no-test-plan` skip. **Stated-claim flagged**: `.gsd-t/test-plans/` is recorded as the canonical plan location (parallel to `.gsd-t/pseudocode/`) — no domain fixed this directory anywhere in scope/contract, so it is `[GUESSED:assumed]`, evidenced only by the sibling-directory pattern. Doc ripple: `commands/gsd-t-help.md` (Command Summaries + printed list), `GSD-T-README.md` (new `## Test-Plan-First Requirements Interrogation (M115)` section — this repo's table of `/gsd-t-plan` etc. actually lives in `README.md`, not here, so a themed section was added instead), `README.md` (commands table row), `templates/CLAUDE-global.md` (successor mapping: `test-plan` → `plan`, also `execute` in `--after` mode). Regression suite run after each change (78, then 165 relevant tests) — 0 fail throughout. M115 A8 suite: 15/15 pass. `plan-visibility` and `halt-convergence` share no file, but they share contract §2.2's marker literals: the mold shows them in place, the halt tool's `parseOpenRows` reads column 6 with `startsWith("GAP")`. The mold's example rows wrote `` `GAP` — … `` and `` `DECIDED-WITHOUT-YOU` — … ``, and a cell that starts with a backtick does not start with the literal — `parseOpenRows(mold)` returned `[]`, so a plan copied from the mold would converge silently (the round cap can never fire on rows it cannot see — the exact corruption this milestone exists to prevent). The real producer already writes the literal bare (the A1 cold run: 57 open rows parse; the enumerator protocol and the A3 fixture likewise), so the fix is on the mold's side, not a second accepted form in the parser: backticks stripped from the two example cells (`templates/TestPlan-spec.md` rows 2–3). Seam asserted mechanically in `test/m115-integrate-mold-halt-seam.test.js` (3 tests — the mold's GAP row IS open to the halt tool; a re-backticked marker is NOT the literal; the self-answered row is not counted as open), so it fails if either side drifts. Wave 3 note for `deterministic-gates`: the lint must exit 0 on this mold's example (D4-T2), and now the same example reads correctly to the halt tool — the two gates agree on what a marker is. Halt-convergence's stated claim (`SYMPTOM_REPEAT_CAP = 2` as the plain reading of "twice") is consistent with the pseudocode's `[RULE] same-symptom-twice-halts` and left as shipped. M115 suite 49 pass / 0 fail; `bin/gsd-t-loop-ledger.cjs`, `test/m83-*`, `test/m87-*` byte-identical.
- 2026-09-03 09:54 PDT: [m115][execute] **`halt-convergence` (Wave 2) complete — A5 the non-convergence halt shipped.** `bin/gsd-t-testplan-halt.cjs` implements both loop caps entirely by CONSUMING `bin/gsd-t-loop-ledger.cjs` through its frozen exports (`appendCycle`) — `git diff --stat bin/gsd-t-loop-ledger.cjs` is empty and asserted mechanically by the test. `[RULE] enumeration-loop-cap-three`: at round 3 with any row still `GAP`/`GAP:CONTRADICTION` in column 6 (contract §2.1/§2.2), it HALTS (exitCode 4) naming every still-open row by table + Seq — never a bare "blocked". `[RULE] same-symptom-twice-halts`: the round→cycle mapping (`roundToCycleSignature`) derives the ledger's signature inputs from the doc's own sorted open-row set when the caller supplies no explicit assertion/surface/fileClass, so the cap works from `--doc` alone; **design decision found during testing** — the ledger's own `HALT_THRESHOLD` is fixed at 3 cycles (a different rule for its own debug-loop use case), so "twice" is read from the ledger's returned `cycles` count against this gate's own `SYMPTOM_REPEAT_CAP = 2`, still fully delegating signature computation/persistence. A second decision found the same way: the repeated-symptom cap is gated on `openRows.length > 0` — a converged round (zero open rows) is never fed to the ledger at all, which is what lets a fully-closed plan be re-checked any number of times without tripping a "repeat" that was never a failure. Exit triple matches contract §4 exactly (0 clean / 4 halted / 64 bad input, one JSON envelope, never throws past the CLI boundary — a single try/catch at `main`). `test/m115-a5-non-convergence-halt.test.js` (13 tests, real subprocess via `spawnSync` so the exit codes are what a caller actually sees): both caps' HALT/no-HALT boundaries, `GAP:CONTRADICTION` recognized alongside `GAP`, explicit-identity mode, five bad-input shapes incl. a directory-as-`--doc` and a corrupted ledger state file forcing the internal-error path, and the ledger byte-identity check. Each of the four cap-boundary assertions verified by mutation (disable each cap / make each cap over-eager) to actually fail without the corresponding guard — not merely present. Files owned by `plan-visibility`/`deterministic-gates`/`front-door-wiring` untouched; this domain does not register itself (no changes to `bin/gsd-t.js`, `templates/workflows/gsd-t-verify.workflow.js`, or the contract).
- 2026-09-03 09:46 PDT: [m115][execute] **`plan-visibility` (Wave 2) complete — A3 self-answer visibility + A6 evidence-or-halt shipped.** `templates/TestPlan-spec.md` is the blank mold (sibling of `PseudoCode-spec.md`): title+purpose, `## Decided without you` (always present, `None — every row is sourced.` when empty), one or more six-column sequence tables matching contract §2 exactly, `## Open gaps`, `## Sign-off`, with one filled example row per row state (`sourced`/`DECIDED-WITHOUT-YOU`/`GAP`). The five A3 rules are stated in the mold's HOW-TO-WRITE comment block as machine-checkable structural statements (heading-before-first-table, heading-present-even-empty, every self-answered row individually sourced under the heading, group is exactly the self-answered set). `templates/prompts/test-plan-evidence-classifier.md` is the A6 protocol: three arms (code-is-wrong / rule-is-wrong / cannot-tell), no default, every arm cites evidence, cannot-tell is a HALT that escalates into the question round rather than picking a side on conflicting-but-citable evidence; §2 mirrors the same three-way decision at row-fill time. `test/m115-a3-self-answer-visibility.test.js` (6 tests) and `test/m115-a6-evidence-or-halt.test.js` (21 tests) prove both rule sets structurally (headings/rows/columns, never substring search), each with a negative case per rule so a removed guard fails the corresponding test — verified by running clean, no implementation of the Wave-3 lint binaries required. Files owned by `deterministic-gates`/`halt-convergence` (`bin/gsd-t-testplan-lint.cjs`, `bin/gsd-t-testplan-halt.cjs`) untouched.
- 2026-09-03 09:45 PDT: [m115][decision] **David: proceed past the Wave-1 halt ("Yes, continue"); rule E7 kept as written.** Hit condition 1 re-written to the gap's shape (a rate change silently alters already-issued invoice figures) rather than the fix's (a closed-month state + reopen) — recorded as a ⚠ Divergence in `PseudoCode-TestPlanFirst.md` and in `hit-conditions.md`, with PM-2's no-rewrite rule knowingly set aside once. Case-space bound raised 94/run → 180 per feature area per run, evidenced by the scoped clean run (94 written + ~79 un-reached). Wave 2 (plan-visibility ∥ halt-convergence) launched. Eight demo-video fallbacks deferred to verify.
- 2026-09-03 09:20 PDT: [m115][halt] **A1 blind replay, re-run CLEAN, scores 2 of 3 — HALTED at the Wave-1 gate for premise re-examination, as designed.** The Wave-1 worker's own pass was NOT blind: its protocol carried the three hit conditions and worked examples drawn from the answer key's domain (rates, closing a month, "the owner cannot be deactivated"); the worker flagged the risk itself and grep confirmed it. Fixed: protocol worked examples now from a lending-library domain, hit conditions moved to `test/fixtures/m115-blind-replay/hit-conditions.md` (scoring side). Clean re-run = headless `claude -p` (Opus 5) from a scratch dir holding ONLY the generic protocol + `requirements-before-review.md`, no project memory (this machine's memory index names the three gaps verbatim, so an in-project subagent would be contaminated). Run 1 (unscoped, whole 571-line doc): hit the 94-case bound on F1/F2/F3/F5 before reaching v1.27 at all — the real command always names its milestone, so run 2 named the `## v1.27` section as the area under test. Run 2: 94 rows, 57 GAP, 4 DECIDED-WITHOUT-YOU. Scored against the pre-registered conditions: **gap 2 (wrong permission model) HIT** — screen vs endpoint rows 61-80, incl. "rate columns must be stripped, not merely hidden"; **gap 3 (owner cannot be deactivated) HIT** — row 131 "P is the LAST ADMIN → deactivate → must be REFUSED, GAP:CONTRADICTION with F4's delete rule", plus self (132) and sole-assignee (129); **gap 1 (month close + reopen) NEAR-MISS = MISS under the pre-registered rule** — rows 37/59a/101/102 surface the underlying defect ("already-issued invoices for the affected period now resolve to a DIFFERENT figure — GAP: unstated rule") but no row has a closed-period STATE as subject and none addresses reopen, because the pre-review text names no closed state for E6 to attach to; the closed-month feature was the review's FIX, and the contaminated worker wrote condition 1 in the shape of the fix rather than the gap. Per PM-2 the condition is not re-written after seeing the output; per the milestone definition a miss halts. Also found cold: "Deactivation — half the named milestone — has zero requirements" (24 GAP rows), and the 94-case bound is too small for one feature area (hit twice). Artifacts: `.gsd-t/scan/m115-cold-enumeration-blind-{unscoped,scoped}.md`. DECISION FOR DAVID: (a) accept 2/3 + substance-of-3 and proceed to Wave 2, re-writing condition 1 to the gap's shape with the divergence flagged; or (b) hold the strict rule and re-examine the premise. Not proceeding on my own.
- 2026-09-03 09:00 PDT: [m115][execute] **`test-plan-first-contract.md` frozen 1.0.0 DRAFT → STABLE — enumerator-core (Wave 1) complete, A1 passed cold, first try.** All four domain tasks done: (T1/T2) the E1-E8 enumeration protocol authored at `templates/prompts/test-plan-enumerator-subagent.md` with a plain-words meaning + worked example per rule, the case-space bound settled at **94 cases** (evidenced by `test-plan-final.md`'s own observed scale, not a round-number guess) with an explicit HALT-at-the-bound rule, and the three per-gap hit conditions written down BEFORE any cold-run output existed (pre-mortem PM-2). (T3, HEADLINE) The protocol run cold against `requirements-before-review.md` alone (571 lines, no held-out file opened first); output recorded to `.gsd-t/scan/m115-cold-enumeration-output.md` before scoring. **All three answer-key gaps surfaced on the first pass**: month close+reopen (E6, both directions), the wrong permission model (E4, `GAP:CONTRADICTION` between the documented screen-level model and the endpoint-level pattern F6 already confirms elsewhere in the same repo), and the owner-cannot-be-deactivated refusal (E8, the irreplaceable-entity case F4 already establishes for DELETE but never states for deactivate). `test/m115-a1-blind-replay.test.js` (6 tests) scores the recorded artifact structurally — each gap's assertion targets a different named table, proven independent by mutation-and-restore during authoring (removing any one gap's row failed only that gap's test). Fixture verified byte-identical (`git diff --stat` empty). (T4) Contract §2 row schema needed no changes — all six columns and three row states, including `GAP:CONTRADICTION`, were exercised exactly as specified by the replay. §9 added recording the case-space bound. **Wave 2 (`plan-visibility`, `halt-convergence`) and Wave 3 (`deterministic-gates`, `front-door-wiring`) may now start.**
- 2026-09-03 08:52 PDT: [release] **v5.17.14 published — fallback gate scans git source only; logging-manifest declaration; migrate-logging skips declared streams; disjointness library no longer silent; cpua verifies on-disk install. M115 planned (8b75e03).** Patch bump. Reverted a second research-pipeline pollution of progress.md (stale UMI/SCIP claims, TD-297 again) and logged TD-298 (plan reported "blocked" on a scope-unresolved exit 64). Suite 3393 pass / 0 fail. Propagated.
- 2026-09-03 08:45 PDT: [fix] **TD-395 round 2 (TimeTracking): the fallback gate scanned GSD-T's own synced tool, and the logging checker could only see an audit store at paths it guessed.** (1) `bin/gsd-t-fallback-detect.cjs --scan` now scans only what git treats as the project's source (tracked + untracked-not-ignored); git-ignored files — where the propagated `bin/*.cjs` tools live — are skipped and COUNTED (`skippedIgnored`), and outside a git repo nothing is filtered and the envelope says so (`gitSourceFilter:false`). TimeTracking worktree: 52 ignored files skipped, 0 unapproved. 3 tests incl. the un-ignored negative. (2) `.gsd-t/logging-manifest.json` — a project DECLARES a stream the candidate paths do not find (`{ "audit": { "module", "store": {kind,table} | path, "retention": "indefinite" } }`); the declaration is CHECKED (missing module/store path → `logging-manifest-invalid`, malformed JSON → FAIL), an external store's rows are not inspected offline and that is reported in `notes` with the module surface as the enforced evidence; `retention: "indefinite"` satisfies the retention rule by policy (noted). `gsd-t migrate-logging` skips a declared stream so it never scaffolds a twin (`declared` in its envelope; unreadable manifest throws). 5 + 2 tests. Contract `logging-verify-gate-contract.md` §discovery (0), CLAUDE.md global + template, `commands/gsd-t-migrate-logging.md` rippled. TimeTracking main + worktree: fresh tools copied and the manifest written (`server/src/audit.ts` → Postgres `tb_audit_log`, indefinite) — the checker now passes audit and fails ONLY on the missing trace stream, which is `gsd-t migrate-logging` (trace only, backend choice is David's). Suite 3391+ pass / 0 fail.
- 2026-09-03 10:23 PDT: [m115][integrate] **Wave 3 integrated — one real cross-domain mismatch found and fixed, two count/line-ref ripples from the new command closed, contract §7 ratified.** (1) **Plan location disagreed across domains**: `front-door-wiring` writes plans to `.gsd-t/test-plans/` (command file + verify-gate glob), while `deterministic-gates`' A7 resolver read `TestPlan-<Title>.md` from `.gsd-t/pseudocode/` — a plan written where the command puts it would NEVER have cleared a `**Plan-Row**` citation. Neither the frozen contract nor the pseudocode fixed the directory (both workers flagged theirs `[GUESSED:assumed]`). Resolver re-pointed at `<project>/.gsd-t/test-plans/` in `runGate`; A7 test fixtures moved with it (185/185 M115+M83+M87 green; M83/M87 files untouched). (2) Contract `test-plan-first-contract.md` 1.0.0 → **1.1.0 STABLE**, new §7.1 freezing the two literals Wave 3 built to: the plan directory and the `**Plan-Row**: <FeatureArea>#<TableName>/Seq-<n>` field. Pseudocode `## Where it lives` gains the location row (style gate exit 0). (3) Stripped a stray `<!-- auto-research-claim … status=uncited -->` marker the research-gate hook had appended to `bin/gsd-t-testplan-lint.cjs` (a repo-internal design choice misclassified as `class=external` — would have R-FAIL-1'd verify). (4) New command ripples: `test/filesystem.test.js` counts 57→58 / 50→51; `graph-metrics-contract.md` `doMetrics` line-ref 5493→5505 (the two dispatch cases shifted it). Full suite 3501 pass / 0 fail after.
- 2026-09-03 08:20 PDT: [release] **v5.17.13 published — TD-395 verify-gate fixes (logging-envelope propagation, graph-use attribution, verify stamp ordering) + phase-workflow null-finalizer guard; M115 defined + partitioned (unbuilt).** Patch bump: bug fixes, no new capability. Suite 3381 pass / 0 fail. Propagated; TimeTracking main + worktree patched directly beforehand so those sessions could continue.
- 2026-09-03 08:27 PDT: [m115][plan] **M115 planned — 25 tasks across 5 domains refined to atomic, all three plan gates green, two pre-mortem findings closed in the plan rather than deferred.** Traceability gate: 25 tasks, 0 violations. Pseudocode style gate: exit 0. `gsd-t parallel --dry-run`: all 25 tasks parse, deps ok, no graph-BROKEN halt (graph live, 1,553 files). **File-disjointness re-validated structurally, path-as-path: 21 distinct owned write paths, each in exactly one domain, zero cross-domain collisions.** The universal `sequential` verdict from the dry-run is intra-domain dep-chaining (each task depends on its predecessor and writes the same file), NOT a collision — domain-level concurrency within a wave is unaffected. **M83 plan hardening — the headline was UNTAGGED and would have shipped unbound**: no task carried `Headline: true`, so the gate passed only because nothing claimed the role. The milestone definition names A1 the blind replay as "the headline and the killer", so `M115-D1-T3` is now tagged, and its **Files** widened from a test-only path to `templates/prompts/test-plan-enumerator-subagent.md` + the test — a headline with a test but no implementing path is the NiceNote M5 dead-code shape the rule exists to catch. `M115-D5-T5` self-described as "the headline"; corrected to this domain's headline, pointing at D1-T3. **Pre-mortem PM-1 (HIGH, dead-gate)**: D5-T4 was told to copy the guard-map gate's pattern, whose discovery agent ends in `.catch(... reason: "discovery-error")` — a broken discovery degrades to a SKIP, so a real malformed plan passes verify whenever discovery hiccups. Same class as the scan haiku-fenced-JSON incident. D5-T4 now specifies FOUR mutually distinguishable outcomes (pass / fail-on-malformed / skip `no-test-plan` / **FAIL `testplan-discovery-error`**), prefers deterministic glob discovery over an LLM agent, and carries a required test that goes red if the discovery arm is turned back into a skip. **Pre-mortem PM-2 (HIGH, unfalsifiable criterion)**: A1 said "surfaced all three gaps" without defining *surfaced*, and the scoring would have been done by the same reasoning that produced the output — a criterion authored after seeing the result cannot fail. The three per-gap hit conditions are now a T2 deliverable written to disk BEFORE any cold run exists, structural (a row's cells, a named gap's subject) never substring, with near-misses explicitly counting as misses; T3 scores the recorded artifact rather than re-running. **No-Fallback: the plan designs in zero fallbacks** — six halt points (A1 miss twice, case-space bound, three unclosed question rounds, repeated symptom, gate bad-input `64`, undecidable classification), each stopping rather than continuing. Six-Stage Pass re-run at plan and recorded in plain English under `## Why this shape` (pseudocode-style exit 0). Written: `.gsd-t/contracts/integration-points.md` (wave groupings, the 21-path disjointness table, cross-domain seams, headline binding, AC map, halt points). Contract stays `1.0.0 DRAFT` — it freezes to STABLE only when A1 passes, which is the Wave-1 gate. Next: `/gsd-t-execute`.
- 2026-09-03 08:25 PDT: [fix] **TD-395 upstream (TimeTracking): two of the three verify-gate blockers were GSD-T defects — fixed; the third is project-side.** (1) `logging-envelope`: `bin/gsd-t-logging-envelope-check.cjs` was never added to `PROJECT_BIN_TOOLS`, and verify-gate reaches it via `__dirname` inside the project's own `bin/` — present on this machine, absent in every project (5th propagation gap of this class). Added, and `test/verify-gate-tools-propagated.test.js` now asserts EVERY `__dirname` tool the gate dispatches to is in the propagation set (structural; mutation-checked with a negative case). (2) `graph-use`: workflows stamp their WIRED claim under their own name (`phase`, `verify`) while every worker query runs through Bash and lands under the query CLI's default label `cli`, which the gate exempts — TimeTracking's ledger: 302 queries under `cli`, 3 claims under `phase`/`verify`, gate FAILED. Attribution, not absence. Gate now counts an UNLABELLED id's queries (one with no wiring claim of its own) as evidence for every claimant whose claim PRECEDES them; a query before the claim is still not evidence; another claimant's queries are never borrowed. `[RULE] graph-use-attributes-unlabelled-queries-by-time`, 4 tests. Real TT ledger: `phase` now 281 attributed. ALSO: verify stamped its claim at Preflight and ran the gate immediately after — a fresh claim with zero queries, failing its own gate by construction every run; the stamp now lands AFTER the gate, before verify's first structural query (dead-code). TT's one stale pre-fix `verify` claim clears on the first graph query logged after it. (3) `tests/playwright`: the gate runs `npx playwright test` exactly as the project's own `npm test` does; TimeTracking's config requires `TEST_DATABASE_URL` and throws without it — the config must load `.env.local` itself and the secret is David's to place. No GSD-T change. Contract line-ref `graph-metrics-contract.md` re-pointed (5488→5493) after the insertion shifted `doMetrics`. Suite 3381 pass / 0 fail. Publish as v5.17.13 once the M115 partition run (in flight on the installed workflow) lands.
- 2026-09-03 08:00 PDT: [m115][partition] **M115 partitioned into 5 domains, 3 waves — competition winner A of 3 (RISK-FIRST preserved).** The winning proposal keeps the milestone definition's risk-first ordering and adds the structural move the other two candidates missed: **every shared-file write in the milestone is quarantined into one domain** (`front-door-wiring`), which is what makes the other four file-disjoint by construction rather than by negotiation. 37 files across 5 domains, zero collisions (checked structurally, path-as-path). **W1 `enumerator-core` RUNS ALONE** — the falsification gate: authors the E1-E8 enumeration protocol, blind-replays it cold against the held-out fixture (input `requirements-before-review.md` only; `test-plan-final.md` + the delta answer key NOT read during the run), and passes only when all three known gaps surface (month close+reopen, the wrong permission model, the owner cannot be deactivated). It also settles the case-space bound the pseudocode explicitly deferred to partition, using the fixture's observed 94-case scale as evidence rather than a round-number guess, with a HALT-at-the-bound rule instead of a silent truncation. Finally it authors `.gsd-t/contracts/test-plan-first-contract.md` (row schema, the 0/4/64 exit triple + envelope, the `DECIDED-WITHOUT-YOU`/`GAP` marker literals, and the two gate verb names), frozen DRAFT→STABLE only after A1 passes — so if the bet fails the whole domain plus contract deletes in one piece and nothing downstream was built against it. **W2 = the three silent-corruption surfaces, two domains concurrent**: `plan-visibility` (A3 self-answered rows grouped/sourced/visible + A6 evidence-or-halt classifier with no default branch; owns the mold + the rule set, writes NO gate binary) and `halt-convergence` (A5 the two loop caps; consumes `bin/gsd-t-loop-ledger.cjs` strictly READ-ONLY through its frozen exports, adding only the round→cycle mapping). They share no file, which is what lets them run together. **W3 = the well-precedented remainder, two domains concurrent**: `deterministic-gates` (A2 `testplan-lint` on the proven pseudocode-style gate shape with the mandatory negative test + A7 the additive traceability plan-row binding — the ONLY domain editing a pre-existing bin file, and FORBIDDEN to touch `test/m83-traceability-gate.test.js` or the m87 gate tests, whose byte-identity IS the preservation proof) and `front-door-wiring` (the command, the router case, both bin-tool registries + CLI dispatch, the verify gate wiring, and all four mandated reference docs). **A8's risk verified live this session, not assumed**: `grep test-plan commands/gsd.md` and `grep testplan bin/gsd-t.js` both return nothing today, so the command would ship DEAD — present on disk, unreachable — which is exactly the failure shape A8 tests for, and the registry omission that has shipped a tool dead four times in this repo's history is now a mechanical assertion rather than a checklist item. Salvaged from the two losing proposals without conflict: the explicit READ-ONLY framing of the loop ledger, the FORBIDDEN-to-edit list making M83/M87 preservation mechanically provable, and the bin-propagation assertion folded into A8. Rejected from them: splitting the gates across two domains (both wanted `bin/gsd-t.js`, reintroducing the collision the quarantine removes). Written: 15 domain files + 1 contract. Next: `/gsd-t-plan`.
- 2026-09-02 18:55 PDT: [m115][chore] **A1 blind-replay answer key secured; misrouted research block reverted.** Copied the TimeTracking rate-ledger origin out of the live worktree into `test/fixtures/m115-blind-replay/` with provenance: requirements BEFORE the review (`8836ba0^`, 571 lines), the first plan draft (210 lines), the final plan (350 lines), requirements AFTER (601 lines), and the 30-line review delta that is the answer key. The pre-review state IS recoverable from git, so A1 is a genuine blind replay — the milestone's stated open item is closed. Also: the phase workflow's research pipeline appended two off-topic "Verified Facts" blocks (tree-sitter/SCIP licensing — leftover M94-M99 claims) to `PseudoCode-TestPlanFirst.md`; reverted via git checkout, style gate still 0, logged as TD-297. Doc-First Enforcement pseudocode rewrite (architect pass, 18:16) committed alongside as parked design.
- 2026-09-02 18:35 PDT: [milestone] **M115 DEFINED — Test-Plan-First Requirements Interrogation (DEFINE-only; partition/plan deferred).** Competition winner **B of 3** — the RISK-FIRST ordering beat a fastest-time-to-value candidate (A) and a dependency-order candidate (C). Rationale for B: the milestone's central bet — that a cold enumeration driven by fixed rules reproduces what David's manual review found — is unproven, and B is the only candidate that tests it FIRST, in Wave 1, with no command/mold/gate built, against an answer key that already exists on disk. A and C both build the machinery before testing the hypothesis, so a failed bet costs four or five waves instead of one. Salvaged from A and C without conflict: the E6/E7/E8 enumeration rules (lifecycle closure, boundary inclusivity, refusal cases) beyond A's five; the KEEP-OR-SUPERSEDE verdict table with expected positions; the guard map `[RULE]` set; and C's unbounded-combinatorics risk (more-than-one-of-everything crossed with a per-endpoint permission matrix), recorded in the doc's Six-Stage risk answer as a bound to settle at partition rather than assumed here. Written: `.gsd-t/pseudocode/PseudoCode-TestPlanFirst.md` (§1 section set + 10-line guard map + 2 `⚠ Divergence` flags; `gsd-t pseudocode-style --doc` exit 0). Two divergences pre-declared, both additive-not-replacing: the QA protocol is no longer the sole owner of test completeness (it stays unchanged and code-first; this becomes its upstream dual), and M83's traceability may bind an acceptance line to a plan row as well as to a file-plus-killing-test (the old binding keeps working untouched for milestones with no plan). Origin evidence verified on disk this session: `/Users/david/Worktrees/TimeTracking/work/docs/rate-ledger-test-plan.md`, 350 lines / 29,253 bytes, dated 2026-09-02. M115 confirmed free — highest id in progress.md was M114. Nothing built; the approach awaits sign-off before partition.
















- 2026-06-27 20:xx MST: [M97 IN PROGRESS — core built, blocked on a real-project SCIP gap] **Built the grep-intercept core (classifier + hook + dist-exclude fix), then discovered the load-bearing blocker: SCIP call-resolution works on fixtures but produces ZERO resolved call edges on real projects.** SHIPPED + tested: (1) `bin/gsd-t-grep-classifier.cjs` — conservative structural-vs-text classifier (9/9 tests; bare symbol/call-shape/member-call/import → structural verb; everything with metachars/spaces/regex → TEXT pass-through). (2) `scripts/gsd-t-graph-intercept.js` — PostToolUse hook on Grep: classify → query graph → replace output via `updatedToolOutput`, original grep kept beneath; FAIL-OPEN, never calls Grep/Read (loop guard), no-graph→no-op. 5/6 hook ACs pass (text-passthrough, no-graph, malformed-stdin fail-open, non-Grep, no-loop static check); **the hook mechanism is PROVEN — it produced a full graph-answer replacement live on binvoice's ambiguous `isEnvelope` symbol.** (3) Indexer+freshness `dist`/`build`/`out`-PREFIX exclude (binvoice indexed `dist-local`/`dist-test` → 854 stale build-output entities polluting who-calls; now 0). **THE BLOCKER (AC-1 pending):** on binvoice, **ALL 37,652 call edges are UNRESOLVED** despite a 4.6MB `index.scip` present — SCIP resolution yields 0 resolved cross-file call edges on real code, though it resolves correctly on the M95 fixture (test m95-scip-call-resolution 5/5). So who-calls/blast-radius (M97's main value) return empty on real projects; only who-imports (tree-sitter file-level) works. **Root cause NOT yet found** — `gsd-t-scip-reader.cjs` reads the proto and resolves on a 2-file fixture but extracts no resolved call edges from binvoice's 4.6MB index.scip (likely a symbol-format / occurrence-role / multi-file mismatch at real scale). This is the next investigation and it gates M97. Stopped here rather than chase a deep SCIP-format bug at ~8pm (the 2-day-trap lesson). Classifier + hook + dist-fix committed; AC-1 hook test SKIPPED-PENDING with a documenting note. Files: `bin/gsd-t-grep-classifier.cjs` (NEW), `scripts/gsd-t-graph-intercept.js` (NEW), `bin/gsd-t-graph-index.cjs` + `bin/gsd-t-graph-freshness.cjs` (dist-prefix exclude), `test/m97-grep-classifier.test.js` (NEW 9/9), `test/m97-graph-intercept-hook.test.js` (NEW 5 pass/1 pending).





- 2026-06-26 22:xx PDT: [M95][COMPLETE — VERIFIED ON REAL ATOS] **Real SCIP call-graph resolution shipped end-to-end; both heavy real-Atos tests PASS; full suite 2502/0/4.** All ACs met: AC-1 fixture resolves; AC-2 d9 returns **164 resolved test→impl edges on real Atos** (was 0) + d3 594,986 edges 3/3 patterns; AC-3 tier honesty (unresolvable-only file stays floor); AC-4 `gsd-t install` auto-installs scip-typescript+scip-python & `gsd-t doctor` reports presence; AC-5 perf — fixed the d9 query-phase hang (test loader ran a per-file `edges WHERE src LIKE ?` full-table scan = O(files×edges) ≈ billions on Atos → bulk-load one query/table) + heavy tests gated `--test-concurrency=1 GSDT_SLOW_TESTS=1`, SCIP build ~200s/~10GB. **Bonus fix:** `openDb` in freshness used `.gsd-t/graph/graph.db` but the canonical store is `.gsd-t/graph.db` (the OOM guard exposed it) — fixed to use the explicit storePath. Commits `a221e35` (core resolution) · `e6ecb17` (bulk-load + zero-dep→guiding-principle) · `efe4b61` (doctor) · `cf79204` (openDb path). Definition/AC closeout: [`.gsd-t/milestones/M95-DEFINITION-scip-callgraph-resolution.md`](.gsd-t/milestones/M95-DEFINITION-scip-callgraph-resolution.md). **Follow-ons (non-blocking):** Python scip-python branch; in-memory 594K-edge query optimization. Files: this entry + the M95 definition status flip.

- 2026-06-26 21:5x PDT: [M95][CORE SHIPPED] **Real SCIP call-graph resolution BUILT — the precise tier is no longer a lie.** New `bin/gsd-t-scip-reader.cjs` decodes `index.scip` (via scip-typescript's bundled protobuf decoder, located across install layouts, fail-loud if absent) into symbol→funcId + per-file reference maps. New `buildScipResolver` (`gsd-t-graph-scip-upgrade.cjs`) runs scip-typescript ONCE per repo (was: once per file, output discarded — the core defect), reads it, and resolves each file's `UNRESOLVED#<name>` call edges to real cross-file funcIds. `tryScipUpgrade` rewritten to resolve from that map and label `compiler-accurate` ONLY when ≥1 edge actually resolved (or no call edges) — a file whose calls all stay unresolvable is `tree-sitter-floor`, killing the relabel-lie. `build_index` auto-builds the resolver and threads it through `parse_and_put`. **Proven:** fixture `e2e/calc.spec.ts → src/calc.ts#computeTotal` resolves, `test-impl` returns the impl (was `[]`); 60-file medium fixture resolves 30/30 cross-file test→impl edges in 561ms; tier-honesty negative case stays floor. M95 tests 5/5 (`test/m95-scip-call-resolution.test.js`); full non-heavy graph regression 22 files / 0 fail. **Auto-install wired:** `gsd-t install` now runs `npm i -g @sourcegraph/scip-typescript` + `scip-python` (M95 "best result" decision — zero-dep is a guiding principle, not a hard rule; both Apache-2.0/MIT, Node-only; fail-loud-non-blocking). Contract `graph-indexer-build-contract.md` tier definition updated to describe real resolution. **REMAINING (AC-5, perf):** the heavy d9 Atos verify built the full index (4,418 files, SCIP +~35s → 209s) but the QUERY phase (`loadStore`/`queryTestImpl` over 594K edges) hung at ~10GB — a scale problem in the query/load path, NOT in resolution (resolution proven correct + fast on fixtures). Heavy tests must run `--test-concurrency=1 GSDT_SLOW_TESTS=1`. Next: optimize the 594K-edge query/load path, then re-run d9 to green. Files: `bin/gsd-t-scip-reader.cjs` (NEW), `bin/gsd-t-graph-scip-upgrade.cjs`, `bin/gsd-t-graph-index.cjs`, `bin/gsd-t.js` (installScipIndexers), `test/m95-scip-call-resolution.test.js` (NEW), `.gsd-t/contracts/graph-indexer-build-contract.md`, `.gsd-t/milestones/M95-DEFINITION-scip-callgraph-resolution.md`, `.gsd-t/progress.md` (this entry).

























































> Prior decision log entries preserved in `.gsd-t/milestones/*/progress.md` — the m85-model-tier-policy-fable-2026-06-09 snapshot holds the full pre-M85 history.



