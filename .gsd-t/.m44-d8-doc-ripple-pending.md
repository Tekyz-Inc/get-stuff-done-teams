# M44 D8 — Doc Ripple Handoff (for coordinator merge)

Do NOT touch `docs/architecture.md` or `.gsd-t/progress.md` directly from
the D8 worker. The D2 worker runs in parallel and also edits these files.
The coordinator must append the two blocks below into the respective
shared documents.

Source: M44 D8 — spawn-plan-visibility — tests + doc-ripple (T7).

---

## Block 1 — append to `.gsd-t/progress.md` (Decision Log)

Append this line (preserving the existing `# Decision Log` section order —
newest entries at the top, so this goes ABOVE older entries and below any
later Decision Log entries from D2 that also need merging).

```
- 2026-04-23 17:00: [execute M44-D8 · Wave 3 parallel-with-D2/D3] M44 D8 spawn-plan-visibility complete — 7/7 tasks shipped (T1 writer + contract skeleton `eed80bb` → T2 status-updater + post-commit hook `5a5dae6` → T3 writer integration at 3 chokepoints `83a8227` → T4 plan derivation helper `1a86735` → T5 dashboard `/api/spawn-plans` + SSE channel `42456e3` → T6 transcript right-side panel `d7b1297` → T7 tests + doc-ripple this commit). **Three writers** (all try/catch-wrapped — plan-write failure NEVER blocks the spawn): `bin/gsd-t-token-capture.cjs::captureSpawn` writes before `await spawnFn()` and closes in both success/error paths; `bin/headless-auto-spawn.cjs::autoSpawnHeadless` writes before the detached child launches; `commands/gsd-t-resume.md` Step 0 writes at every worker iteration under `GSD_T_UNATTENDED_WORKER=1`. **Writer derivation** is deterministic projection of `.gsd-t/partition.md` + `.gsd-t/domains/*/tasks.md` via `bin/spawn-plan-derive.cjs` — no LLM calls, no prompts, silent-fall to empty-slice when partition absent. **Atomic writes only** (temp file + rename). **Storage**: `.gsd-t/spawns/{spawnId}.json` schema v1. **Post-commit hook**: `scripts/gsd-t-post-commit-spawn-plan.sh` + template `templates/hooks/post-commit-spawn-plan.sh` — greps commit message for all `[M\d+-D\d+-T\d+]` ids, sums token attribution from `.gsd-t/token-log.md` (rows where `Task` matches AND `Datetime-start >= spawn.startedAt`), calls `markTaskDone` with `{taskId, commit, tokens}`. Silent-fail: always `exit 0`. **Hook bugfix caught during T7 testing**: initial hook used `process.argv[2]`/`argv[3]` for positional args, but `node -e <script> A B` places A at argv[1] and B at argv[2] (no `[eval]` placeholder). Fixed in both the canonical script and template in the same T7 commit. **Reader**: new `GET /api/spawn-plans` (active plans newest-first) + SSE `/api/spawn-plans/stream` (fs.watch + mtime-dedup, initial snapshot on connect). `scripts/gsd-t-transcript.html` adds right-side `<aside class="spawn-panel">` with Layer 1 (project) + Layer 2 (active spawn); status icons `☐ ◐ ✓`; token cells render as `in=12.5k out=1.7k $0.42` (k-suffix above 1000, 2-decimal USD), `—` when null. Grid extended `280px 1fr` → `280px 1fr 320px`. **Additive everywhere**: no transcript-stream rendering changes, no token-log header changes, no existing endpoint/SSE changes. **Contract**: new `.gsd-t/contracts/spawn-plan-contract.md` v1.0.0. **Tests**: 5 new files, 36 tests — `test/m44-d8-spawn-plan-writer.test.js` (8) · `status-updater.test.js` (10) · `post-commit-hook.test.js` (5) · `dashboard-spawn-plans-endpoint.test.js` (5) · `transcript-renderer-panel.test.js` (8). Full-suite delta: +36 passing tests; pre-existing 7 failures (M43 progress parser, dashboard autostart, watch-progress-writer, supervisor script — all predate this commit) unchanged. **Disjointness with D2**: D8 owns `bin/spawn-plan-*`, `scripts/gsd-t-post-commit-spawn-plan.sh`, `templates/hooks/post-commit-spawn-plan.sh`, `scripts/spawn-plan-fmt-tokens.cjs`, `.gsd-t/spawns/`; D8 adds additive-only blocks to `bin/gsd-t-token-capture.cjs`, `bin/headless-auto-spawn.cjs`, `scripts/gsd-t-dashboard-server.js`, `scripts/gsd-t-transcript.html`, `commands/gsd-t-resume.md`. D2's config/orchestrator/CLI surface untouched.
```

---

## Block 2 — append to `docs/architecture.md`

Append this new section. Place BEFORE `## Planned Architecture Changes
(M23-M24)` and AFTER the `M44 Parallel CLI` section (or wherever the last
subsystem description ends). Content unchanged from the originally-drafted
ripple — matches what landed on disk during D8 execution:

```markdown
## Spawn Plan Visibility (M44 D8, v3.18.10+)

Right-side two-layer task panel in the dashboard / transcript visualizer.
Answers exactly one question: *"Of the tasks that were supposed to happen
in this spawn, which are done, which are in flight, which are pending?"*
Observability-only; zero added LLM token cost.

**Writers (3 chokepoints; all wrapped in try/catch — plan-write failure NEVER blocks the spawn)**:
- `bin/gsd-t-token-capture.cjs` — `captureSpawn()` calls `writeSpawnPlan`
  before `await spawnFn()` and `markSpawnEnded` in both success/error paths.
- `bin/headless-auto-spawn.cjs` — `autoSpawnHeadless()` calls
  `writeSpawnPlan` before the detached child launches; `markSessionCompleted`
  now also closes the plan.
- `commands/gsd-t-resume.md` Step 0 — under `GSD_T_UNATTENDED_WORKER=1`,
  every worker iteration writes a plan at iteration start.

**Derivation**: `bin/spawn-plan-derive.cjs` reads `.gsd-t/partition.md` +
`.gsd-t/domains/*/tasks.md` and emits `{milestone, wave, domains, tasks}`.
Deterministic projection — no LLM calls, no prompts. Silent-fails to an
empty slice when partition is absent.

**Status updater**: `bin/spawn-plan-status-updater.cjs` — atomic JSON
patches:
- `markTaskDone({spawnId, taskId, commit, tokens?, projectDir})`
- `markSpawnEnded({spawnId, endedReason, projectDir})`
- `sumTokensForTask(...)` — parses `.gsd-t/token-log.md` rows where
  `Task` column matches the id AND `Datetime-start >= spawn.startedAt`.

**Post-commit hook**: `scripts/gsd-t-post-commit-spawn-plan.sh` (+ template
at `templates/hooks/post-commit-spawn-plan.sh`). Greps commit message for
all `[M\d+-D\d+-T\d+]` ids; for each active plan, calls `markTaskDone`
with token attribution. Silent-fail: always `exit 0`.

**Reader surface**:
- `GET /api/spawn-plans` — active plans (where `endedAt === null`),
  newest-first.
- SSE channel `/api/spawn-plans/stream` — `fs.watch` on
  `.gsd-t/spawns/*.json` emits `{spawnId, plan}` on every change.
- `scripts/gsd-t-transcript.html` — right-side `<aside class="spawn-panel">`
  with Layer 1 (project) + Layer 2 (active spawn). Status icons
  `☐` pending / `◐` in_progress / `✓` done. Token cells render as
  `in=12.5k out=1.7k $0.42` (k-suffix above 1000, 2-decimal USD); `—` when
  attribution returned null. Token formatting helpers also live standalone
  at `scripts/spawn-plan-fmt-tokens.cjs` (testable under Node).

**Storage**: `.gsd-t/spawns/{spawnId}.json`. Schema v1. Atomic writes
(temp file + rename). `spawnId` is filesystem-safe
(`^[A-Za-z0-9._-]{1,200}$`). One ACTIVE plan per spawn; `endedAt === null`
means in-flight.

**Invariants** (from `.gsd-t/domains/m44-d8-spawn-plan-visibility/constraints.md`):
1. Writer DERIVES, never decides.
2. Spawn must launch even if writer fails.
3. Atomic writes only.
4. Post-commit hook silent-fail.
5. Zero new LLM token cost.
6. Additive edits to existing files only.
7. No transcript-derivation fallback — missing plan file → *"no active
   spawn plan"*, never reconstruct from transcript heuristics.

**Contract**: `.gsd-t/contracts/spawn-plan-contract.md` v1.0.0.

**Tests**: `test/m44-d8-spawn-plan-writer.test.js`,
`test/m44-d8-spawn-plan-status-updater.test.js`,
`test/m44-d8-post-commit-hook.test.js`,
`test/m44-d8-dashboard-spawn-plans-endpoint.test.js`,
`test/m44-d8-transcript-renderer-panel.test.js` — 36 tests total.
```

---

## Merge guidance for coordinator

1. Append Block 1 line to `.gsd-t/progress.md` Decision Log in the right
   chronological slot (newest-first — interleave with D2's Decision Log
   entry if D2 timestamps differently).
2. Append Block 2 section to `docs/architecture.md`. If D2 also adds a
   section in the same region, keep both; section headings are unique.
3. Delete this file once both blocks are merged.
