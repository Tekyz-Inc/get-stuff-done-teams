# Constraints: cleanup

## Must Follow
- All functions must remain ≤ 30 lines
- Zero external npm dependencies
- All 116+ existing tests must continue to pass
- New tests use Node.js built-in test runner (`node --test`)

## Must Not
- Modify files outside owned scope
- Change any function signatures (only internal logic)
- Break any existing test assertions

## Dependencies
- Depends on: none (all items are independent)
- Depended on by: none
