# GSD-T Progress

## Project: GSD-T Framework (@tekyzinc/gsd-t)
## Status: M37 VERIFIED — Universal Context Auto-Pause
## Date: 2026-04-16
## Version: 3.11.10 (minor — Universal Context Auto-Pause)

## Current Milestone
**M37: Universal Context Auto-Pause** — VERIFIED (all gates pass, auto-invoking complete-milestone)

**Goal**: When context usage crosses a threshold (default 75%), automatically execute `gsd-t-pause` to save exact position — then instruct the user to `/clear` and `/user:gsd-t-resume`. Works in ANY session type: execute, quick, debug, ad-hoc prototyping, research, brainstorming. No session should ever hit the runtime's ~95% `/compact` wall again.

**Scope**:
- **IN**: Strengthen the `additionalContext` signal from the context meter hook to be an unignorable MANDATORY STOP instruction (not a suggestion). Add a new `## Universal Auto-Pause Rule` section to `CLAUDE-global.md` template and project `CLAUDE.md` that makes Claude treat the context-meter pause signal as a hard stop — same weight as the Destructive Action Guard. Update `buildAdditionalContext()` in `threshold.js` to emit a stronger, multi-line instruction that names the exact steps (pause → clear → resume). Update the context-meter contract to document this as a mandatory behavioral rule. Propagate to all downstream projects via `update-all`.
- **OUT**: Changing the hook architecture itself (it already works correctly). Adding new hooks or scripts. Changing the threshold percentages (75% default is fine). Changing how `gsd-t-pause` or `gsd-t-resume` work (they're already correct).

**Success criteria**:
1. The `additionalContext` message emitted at ≥75% is an explicit MANDATORY STOP instruction, not a suggestion.
2. `CLAUDE-global.md` contains a Universal Auto-Pause Rule section with the same enforcement weight as Destructive Action Guard.
3. Every GSD-T command file that runs in a loop (execute, wave, integrate, quick, debug) has an explicit "if auto-pause signal received, STOP" check.
4. The context-meter contract (v1.2.0) documents the auto-pause behavioral requirement.
5. After `update-all`, all downstream projects receive the updated templates and contract.

## Milestones

| # | Milestone | Status | Version | Domains |
|---|-----------|--------|---------|---------|
| M37 | Universal Context Auto-Pause | VERIFIED | 3.11.10 | m37-auto-pause (single domain: threshold.js, contract v1.2.0, CLAUDE-global template, 5 command files) |
| M36 | Cross-Platform Unattended Supervisor Loop | COMPLETE | 3.10.10 | Archived (6 domains: m36-supervisor-core, m36-watch-loop, m36-safety-rails, m36-cross-platform, m36-m35-gap-fixes, m36-docs-and-tests; REQ-079–REQ-087; major version bump 2.x→3.x marking M34/M35/M36 arc) |
| M35 | No Silent Degradation + Surgical Model Escalation + Token Telemetry | COMPLETE | 2.76.10 | Archived (7 domains: degradation-rip-out, model-selector-advisor, runway-estimator, token-telemetry, optimization-backlog, headless-auto-spawn, docs-and-tests; 38 tasks) |
| M34 | Context Meter | COMPLETE | 2.75.10 | Archived |

## Completed Milestones

| Milestone | Version | Completed | Tag | Summary |
|-----------|---------|-----------|-----|---------|
| M36 Cross-Platform Unattended Supervisor Loop | 3.10.10 | 2026-04-15 | v3.10.10 | Detached supervisor relay for 24h+ unattended milestone execution; 3 new slash commands (unattended/unattended-watch/unattended-stop); ScheduleWakeup(270s) in-session watch loop; /clear+/resume auto-reattach via Step 0; cross-platform (macOS/Linux/Windows except sleep-prevention); safety rails (gutter/blocker/timeout); handoff-lock primitive closes M35 parent/child race; 5 command files drop "Run /clear" STOP; 1226/1226 tests |
| M35 Runway-Protected Execution | 2.76.10 | 2026-04-15 | v2.76.10 | Removed silent quality degradation; surgical per-phase model selection; pre-flight runway estimator with headless auto-spawn; 18-field per-spawn token telemetry; detect-only optimization backlog; 985/985 tests |
| M34 Context Meter | 2.75.10 | 2026-04-14 | v2.75.10 | Real context-window measurement via Anthropic count_tokens API; replaces task-counter proxy entirely; 941/941 tests |

## Prior Milestone (Archived)

**M35: Runway-Protected Execution — No Silent Degradation + Surgical Model Escalation + Token Telemetry** — COMPLETE (v2.76.10)
- Rips out silent quality-degradation under context pressure; three-band gate (normal/warn/stop) + surgical per-phase model selection + pre-flight runway estimator + headless auto-spawn + 18-field per-spawn token telemetry + detect-only optimization backlog
- 7 domains: m35-degradation-rip-out, m35-model-selector-advisor, m35-runway-estimator, m35-token-telemetry, m35-optimization-backlog, m35-headless-auto-spawn, m35-docs-and-tests (38 tasks across 5 waves)
- 5 contracts: token-budget-contract v3.0.0 REWRITTEN (clean break, no compat shim), model-selection-contract v1.0.0 NEW, runway-estimator-contract v1.0.0 NEW, token-telemetry-contract v1.0.0 NEW, headless-auto-spawn-contract v1.0.0 NEW
- Tests: 941 → 985 (+44 net new)
- Archived: .gsd-t/milestones/M35-runway-protected-execution-2026-04-15/

**M34: Context Meter — API-Based Token Counting (REPLACES Task Counter)** — COMPLETE (v2.75.10)
- Real context window measurement via Anthropic count_tokens API; replaces the task-counter proxy entirely
- 5 domains: context-meter-config, context-meter-hook, installer-integration, token-budget-replacement, m34-docs-and-tests (32 tasks + 11 gap-closure sites)
- 3 contracts: context-meter-contract v1.0.0 NEW, token-budget-contract v2.0.0 REWRITTEN, context-observability-contract v2.0.0 UPDATED
- Tests: 833 → 941 (+108)
- Archived: .gsd-t/milestones/M34-context-meter-2026-04-14/

**Pre-Milestone: Refined Model Tiers** — COMPLETE (v2.51.11)
- QA model promotion Haiku → Sonnet, Red Team → Opus, Haiku narrowed to mechanical-only
- Files: gsd-t-execute.md, gsd-t-quick.md, gsd-t-integrate.md, gsd-t-debug.md, CLAUDE-global.md, project CLAUDE.md
- Note: ~/.claude/CLAUDE.md syncs on next version-update-all (not edited directly to avoid permission prompt)

**M31: Self-Calibrating QA + Token-Aware Orchestration** — COMPLETE (v2.52.10)
- Enhancements 3.1 (Harness Audit) + 3.2 (QA Calibration) + 3.7 (Token-Aware Orchestration)
- PRD: docs/prd-harness-evolution.md
- 4 domains: harness-audit (3 tasks), qa-calibrator (2 tasks), token-orchestrator (2 tasks), command-integration (4 tasks) = 11 tasks
- New: bin/component-registry.js, bin/qa-calibrator.js, bin/token-budget.js, commands/gsd-t-audit.md
- Wired into: execute, quick, integrate, wave, complete-milestone, status, help
- Tests: 828 total (88 new for M31 modules)
- Archived: .gsd-t/milestones/M31-self-calibrating-qa-2026-04-01/

**M32: Quality Culture & Design** — COMPLETE (v2.53.10)
- Enhancements 3.3 (Quality North Star) + 3.4 (Design Brief) + 3.5 (Evaluator Interactivity)
- 3 domains: quality-persona (1 task), design-brief (1 task), evaluator-interactivity (1 task) = 3 tasks
- All domains independent; gsd-t-setup.md shared between quality-persona and design-brief (different sections)
- Contracts: quality-persona-contract.md, design-brief-contract.md, exploratory-testing-contract.md
- Domain artifacts: scope.md + tasks.md (expanded with acceptance criteria) + constraints.md for all 3 domains
- REQ coverage: REQ-060 (quality-persona T1), REQ-061 (design-brief T1), REQ-062 (evaluator-interactivity T1)
- Wave: Single Wave 1 — all parallel-safe; quality-persona before design-brief for gsd-t-setup.md sequential edit
- Execution order: quality-persona T1 → design-brief T1 → evaluator-interactivity T1 (parallel) → checkpoint
- Archived: .gsd-t/milestones/M32-quality-culture-design-2026-04-01/

**M33: Adaptive Iteration** — DEFINED (v2.54.10)
- Enhancement 3.6 (Configurable Iteration Budget)

**M30: Stack Rules Engine — Execute-Time Best Practice Enforcement** — COMPLETE (v2.48.10)
- Stack detection + best practice injection: 4 templates (_security.md, react.md, typescript.md, node-api.md), detection wired into execute/quick/integrate/wave/debug, QA enforcement, 135 new tests
- 2 domains: stack-templates (3 tasks), command-integration (4 tasks)
- Archived: .gsd-t/milestones/M30-stack-rules-engine-2026-03-24/

**M29: Compaction-Proof Debug Loop** — COMPLETE (v2.49.10)
- External debug-loop controller (bin/debug-ledger.js + gsd-t headless --debug-loop): JSONL ledger persists hypothesis/fix/learning across sessions, anti-repetition preamble injection, escalation tiers (sonnet 1-5, opus 6-15, STOP 16-20), 50KB compaction, 5 commands delegate fix-retest loops externally, 83 new tests (671 total)
- 3 domains: debug-state-protocol (3 tasks), headless-loop (3 tasks), command-integration (3 tasks)
- Archived: .gsd-t/milestones/M29-compaction-proof-debug-loop-2026-03-24/

**M28: Doc-Ripple Subagent — Automated Document Ripple Enforcement** — COMPLETE (v2.46.10)
- Automated doc-ripple agent: threshold check (7 FIRE/3 SKIP), blast radius analysis, manifest generation, parallel document updates
- 2 domains: doc-ripple-agent (contract + command + tests), command-integration (wired into execute/integrate/quick/debug/wave)
- Archived: .gsd-t/milestones/M28-doc-ripple-subagent-2026-03-24/

**M25: Telemetry Collection & Metrics Dashboard (Tier 1)** — COMPLETE (v2.43.10)
- Task telemetry with weighted signal taxonomy, rollups, process ELO, pre-flight intelligence check, Chart.js dashboard, gsd-t-metrics command
- 4 domains: metrics-collection, metrics-rollup, metrics-dashboard, metrics-commands
- Archived: .gsd-t/milestones/M25-telemetry-metrics-2026-03-23/

**M26: Declarative Rule Engine & Patch Lifecycle (Tier 2)** — COMPLETE (v2.44.10)
- Declarative rule engine (bin/rule-engine.js) + patch lifecycle (bin/patch-lifecycle.js), 5-stage lifecycle (candidate->applied->measured->promoted->graduated), promotion gate (>55%), quality budget governance
- 3 domains: rule-engine, patch-lifecycle, command-integration
- Archived: .gsd-t/milestones/M26-rule-engine-patch-lifecycle-2026-03-23/
- **Goal**: Auto-detect failure patterns, generate candidate patches, and manage their lifecycle through promotion gates with measurable improvement thresholds. Promoted patches that sustain improvement graduate into permanent methodology artifacts.
- **Scope**:
  - `.gsd-t/metrics/rules.jsonl` — declarative rule engine: pattern detection triggers as JSON objects (not hardcoded heuristics). Adding a new detection pattern = JSON append, not code deploy
  - `.gsd-t/metrics/patch-templates.jsonl` — maps triggers to specific command file / constraints.md edits. Each template defines: trigger pattern, target file, edit type, edit content
  - Patch lifecycle with 5 stages: `candidate → applied → measured → promoted → graduated`
    - **candidate**: auto-generated when heuristic detects repeated pattern (>=3 occurrences in task-metrics)
    - **applied**: patch template executed, edit applied to target file
    - **measured**: next 2+ milestones track whether the target metric improved
    - **promoted**: patch exceeds improvement threshold (>55% win rate, adapted from AlphaZero)
    - **graduated**: promoted patch sustained for 3+ milestones → absorbed into permanent methodology artifact (constraints.md, verify checks, plan pre-conditions) → removed from rules.jsonl
  - Promotion gates — patches must measurably improve target metric before becoming permanent
  - Activation count tracking — each rule records how many times it fires. Rules that haven't prevented a failure in N milestones flagged for deprecation
  - Periodic consolidation — every 5 milestones, related rules distilled into single cleaner rule (anti-bloat)
  - Quality budget governance — per-milestone rework ceiling (e.g., max 20% of tasks require fix cycles). When budget exhausted, system auto-tightens constraints: force discuss phase, require contract review, split large tasks
  - Extends `commands/gsd-t-complete-milestone.md` — rule evaluation + patch generation + promotion + graduation in distillation step
  - Extends `commands/gsd-t-execute.md` — pre-task active rule injection into subagent prompts
  - Extends `commands/gsd-t-plan.md` — pre-mortem step reading rules for domain-type failure patterns
  - New directory: `.gsd-t/metrics/patches/` — individual patch files with status tracking
  - New contract: `.gsd-t/contracts/rule-engine-contract.md` — rule schema, patch template schema, promotion gate thresholds, graduation criteria
- **Not in scope (Tier 3)**: Neo4j cross-project causal inference, cross-project rule propagation
- **Predecessor**: M25 (task-metrics.jsonl must exist and be populated for pattern detection)
- **Brainstorm**: `.gsd-t/brainstorm-2026-03-20-telemetry.md`
- **Impact on existing**: All additive — no breaking changes to existing contracts

**Success criteria**:
- [x] Rules.jsonl stores detection patterns as declarative JSON objects
- [x] Patch templates auto-generate candidate patches when patterns detected (>=3 occurrences)
- [x] Promotion gate blocks patch advancement unless >55% improvement measured
- [x] Graduated patches write themselves into constraints.md or verify checks and exit rules.jsonl
- [x] Activation count tracking flags inactive rules for deprecation
- [x] Quality budget governance triggers constraint tightening when rework ceiling exceeded
- [x] Pre-mortem in plan surfaces historical failure patterns for current domain types
- [x] All existing tests pass with no regressions (373+ tests)

**M27: Cross-Project Learning & Global Sync (Tier 2.5)** — COMPLETE (v2.45.10)
- **Goal**: Propagate proven rules across projects, enable cross-project comparison using signal-type distributions, and eventually ship validated rules in the npm package.
- **Scope**:
  - Dual-layer learning architecture:
    - Project-specific: `.gsd-t/metrics/` (task-metrics, rollup, rules, patches) — stays local
    - Cross-project: `~/.claude/metrics/` (global rollup, global rules, signal distributions) — shared across all registered GSD-T projects
  - Global patch propagation — when a rule is promoted in one project:
    1. Copy to `~/.claude/metrics/global-rules.jsonl` with source project tag
    2. On `gsd-t-version-update-all`, propagate global rules to all registered projects as candidates (not promoted — each project must re-validate)
    3. Rules that achieve promotion in 3+ projects → marked as universal
  - Cross-project signal-type comparison — compare weighted signal distributions (not just raw pass/fail rates) across projects
  - Cross-project rollup aggregation — domain-type pattern matching: compare similar domain types across projects to identify systemic patterns
  - npm distribution pipeline — universal rules achieving promotion in 5+ projects are candidates for shipping in the GSD-T npm package itself (in `templates/` or `examples/rules/`)
  - Extends `bin/gsd-t.js` `doUpdateAll()` — adds global rule sync step during update-all
  - Extends `commands/gsd-t-metrics.md` — adds cross-project comparison queries
  - Extends `commands/gsd-t-status.md` — adds global ELO display
  - Extends `commands/gsd-t-complete-milestone.md` — adds global rule promotion check after local promotion
  - New directory: `~/.claude/metrics/` — global rollup, global rules, signal distributions
  - New contract: `.gsd-t/contracts/cross-project-sync-contract.md` — global rule schema, propagation protocol, universal promotion criteria
- **Not in scope**: Neo4j graph database (optional power tier, not required for cross-project learning)
- **Predecessor**: M26 (rule engine must exist with promotion gates for cross-project propagation to work)
- **Brainstorm**: `.gsd-t/brainstorm-2026-03-20-telemetry.md`
- **Impact on existing**: All additive — no breaking changes to existing contracts

**Success criteria**:
- [x] Promoted rules propagate to `~/.claude/metrics/global-rules.jsonl`
- [x] `gsd-t-version-update-all` syncs global rules to all registered projects as candidates
- [x] Rules achieving promotion in 3+ projects marked as universal
- [x] Cross-project comparison uses signal-type distributions, not just raw rates
- [x] `gsd-t-metrics --cross-project` returns domain-type comparison across projects
- [x] All existing tests pass with no regressions (481 tests, 48 new)

**M28: Doc-Ripple Subagent — Automated Document Ripple Enforcement** — COMPLETE (v2.46.10)
- **Goal**: Eliminate the "forgot to update X" failure mode by creating a dedicated agent that automatically identifies and updates all downstream documents after any code change. When doc-ripple completes, the user should never need to ask "did you update everything?"
- **Scope**:
  - `commands/gsd-t-doc-ripple.md` — new command file defining the doc-ripple agent behavior: reads `git diff`, cross-references against pre-commit gate checklist, produces a manifest of affected documents, spawns parallel subagents to update each one
  - `.gsd-t/contracts/doc-ripple-contract.md` — trigger conditions, manifest format, update protocol, threshold logic
  - Integration into 5 command files that produce code changes: `gsd-t-execute.md`, `gsd-t-integrate.md`, `gsd-t-quick.md`, `gsd-t-debug.md`, `gsd-t-wave.md` — auto-spawn doc-ripple before reporting completion
  - Threshold logic: skip on trivial changes (single-file, no cross-cutting impact), fire on changes that touch contracts, standards, conventions, APIs, or patterns used across multiple files
  - Manifest-based audit trail: doc-ripple produces a `.gsd-t/doc-ripple-manifest.md` showing what was checked, what was updated, and what was already current
- **Not in scope**: Replacing the Pre-Commit Gate (doc-ripple supplements it, doesn't replace it). Auto-committing (doc-ripple updates files, the lead agent still commits). Modifying non-GSD-T projects.
- **Predecessor**: REQ-051 (Document Ripple Completion Gate — the rule this agent enforces structurally)
- **Impact on existing**: All additive — no breaking changes to existing contracts. Existing Document Ripple sections in commands remain as documentation; doc-ripple agent provides automated enforcement.

**Success criteria**:
- [ ] `commands/gsd-t-doc-ripple.md` exists and defines blast radius analysis + parallel update dispatch
- [ ] Contract `.gsd-t/contracts/doc-ripple-contract.md` defines trigger conditions, manifest format, update protocol
- [ ] Doc-ripple auto-spawns in execute, integrate, quick, debug, and wave commands before reporting completion
- [ ] Threshold logic correctly skips trivial single-file changes and fires on cross-cutting changes
- [ ] Manifest audit trail shows what was checked/updated/skipped
- [ ] All existing tests pass with no regressions (480+ tests)

**M29: Compaction-Proof Debug Loop** — VERIFIED
- **Goal**: Eliminate context compaction during debug-fix-retest cycles by moving the retry loop to an external process (headless exec) with a cumulative debug ledger that preserves all hypothesis/fix/learning history across fresh sessions. When done, a 20-iteration debug session should produce zero compaction events.
- **Scope**:
  - `.gsd-t/debug-state.jsonl` — structured debug ledger protocol: each entry records iteration, test, error, hypothesis, fix attempted, result (PASS/STILL_FAILS), and learning. Serves as cross-session memory that survives context resets
  - Ledger compaction: when ledger exceeds 50KB, spawn a summarizer session (haiku) to condense into a summary + last 5 raw entries. Prevents the ledger itself from overwhelming fresh sessions
  - Anti-repetition prompt injection: headless exec generates a preamble from the ledger listing all failed hypotheses and the current narrowing direction, injected into each new session's prompt
  - `gsd-t headless --debug-loop` mode in `bin/gsd-t.js`: external iteration manager that runs test→fix→retest cycles as separate `claude -p` sessions. Each session starts fresh (zero accumulated context). Loop controller is pure Node.js with zero AI context
  - Escalation tiers: iterations 1-5 use sonnet, 6-15 use opus, 16-20 STOP and present full diagnostic history to user
  - `--max-iterations N` flag (default 20) for hard ceiling enforced externally
  - Integration into `commands/gsd-t-execute.md`, `commands/gsd-t-wave.md`, `commands/gsd-t-test-sync.md`, `commands/gsd-t-verify.md`, `commands/gsd-t-debug.md` — when fix-retest loops are needed, delegate to headless debug-loop instead of looping in-context
- **Not in scope**: Changing the orchestrator context self-check (M22 Step 3.5 — that remains as defense-in-depth). Modifying the core headless exec/query functionality. Adding new slash commands.
- **Predecessor**: M23 (headless exec must exist), M22 (context observability for CTX_PCT tracking)
- **Impact on existing**: Additive to bin/gsd-t.js (new subcommand mode). Command file changes are behavioral (delegate loops externally instead of running in-context) — no new commands, no removed functionality.

**Success criteria**:
- [x] `.gsd-t/debug-state.jsonl` protocol defined with iteration/test/error/hypothesis/fix/result/learning fields
- [x] `gsd-t headless --debug-loop` runs test→fix→retest as separate `claude -p` sessions with fresh context each
- [x] Anti-repetition preamble auto-generated from ledger and injected into each session's prompt
- [x] Escalation tiers: sonnet (1-5), opus (6-15), hard stop (16-20) with full diagnostic output
- [x] Ledger compaction triggers at 50KB threshold, preserving summary + last 5 entries
- [x] `--max-iterations N` flag enforced by external process (not by AI in-context)
- [x] Execute, wave, test-sync, verify, and debug commands delegate fix-retest loops to headless debug-loop
- [x] All existing tests pass with no regressions (671 tests, 83 new)

**M30: Stack Rules Engine — Execute-Time Best Practice Enforcement** — DEFINED
- **Goal**: Auto-detect project tech stack at execute-time, inject mandatory best practice rules from `templates/stacks/` into subagent prompts, and enforce compliance via QA. When done, a React project should automatically get React/TypeScript/security rules injected into every subagent — no manual setup required. Violations fail the task, same weight as contract violations.
- **Scope**:
  - `templates/stacks/` directory — stack-specific best practice rule files:
    - `_security.md` — universal, always injected regardless of detected stack (XSS, prompt injection, OWASP Top 10, auth token handling, input sanitization, AI-specific security)
    - `react.md` — React patterns (React Query, hooks rules, component design, Container/Presenter, a11y, anti-patterns)
    - `typescript.md` — strict TypeScript (no `any`, interfaces for props, generics, strict tsconfig)
    - `node-api.md` — Node.js API patterns (Express/Fastify, error handling, middleware, service layer)
  - Stack detection engine — reads project files to determine tech stack:
    - `package.json` → detect react, express, fastify, hono, tailwindcss, typescript, etc.
    - `requirements.txt` / `pyproject.toml` → detect python, fastapi, django, flask
    - `go.mod` → detect go
    - `Cargo.toml` → detect rust
    - Detection runs at subagent spawn time, not at init/setup
  - Subagent prompt injection — for every subagent-spawning command (execute, quick, integrate, wave, debug):
    1. Run stack detection against project files
    2. Match detected stacks against available `templates/stacks/*.md` files
    3. Always include `_security.md` (underscore prefix = universal)
    4. Append matched rules to subagent prompt with: "These are MANDATORY standards. Violations fail the task."
    5. No on-disk modifications — rules injected into prompt only
  - QA enforcement — QA subagent receives the same stack rules and validates compliance:
    - Stack rule violations reported alongside contract violations
    - Stack violation = task failure (not warning)
  - Convention: `_` prefix = universal (always injected), no prefix = stack-specific (injected when detected)
  - Extensible: adding a new stack = dropping a `.md` file in `templates/stacks/`. No code changes to the engine.
- **Not in scope**: Writing stack rule files for every language day 1 (ship with 4: _security, react, typescript, node-api). Modifying project CLAUDE.md on disk. Creating new slash commands. Changing `gsd-t-setup` or `gsd-t-init`.
- **Predecessor**: None (standalone feature, uses existing subagent spawn patterns)
- **Impact on existing**: Modifies subagent prompt construction in execute, quick, integrate, wave, debug. All additive — existing behavior unchanged for projects with no matching stack templates.

**Success criteria**:
- [ ] `templates/stacks/` directory exists with `_security.md`, `react.md`, `typescript.md`, `node-api.md`
- [ ] Stack detection correctly identifies React, TypeScript, Node.js, Python, Go, Rust from project manifest files
- [ ] `_security.md` injected into every subagent prompt regardless of detected stack
- [ ] Stack-specific rules injected only when matching stack detected
- [ ] Subagent prompt includes "MANDATORY standards — violations fail the task" framing
- [ ] QA subagent receives and validates against the same stack rules
- [ ] Stack rule violations block task completion (same severity as contract violations)
- [ ] Adding a new stack rule = dropping a `.md` file in `templates/stacks/` with no code changes
- [ ] All existing tests pass with no regressions (536+ tests)

## Domains (M34)
| Domain                    | Status   | Tasks | Completed |
|---------------------------|----------|-------|-----------|
| context-meter-config      | planned  | 4     | 0         |
| context-meter-hook        | complete | 5     | 5         |
| installer-integration     | planned  | 6     | 0         |
| token-budget-replacement  | planned  | 10    | 0         |
| m34-docs-and-tests        | planned  | 9     | 0         |

**Total: 34 tasks across 5 domains. Phase: PLANNED → ready for `/user:gsd-t-execute`.**

## Contracts (M34)
- [x] context-meter-contract.md — config schema, state file format, hook I/O, threshold mapping (NEW)
- [x] token-budget-contract.md — v2.0.0: real-count source rewrite, env-var references retired, orchestrator gate semantics (UPDATED)
- [x] context-observability-contract.md — v2.0.0: Ctx% source → context-meter state file, env-var references retired (UPDATED)
- [x] integration-points.md — M34 dependency graph, checkpoints CP1–CP4, file-ownership map, wave ordering (UPDATED)

## Integration Checkpoints (M34)
- [ ] M34-CP1: context-meter-contract.md finalized → unblocks context-meter-hook + token-budget-replacement
- [x] M34-CP2: context-meter-hook unit tests passing → unblocks installer-integration + token-budget-replacement real-count tests (SATISFIED — 1f19072, scripts/gsd-t-context-meter.js, 15/15 tests green, full suite 919/919)
- [ ] M34-CP3: token-budget-replacement deletes task-counter.cjs + removes command-file references → unblocks installer PROJECT_BIN_TOOLS update
- [ ] M34-CP4: All four implementation domains unit tests passing → unblocks m34-docs-and-tests pass

## Domains (M30)
| Domain              | Status  | Tasks | Completed |
|---------------------|---------|-------|-----------|
| stack-templates     | done    | 3     | 3         |
| command-integration | done    | 4     | 4         |

## Contracts (M30)
- [x] stack-rules-contract.md — detection protocol, template conventions, prompt injection format, QA enforcement
- [x] integration-points.md — UPDATED with M30 dependency graph, 2 waves, 2 checkpoints

## Execution Order (M30)
Wave 1: stack-templates (foundation — produces template files consumed by command-integration)
Wave 2: command-integration (wiring — modifies 5 commands + QA prompts + 4 reference docs)

## Queued Milestones

None — backlog item #10 (Docker Enterprise) available when ready.

## Previous Milestones (Archived)

**M23: GSD 2 Tier 2 — Headless Mode** — COMPLETE
- Result: 3 domains (headless-exec, headless-query, pipeline-integration). `gsd-t headless` CLI subcommand wraps `claude -p` with exit codes 0-4, --json/--timeout/--log flags. `gsd-t headless query` returns JSON from .gsd-t/ file parsing in ~50ms (no LLM). 7 query types: status, domains, contracts, debt, context, backlog, graph. CI examples: GitHub Actions + GitLab CI. 36 new tests (329 total). v2.41.10.

**M20: Graph Abstraction Layer + Native Indexer + CGC Integration** — COMPLETE
- Result: 6 new files (graph-store, graph-parsers, graph-overlay, graph-indexer, graph-query, graph-cgc), 3 CLI subcommands (graph index/status/query), 4 new contracts, 70 new tests. Self-indexed: 264 entities, 725 relationships. 280/280 tests pass.

**M21: Graph-Powered Commands** — COMPLETE
- Result: Graph-enhanced analysis in 21 of 49 commands across 4 tiers. All steps guarded with "if graph available." 280/280 tests pass.

## Milestones Roadmap
| #   | Milestone                             | Status  | Version | Domains |
|-----|---------------------------------------|---------|---------|---------|
| M22 | GSD 2 Tier 1 — Execution Quality      | COMPLETE    | 2.40.10 | 5       |
| M23 | GSD 2 Tier 2 — Headless Mode          | COMPLETE | 2.41.10 | 3       |
| M24 | Docker (Enterprise)                                | BACKLOG | 2.42.10 | 2       |
| M25 | Telemetry Collection & Metrics Dashboard (Tier 1)  | COMPLETE | 2.43.10 | 4       |
| M26 | Declarative Rule Engine & Patch Lifecycle (Tier 2)  | COMPLETE    | 2.44.10 | 3       |
| M27 | Cross-Project Learning & Global Sync (Tier 2.5)    | COMPLETE   | 2.45.10 | 3       |
| M28 | Doc-Ripple Subagent                                | EXECUTED    | 2.46.10 | 2       |
| M29 | Compaction-Proof Debug Loop                        | COMPLETE    | 2.49.10 | 3       |
| M30 | Stack Rules Engine                                  | COMPLETE    | 2.48.10 | 2       |

## Domains (M29)
| Domain               | Status  | Tasks | Completed |
|----------------------|---------|-------|-----------|
| debug-state-protocol | complete | 3     | 3         |
| headless-loop        | complete | 3     | 3         |
| command-integration  | complete | 3     | 3         |

## Contracts (M29)
- [x] debug-loop-contract.md — ledger schema, API definitions, escalation tiers, CLI interface, command integration pattern
- [x] integration-points.md — UPDATED with M29 dependency graph, 3 waves, 2 checkpoints

## Execution Order (M29)
Wave 1: debug-state-protocol (foundation — produces ledger API consumed by headless-loop)
Wave 2: headless-loop (loop controller — consumes ledger, produces debug-loop CLI mode)
Wave 3: command-integration (wiring — modifies 5 commands + 4 reference docs)

## Domains (M28)
| Domain              | Status      | Tasks | Completed |
|---------------------|-------------|-------|-----------|
| doc-ripple-agent    | complete    | 3     | 3         |
| command-integration | complete | 4     | 4         |

## Contracts (M28)
- [x] doc-ripple-contract.md — trigger conditions, manifest format, update protocol, threshold logic, integration pattern
- [x] integration-points.md — UPDATED with M28 dependency graph, 2 waves, 1 checkpoint

## Execution Order (M28)
Wave 1: doc-ripple-agent (foundation — produces command file + contract consumed by integration)
Wave 2: command-integration (wiring — adds spawn blocks to 5 commands + updates 4 reference docs)

## Domains (M27 — archived)
| Domain              | Status   | Tasks | Completed |
|---------------------|----------|-------|-----------|
| global-metrics      | complete | 4     | 4         |
| cross-project-sync  | complete | 3     | 3         |
| command-extensions   | complete | 4     | 4         |

## Contracts (M27)
- [x] cross-project-sync-contract.md — global-rules.jsonl, global-rollup.jsonl, global-signal-distributions.jsonl schemas, propagation protocol, universal promotion thresholds, global ELO
- [x] integration-points.md — UPDATED with M27 dependency graph, 3 checkpoints, 3 wave groups

## Execution Order (M27)
Wave 1: global-metrics (foundation — produces global-sync-manager.js consumed by all others)
Wave 2: cross-project-sync (propagation layer — extends doUpdateAll)
Wave 3: command-extensions (wiring — extends metrics, status, complete-milestone commands)

## Domains (M26 — archived)
| Domain              | Status   | Tasks | Completed |
|---------------------|----------|-------|-----------|
| rule-engine         | complete | 5     | 5         |
| patch-lifecycle     | complete | 4     | 4         |
| command-integration | complete | 4     | 4         |

## Domains (M25 — archived)
| Domain               | Status  | Tasks | Completed |
|----------------------|---------|-------|-----------|
| metrics-collection   | complete | 5     | 5         |
| metrics-rollup       | complete | 5     | 5         |
| metrics-dashboard    | complete | 2     | 2         |
| metrics-commands     | complete | 4     | 4         |

## Contracts (M25)
- [x] metrics-schema-contract.md — task-metrics.jsonl + rollup.jsonl schemas, signal taxonomy, ELO formula, pre-flight check, heuristic types
- [x] event-schema-contract.md — EXTENDED with task_complete event type + event-writer.js updated
- [x] dashboard-server-contract.md — EXTENDED with GET /metrics endpoint + readMetricsData export

## Execution Order (M25)
Wave 1: metrics-collection (foundation — produces task-metrics.jsonl consumed by all others)
Wave 2: metrics-rollup (aggregation — reads task-metrics.jsonl, produces rollup.jsonl + ELO)
Wave 3: metrics-dashboard + metrics-commands (parallel — terminal consumers, no shared files)

## Domains (M22 — archived)
| Domain                 | Status  | Tasks | Completed |
|------------------------|---------|-------|-----------|
| context-observability  | complete | 4     | 4         |
| fresh-dispatch         | complete | 4     | 4         |
| worktree-isolation     | complete | 4     | 4         |
| goal-backward          | complete | 3     | 3         |
| adaptive-replan        | complete | 3     | 3         |

## Contracts (M22)
- [x] fresh-dispatch-contract.md — Task-level subagent dispatch interface (dispatch payload, summary format, plan constraint)
- [x] worktree-isolation-contract.md — Worktree lifecycle + atomic merge protocol (creation, merge, rollback, ownership validation)
- [x] goal-backward-contract.md — Behavior verification interface (placeholder patterns, findings report, severity levels)
- [x] adaptive-replan-contract.md — Post-domain replan check + plan revision protocol (constraint categories, cycle guard, revision format)
- [x] context-observability-contract.md — Context window tracking + token breakdown format (extended token-log, alerts, aggregation)

## Execution Order (M22)
Wave 1: context-observability (foundation — tracking needed by all other domains)
Wave 2: fresh-dispatch (core dispatch mechanism — consumed by worktree + replan)
Wave 3a: worktree-isolation (filesystem isolation + merge protocol)
Wave 3b: goal-backward (behavior verification — sequential after worktree, both touch gsd-t-wave.md)
Wave 4: adaptive-replan (consumes fresh-dispatch summaries, integrates with worktree merge flow)

## Completed Milestones
| Milestone | Version | Completed | Tag |
|-----------|---------|-----------|-----|
| Compaction-Proof Debug Loop      | 2.49.10 | 2026-03-24 | v2.49.10  |
| Stack Rules Engine               | 2.48.10 | 2026-03-24 | v2.48.10  |
| GSD 2 Tier 1 — Execution Quality | 2.40.10 | 2026-03-22 | v2.40.10  |
| Graph-Powered Commands       | 2.38.10 | 2026-03-18 | v2.38.10  |
| Graph Engine                 | 2.37.10 | 2026-03-18 | v2.37.10  |
| Shared Service Detection     | 2.35.10 | 2026-03-11 | v2.35.10  |
| Multi-Consumer Surface ID    | 2.34.11 | 2026-03-11 | v2.34.11  |
| Scan Visual Output           | 2.34.10 | 2026-03-09 | v2.34.10  |
| Real-Time Agent Dashboard    | 2.33.10 | 2026-03-04 | v2.33.10  |
| Execution Intelligence Layer | 2.32.10 | 2026-03-04 | v2.32.10  |
| Backlog Management System | 2.8.0 | 2026-02-10 | v2.8.0 |
| QA Agent — Test-Driven Contracts | 2.22.0 | 2026-02-17 | v2.22.0 |
| Contract & Doc Alignment (Tech Debt Fix) | 2.21.2 | 2026-02-18 | v2.21.2 |
| Count Fix + QA Contract Alignment | 2.23.1 | 2026-02-18 | v2.23.1 |
| Testing Foundation | 2.24.0 | 2026-02-18 | v2.24.0 |
| Security Hardening | 2.24.1 | 2026-02-18 | v2.24.1 |
| CLI Quality Improvement | 2.24.2 | 2026-02-19 | v2.24.2 |
| Command File Cleanup | 2.24.3 | 2026-02-19 | v2.24.3 |
| Housekeeping + Contract Sync | 2.24.4 | 2026-02-18 | v2.24.4 |
| Cleanup Sprint | 2.24.5 | 2026-02-18 | v2.24.5 |
| Token Efficiency | 2.25.10 | 2026-02-18 | v2.25.10 |
| Execution Quality | 2.26.10 | 2026-02-18 | v2.26.10 |
| Planning Intelligence | 2.27.10 | 2026-02-18 | v2.27.10 |
| Tooling & UX | 2.28.10 | 2026-02-18 | v2.28.10 |

## Domains (M19)
| Domain | Status | Tasks | Completed |
|--------|--------|-------|-----------|
| partition-shared-first   | complete | 3 | 3 |
| feature-multiclient-guard | complete | 3 | 3 |
| plan-sharedcore-first    | complete | 2 | 2 |

## Contracts (M19)
- [x] No new contracts — internal step enhancements only

## Blockers
<!-- No active blockers -->

## Decision Log

> Older entries archived under `progress-archive/` — see `progress-archive/INDEX.md` for the date-range index.

- 2026-04-16 00:20: [M37-execute] Universal Context Auto-Pause — all core tasks complete. `buildAdditionalContext()` in threshold.js strengthened from single-line suggestion to 6-line MANDATORY STOP instruction. context-meter-contract.md bumped to v1.2.0 with new §"Universal Auto-Pause Rule". CLAUDE-global.md template updated with new MANDATORY section (same weight as Destructive Action Guard). Step 0.2 auto-pause rule added to 5 loop commands (execute, wave, integrate, quick, debug). All 1228 tests pass (1224 unit + 4 e2e). Pending: verify phase + npm publish + update-all propagation.
- 2026-04-15 19:25: [backlog] Added backlog item #13: "Agent Topology Dashboard Redesign" (type: ux, app: gsd-t, category: commands) — redesign dashboard to card-based directed graph layout with GSD-T role names, enriched per-agent metrics, and click-to-expand detail panel. Reference design provided by user.
- 2026-04-15 19:15: [milestone-defined] M37 Universal Context Auto-Pause — user hit context exhaustion twice during ad-hoc dashboard prototyping. Root cause: the context meter's `additionalContext` signal at 75% is a suggestion that Claude ignores. Fix: strengthen to MANDATORY STOP instruction + add enforcement rule to CLAUDE-global.md template with same weight as Destructive Action Guard. Simple scope (no new hooks/scripts), primarily template and contract updates.
- 2026-04-15 17:30: [fix] v3.10.16 — unattended launch friction fixes. Three bugs: (1) spawnSupervisor targeted gsd-t.js (no unattended subcommand) instead of gsd-t-unattended.cjs — supervisor crashed immediately on every launch. (2) Dirty worktree check refused launch on any untracked file — changed to auto-whitelist dirty files into config.json and proceed. (3) No milestone in progress.md caused hard refusal — added readiness bootstrap that auto-writes milestone from conversation context or --milestone flag. Also: update-all now creates .gsd-t/.unattended/config.json with defaults (protectedBranches, dirtyTreeWhitelist) so users can easily customize per-project, and adds bin/*.cjs + migration markers to .gitignore. 1136/1136 tests.
- 2026-04-15 15:10: [fix] v3.10.15 — propagate bin tools to downstream projects. `PROJECT_BIN_TOOLS` expanded from 5→13 entries. Created `.cjs` copies of 8 bin files (supervisor, handoff-lock, headless-auto-spawn, runway-estimator, token-telemetry, token-optimizer) with internal cross-requires updated. Updated 15 command files to reference `.cjs` versions. Unblocks `/gsd-t-unattended` launch from downstream projects like Tekyz-CRM. 1229/1229 tests.
- 2026-04-15 14:57: [fix] v3.10.14 — transcript parser orphaned tool_use blocks cause count_tokens 400. With API key now working (v3.10.13), the hook passed the key check but the API returned 400 because mid-session compaction orphans tool_use/tool_result pairs. Added `sanitizeToolPairs()` to `scripts/context-meter/transcript-parser.js` — enforces adjacency constraint, strips unpaired blocks. First successful real-time measurement: `inputTokens: 158543, pct: 79.3%, threshold: warn`. Context meter is fully working end-to-end for the first time. 1229/1229 tests.
- 2026-04-15 14:50: [fix] v3.10.13 — P0 propagation gap fix for v3.10.12 context meter regression. Verification after shipping v3.10.12 revealed the `stale` band fix was dead code in every downstream project: `PROJECT_BIN_TOOLS` in `bin/gsd-t.js` never included `token-budget.js`, so `update-all` never copied it; every downstream command-file gate snippet `require('./bin/token-budget.js')` threw `MODULE_NOT_FOUND`, the surrounding `try{...}catch(_){process.stdout.write('0')}` swallowed it, and the gate saw `pct: 0` = normal band — identical invisible failure to the original regression. Fix: renamed `bin/token-budget.js` → `bin/token-budget.cjs` (so it runs as CJS regardless of downstream `package.json` `"type": "module"`), added `"token-budget.cjs"` to `PROJECT_BIN_TOOLS`, swept all 17 command files replacing `./bin/token-budget.js` → `./bin/token-budget.cjs` (execute, wave, quick, debug, integrate, doc-ripple, verify, plan, discuss, visualize, reflect, brainstorm, audit, prd, resume, unattended, help), updated `test/token-budget.test.js` require path. package.json 3.10.12 → 3.10.13. CHANGELOG `[3.10.13]` entry. 1228/1228 tests green (same baseline as v3.10.12). v3.10.12 + v3.10.13 together are the complete fix — shipping 12 alone was incomplete.
- 2026-04-15 12:50: [success] M36 EXECUTE-COMPLETE — Waves 1-4 delivered, 1226/1226 tests green, v2.77.0 ready for verify+complete-milestone. Waves 1+2 (committed 978474d): `bin/gsd-t-unattended.js` detached supervisor (1259 lines), `bin/gsd-t-unattended-safety.js` (gutter/blocker/timeout checks), `bin/gsd-t-unattended-platform.js` (spawnSupervisor/preventSleep/notify/resolveClaudePath matrix), `bin/handoff-lock.js` parent/child race guard, `commands/gsd-t-unattended-watch.md` + `gsd-t-unattended-stop.md`, `docs/unattended-windows-caveats.md`, project-shared `.claude/settings.json` SessionStart auto-resume hook; 79 new tests (supervisor 42, safety 18, platform 14, handoff-lock 16, headless-auto-spawn +9). Wave 3 (committed 13577ca, 3 parallel tasks): `commands/gsd-t-unattended.md` launch command (327 lines, ScheduleWakeup(270) watch-loop scheduling, kill-0 2s liveness poll, initial status block), `commands/gsd-t-resume.md` Step 0 Unattended Supervisor Auto-Reattach + Step 0.2 Handoff Lock Wait (waitForLockRelease wired for headless child sessions via CLAUDE_HEADLESS_SESSION_ID env gate), `commands/gsd-t-wave.md` final "Run /clear" STOP block replaced with `autoSpawnHeadless()`, `test/command-files-stop-message-audit.test.js` grep-based regression guard (+1 test), `test/filesystem.test.js` command count bumps 60→61 / 55→56. Wave 4 doc ripple: README.md +45 (Unattended Execution section + features bullet + commands table), docs/GSD-T-README.md +16 (3 new rows, Unattended Execution subsection), CHANGELOG.md +70 ([2.77.0] - 2026-04-15 full entry: 9 Added items + 5 Changed files + Fixed/Tests/Migration), templates/CLAUDE-global.md +17 (3 new rows, Unattended Execution section), docs/architecture.md +77 (Unattended Supervisor section with component diagram, state dir, platform layer, safety rails, handoff-lock), docs/infrastructure.md +104 (setup section with CLI flags, config schema, platform helpers matrix, 4 troubleshooting scenarios), docs/methodology.md +8 ("From Runway-Protected Execution to Cross-Session Relay (M36)" 3-paragraph narrative), docs/requirements.md +29 (REQ-079 through REQ-087, 9 new requirements with domain traceability), commands/gsd-t-help.md +3 (AUTOMATION section entries), package.json 2.76.10→2.77.0 + description updated "56 slash commands"→"61 slash commands with unattended supervisor relay". Final test run: 1226/1226 green across 280 suites in ~17s. Standing "Execute until M36 is done" directive honored end-to-end — no Next Up hints between waves, clean auto-advance through execute → docs ripple → progress update → verify (next). Pending: `/user:gsd-t-verify` → `/user:gsd-t-complete-milestone` → USER GATE → `npm publish` → `/user:gsd-t-version-update-all`.
- 2026-04-15 11:35: [milestone-partitioned] M36 PARTITIONED into 6 domains with `unattended-supervisor-contract.md` v1.0.0 (draft). Domains: (1) **m36-supervisor-core** — owns `bin/gsd-t-unattended.js` main loop, `.gsd-t/.unattended/` state directory, PID/state.json/run.log lifecycle, `spawnSync('claude', ['-p', '/gsd-t-resume'])` relay. (2) **m36-watch-loop** — owns 3 new slash commands (`gsd-t-unattended`, `gsd-t-unattended-watch`, `gsd-t-unattended-stop`), `ScheduleWakeup` @ 270s cache-friendly cadence, and `gsd-t-resume.md` Step 0 auto-reattach block that transparently survives `/clear`+`/compact`. (3) **m36-safety-rails** — owns `bin/gsd-t-unattended-safety.js` with composable pure checks: `checkGitBranch` (protected list: main/master/develop/trunk/release-*/hotfix-*), `checkWorktreeCleanliness` (whitelist for GSD-T hot runtime files), `checkIterationCap`, `checkWallClockCap`, `detectGutter` (3 patterns: repeated-error / file-thrash / no-progress N=5 iters default), `detectBlockerSentinel`, `validateState`; `.gsd-t/unattended-config.json` optional per-project override. (4) **m36-cross-platform** — owns `bin/gsd-t-unattended-platform.js` isolating all `process.platform` branches: `spawnWorker` (`claude`/`claude.cmd`), `spawnSupervisor` detached-launch recipe, `notify` matrix (`osascript`/`notify-send`/`msg.exe`), `preventSleep`/`releaseSleep` (`caffeinate -i -w {pid}` on darwin, documented gap on win32), `isAlive`; closes deferred Spike C for Windows PowerShell quoting. (5) **m36-m35-gap-fixes** — owns the two M35 gaps the research chain surfaced: (a) NEW `bin/handoff-lock.js` mechanical sentinel (acquire/release/waitForRelease/cleanStale) to prevent parent/child race in `bin/headless-auto-spawn.js`; (b) sweep 5 command files (`gsd-t-execute.md`, `wave.md`, `quick.md`, `integrate.md`, `debug.md`) replacing `Run /clear then /user:gsd-t-resume` STOP blocks with direct `autoSpawnHeadless()` calls so users never type /clear for runway handoff — relies on Phase 0 fix which made `autoSpawnHeadless` actually work end-to-end. (6) **m36-docs-and-tests** — terminal domain: docs ripple across README + GSD-T-README + architecture + infrastructure + methodology + requirements (REQ-079+), 4-surface command reference update (README, GSD-T-README, CLAUDE-global template, gsd-t-help.md), CHANGELOG [2.77.0] entry, `package.json` 2.76.10 → 2.77.0, progress.md status→COMPLETE, `test/m36-integration.test.js` end-to-end shim-worker test, contract finalization, `/user:gsd-t-complete-milestone` invocation, `npm publish` (user-gated), `/user:gsd-t-version-update-all`. Contract `unattended-supervisor-contract.md` v1.0.0 (414 lines) authored during partition covering: state.json schema (14 fields), status enum (`initializing`/`running`/`done`/`failed`/`stopped`/`crashed`), unified exit code table (0–8 + 124, aligned with `mapHeadlessExitCode` M36 Phase 0 +5 command-dispatch-failed), CLI surface (`gsd-t unattended --hours/--max-iterations/--project/--branch/--on-done/--dry-run/--verbose/--test-mode`), launch handshake (detached spawn, readiness poll ≤5s, singleton check via PID file), watch tick decision tree (5 terminal checks + reschedule), resume auto-reattach handshake (skip normal flow if live supervisor detected), stop sentinel mechanism (race-free file-based), notification levels (`info`/`warn`/`done`/`failed`), safety hook points (pre-launch/supervisor-init/pre-worker/post-worker), optional config.json with default thresholds, testing contract (~60 net new tests → ~1165 target), version history, pending verification status. Baseline: 1083/1083 tests green, v2.76.10, Phase 0 smoke verified end-to-end. Integration points: m36-supervisor-core exports loop primitives + imports `mapHeadlessExitCode` from `bin/gsd-t.js` (Phase 0 fixed, read-only); m36-watch-loop reads state.json + PID from core (write-exclusive to core); m36-safety-rails is pure-function checks imported by core at hook points; m36-cross-platform is the platform-branch isolation layer imported by core + watch-loop; m36-m35-gap-fixes shares `.gsd-t/.handoff/` convention with safety-rails whitelist; m36-docs-and-tests depends on ALL other domains completing first. Next: `/user:gsd-t-plan` to expand each domain's `tasks.md` with atomic dependency-ordered tasks, then execute waves. Standing "Execute until M36 is done" directive continues to apply — no Next Up hints between phases.
- 2026-04-15 11:20: [success] M36 Phase 0 P0 fix VERIFIED end-to-end. Smoke test against real `claude -p` in `/tmp/gsd-t-spike-a`: `node bin/gsd-t.js headless help --timeout=60` returned the GSD-T help banner (`╔═══ GSD-T Command Reference ═══╗` + full command table), NOT `Unknown command: /gsd-t-help`. Pre-fix behavior was silent failure within ~2s — this is the first real end-to-end proof that `buildHeadlessCmd()` dropping the `/user:` prefix actually dispatches slash commands correctly in non-interactive mode. Unit-test baseline re-verified post-resume: 1083/1083 green (1078 M35 baseline + 5 new Phase 0 tests: 1 `buildHeadlessCmd` regression asserting no `/user:` prefix + 4 `mapHeadlessExitCode` sentinel tests for new exit code 5 command-dispatch-failed). Phase 0 code changes: `bin/gsd-t.js:2596-2598` (`buildHeadlessCmd` returns `/gsd-t-${command}` bare), `bin/gsd-t.js:2605-2625` (`mapHeadlessExitCode` adds `/^unknown command:/im` regex → exit 5, priority over verify-failed), `test/headless.test.js` (+5 tests), `docs/architecture.md:71-78` (4 lines), `.gsd-t/domains/headless-exec/scope.md:12-13` (spawn + exit-code lines). M35's 985 → dev 1078 → M36 Phase 0 1083 test progression confirmed. Next: commit Phase 0 checkpoint, then auto-advance to partition per standing "Execute until M36 is done" directive. Scratch dir `/tmp/gsd-t-spike-a` preserved as the Phase 0 smoke fixture.
- 2026-04-15 10:55: [milestone-defined] M36 DEFINED — Cross-Platform Unattended Supervisor Loop. Goal: `gsd-t unattended` — detached OS-process supervisor spawns fresh `claude -p` workers in a relay to run the active milestone to completion over 24h+ without a human in the loop. Launchable from within an interactive Claude session via `/user:gsd-t-unattended`, with in-session watch loop (`ScheduleWakeup` @ 270s cache-friendly cadence) displaying live progress and transparently re-attaching across `/compact`, `/clear`, and `/resume`. Success criteria: (1) runs real milestone DEFINED→COMPLETED unattended on macOS+Linux, (2) in-session launch detaches cleanly without blocking terminal, (3) `/clear`+resume during live run auto-reattaches watch loop, (4) pre-existing M35 P0 bug fixed (`bin/gsd-t.js:2598` drops `/user:` prefix), (5) 1078/1078 test baseline stays green, (6) published as v2.77.0. Phase 0 (spikes + P0 fix) effectively done: Spikes A/B/D all PASS on 2026-04-15 (findings in `.gsd-t/M36-spike-findings.md`), Spike C deferred to Phase 3. Baseline: 1078/1078 tests green, v2.76.10. Target: v2.77.0. Next: partition into domains. Rationale for fold-in: P0 headless dispatch bug discovered in Spike A makes M35's `autoSpawnHeadless()` silently fail end-to-end — fixing it is a prerequisite to M36's relay design since M36 inherits `buildHeadlessCmd()`. Cleanest structure is Phase 0 fix bundled into M36 rather than standalone patch release.
- 2026-04-15 10:50: [research] M36 Phase 0 spikes COMPLETE — 3/3 PASS. Spike A: `claude -p "/gsd-t-X"` dispatches slash commands correctly BUT requires bare `/gsd-t-X` form; `/user:gsd-t-X` returns `Unknown command` in non-interactive mode. Spike B: SessionStart hooks fire under `-p` — full hook surface (version check, auto-route, context meter PostToolUse) works in detached workers. Spike D: `spawnSync` captures exit codes cleanly; ⚠ unknown slash commands return exit 0 with error in stdout, supervisor must scan stdout for sentinel. P0 BUG DISCOVERED: `bin/gsd-t.js:2598` `buildHeadlessCmd()` returns `/user:gsd-t-${command}` — M35's entire headless auto-spawn path has been silently broken since ship (workers die with "Unknown command:" in 2s, `mapHeadlessExitCode()` returns 0 because the error doesn't match its sentinel list, orchestrator reports success). Invisible to interactive testing (interactive accepts `/user:` prefix) and to 985/985 test suite (tests feed synthetic strings to `mapHeadlessExitCode`, never real subprocess output) and to production telemetry (no headless spawns have actually fired since M35 shipped ~24h ago). Fix is 2-line edit + sentinel addition + regression test. Full findings: `.gsd-t/M36-spike-findings.md`. Scratch dir: `/tmp/gsd-t-spike-a/`. User decision: fold fix into M36 Phase 0 rather than standalone patch.
- 2026-04-15 03:45: [success] M35 COMPLETE — v2.76.10 archived and tagged. All 5 waves shipped (Wave 1 5/5, Wave 2 11/11, Wave 3 8/8, Wave 4 6/6, Wave 5 8/8 = 38/38 atomic tasks); 985/985 tests green; 10/10 requirements complete (REQ-069–REQ-078); 5 contracts ACTIVE (token-budget v3.0.0 REWRITE + model-selection/runway-estimator/token-telemetry/headless-auto-spawn v1.0.0 NEW). Archived to `.gsd-t/milestones/M35-runway-protected-execution-2026-04-15/` with progress snapshot, verify-report, 5-contract snapshot, all 7 domain folders, and summary.md. M35 domains removed from `.gsd-t/domains/` (archive preserved). `.gsd-t/progress.md` updated: status READY, version 2.76.10, M35 added to Completed Milestones table and Prior Milestone section, M34 table entry retained. Verify-report written inline at complete-milestone time synthesizing Wave 5 DAT-T8 evidence (full test suite + forbidden-term grep + REQ-069–078 goal-backward trace — all PASS). Structural guarantee documented: with `STOP_THRESHOLD_PCT=85` + pre-flight runway refusal, the 95% runtime native compact is unreachable under healthy operation; `halt_type: native-compact` is now a defect signal. Standing directive "continue until milestone is complete" honored end-to-end — no Next Up blocks, no mid-wave pauses, clean auto-advance through verify → complete-milestone. Next: git tag v2.76.10 + push (user-invoked), then `npm publish` + `gsd-t version-update-all` per project convention.
- 2026-04-15 03:15: [success] M35 Wave 5 COMPLETE — 8/8 tasks, 985/985 tests green, v2.76.10 ready for complete-milestone. Delivered: (DAT-T1) `README.md` + `docs/GSD-T-README.md` rewritten — new "Runway-Protected Execution (M35)" section, 5-band table replaced with 3-band (normal <70 / warn 70–85 / stop ≥85), "zero silent quality degradation" prose, per-phase model-selector + `/advisor` sections, `gsd-t metrics --tokens|--halts|--context-window` CLI table, optimization apply/reject sections, `thresholdPct` description no longer references "70% = downgrade". (DAT-T2) `docs/methodology.md` gained "From Silent Degradation to Aggressive Pause-Resume (M35)" section with five principles (quality non-negotiable / explicit per-phase model selection / user never types `/clear` / data before optimization / clean break no compat shim) + "Structural guarantee" paragraph explaining why 95% native compact is structurally unreachable; `docs/infrastructure.md` threshold table replaced 5-band with 3-band and added full Runway-Protected Execution M35 section covering model-selector/runway-estimator/headless-auto-spawn/token-telemetry/optimization-backlog + `gsd-t metrics` CLI table + `/advisor` escalation convention; `docs/architecture.md` gained "M35 supporting components" subsection documenting model-selector/token-optimizer/check-headless-sessions referenced from the context-meter dataflow, plus model-selection-contract.md v1.0.0 in the contracts list. (DAT-T3) `docs/requirements.md` M35 traceability table consistency pass — REQ-076 marked complete (Wave 4), REQ-077 marked complete (Waves 3–4). (DAT-T4) `docs/prd-harness-evolution.md` §3.7 consistency pass — risk table "Token exhaustion mid-milestone" mitigation rewritten from "graduated degradation" to "runway-protected execution with pre-flight estimator + headless auto-spawn; no graduated degradation; quality never lowered under pressure"; session-cost projection prose rewritten to reference M35 runway estimator + fresh-context headless continuation instead of graduated degradation; §3.7 header already documents "REWRITTEN IN M35" with all 5 M35 contracts cited. (DAT-T5) `CHANGELOG.md` new `[2.76.10] - 2026-04-15` top-section entry — ~65 lines across Added (model-selector, runway-estimator, headless-auto-spawn, check-headless-sessions, token-telemetry, token-optimizer, advisor-integration, 5 new contracts, 2 new commands, gsd-t metrics flags, test coverage summary), Changed (token-budget v3.0.0, orchestrator gate semantics, 6 command files with Step 0 runway gate + Model Assignment + token brackets, resume+status banners, complete-milestone optimizer hook, backlog-list --file flag, help.md OPTIMIZATION section, docs ripple listing each of 8 doc targets), Removed (graduated degradation bands, runtime model downgrade path, phase-skipping under pressure, manual /clear prompts under normal operation), Migration (return-shape contract change note, historical note on halt_type: native-compact as defect signal), Propagation (`gsd-t version-update-all` instructions). (DAT-T6) `package.json` version 2.75.10 → 2.76.10; `.gsd-t/progress.md` version header 2.75.10 → 2.76.10, status line "M35 COMPLETE — all 5 waves shipped, v2.76.10 tagged", Wave 5 completion stamp added. (DAT-T7) template + memory consistency audit — `templates/CLAUDE-global.md` line 369 already carries the full M35 Context Gate block referencing three-band model + `bin/model-selector.js` + `bin/token-telemetry.js` (shipped in Wave 2); `templates/CLAUDE-project.md` retirement comments preserved as historical prose; `feedback_no_silent_degradation.md` memory verified still accurate (now fully implemented); `project_compaction_regression.md` memory verified still accurate (historical, preserved). (DAT-T8) full test suite ran clean — 985/985 green across 227 suites in ~17s; global grep for `downgrade|conserve|modelOverrides|skipPhases` under `bin/`, `scripts/`, `commands/`, `templates/` returns only historical/prose references explaining the removal (acceptable per REQ-070 criterion); REQ-069–078 goal-backward verify — all 10 requirements now marked complete, REQ-078 "structural elimination of native compact messages" moved from pending → complete (Wave 5) since `STOP_THRESHOLD_PCT=85` + runway estimator pre-flight refusal structurally prevent reaching 95%. Wave 5 doc-ripple discipline honored per memory `feedback_auto_complete_milestone` — no Next Up block, auto-advance into complete-milestone on next commit. Files: `README.md`, `docs/GSD-T-README.md`, `docs/methodology.md`, `docs/architecture.md`, `docs/infrastructure.md`, `docs/requirements.md`, `docs/prd-harness-evolution.md`, `CHANGELOG.md`, `package.json`, `.gsd-t/progress.md`.
- 2026-04-15 02:30: [success] M35 Wave 4 COMPLETE — 6/6 atomic tasks, 985/985 tests green. Delivered: (OB-T1) `bin/token-optimizer.js` created with four declarative detection rules (demote/escalate/runway-tune/investigate), fingerprint-based 5-milestone rejection cooldown, `detectRecommendations({projectDir, lookbackMilestones:3})` + `appendToBacklog` + `readBacklog`/`writeBacklog`/`parseBacklog`/`setRecommendationStatus` exports; OB-T1 test coverage in `test/token-optimizer.test.js` — 18 unit tests across all 4 rules + parseBacklog round-trip + empty-recommendations marker + cooldown filter + robustness against missing/malformed files. (OB-T2) `commands/gsd-t-optimization-apply.md` and `gsd-t-optimization-reject.md` created — apply auto-routes demote/escalate/runway-tune to `/user:gsd-t-quick` and investigate to `/user:gsd-t-backlog-promote`, idempotent on already-promoted; reject marks `status: rejected`+`rejection_cooldown: 5`, captures optional `--reason` in token-log + Decision Log (never in backlog body to keep parseBacklog simple), idempotent. (OB-T3) `commands/gsd-t-complete-milestone.md` Step 14 non-blocking optimizer invocation wrapped in try/catch; `commands/gsd-t-backlog-list.md` gained `--file` flag support (renders `optimization-backlog.md` via parseBacklog with optional `--status {pending|promoted|rejected}` filter, simplified table ID/Type/Status/Evidence); `commands/gsd-t-help.md` new OPTIMIZATION section in summary table + detail entries for both new commands. (OB-T4) `test/token-optimizer.test.js` integration roundtrip: fixture (10 opus success + 10 sonnet 50% failure) → detect returns ≥2 recs → append both → simulate apply (promoted) on demote → simulate reject (cooldown 5) on escalate → re-detect asserts escalate filtered by cooldown. (HAS-T4) new `bin/check-headless-sessions.js` shared helper (exports checkCompletedSessions, markSurfaced, formatBanner, printBannerIfAny, computeDurationLabel) scans `.gsd-t/headless-sessions/` for `status==="completed" && surfaced!==true`, skips `-context.json` files, tolerates malformed JSON silently, sorts oldest-first; `commands/gsd-t-resume.md` gained Step 0.5 Headless Read-Back Banner MANDATORY invoking `node bin/check-headless-sessions.js . 2>/dev/null || true`; `commands/gsd-t-status.md` gained Step 0 Headless Read-Back Banner + Step 0.5 Optimization Backlog Pending Count (one-liner, suppressed when N=0, wrapped in try/catch for optimizer unavailability). (HAS-T5) `test/headless-auto-spawn.test.js` — 16 tests across 8 describe blocks: makeSessionId format, autoSpawnHeadless return+session file, markSessionCompleted, checkCompletedSessions (4 sub-tests), markSurfaced, formatBanner, non-darwin degradation, E2E shim-process smoke (polls for up to 10s for session status transition). Tests use `fs.mkdtempSync` tempdir pattern + shim `bin/gsd-t.js` exiting immediately. `.gsd-t/optimization-backlog.md` stub created with format documentation. `test/filesystem.test.js` updated 51→53 / 56→58 to accommodate two new command files. Wave 4 commit e62d5e3. Next: Wave 5 docs-and-tests T1–T8.
- 2026-04-15 00:30: [success] M35 Wave 2 COMPLETE — 11/11 atomic tasks, 1011/1011 tests green (Wave 1 baseline 954 → +57). Delivered: (DR-T3) swept 6 command files replacing Token Budget Check blocks with three-band handler (normal/warn/stop) — gsd-t-execute/wave/quick/integrate/debug/doc-ripple; (DR-T4) rewrote `docs/prd-harness-evolution.md` §3.7 "Context Gate + Surgical Model Escalation" and updated `templates/CLAUDE-global.md` + `templates/CLAUDE-project.md` to remove `downgrade`/`conserve` references; (MSA-T1) resolved open question — no programmable `/advisor` API exists, documented convention-based fallback at `.gsd-t/M35-advisor-findings.md`; (MSA-T2) created `bin/model-selector.js` with declarative PHASE_RULES table (13+ phases) + complexity signal overrides (cross_module_refactor/security_boundary/data_loss_risk/contract_design escalate sonnet→opus), 36 tests; (MSA-T3) created `bin/advisor-integration.js` convention-based fallback — always returns `{available:false}`, appends `missed_escalation` markers to `.gsd-t/token-log.md`, 14 tests; (MSA-T4) finalized `.gsd-t/contracts/model-selection-contract.md` v1.0.0 annotations; (MSA-T5) injected `## Model Assignment` blocks into 11 command files (execute, wave, quick, integrate, debug, doc-ripple, partition, discuss, plan, verify, test-sync); (TT-T3) wired `## Per-Spawn Token Bracket` T0/T1 shim into 6 command files referencing `bin/token-telemetry.js#recordSpawn` — additive alongside existing OBSERVABILITY LOGGING blocks; (TT-T4) implemented `gsd-t metrics --tokens [--by=...]` CLI in `bin/gsd-t.js` `doMetrics()` with `formatInt`/`formatPct`/`parseMetricsByFlag` helpers, smoke-tested with 3 sample records (format correct: count/total/mean/median/p95 columns); (TT-T5) same `doMetrics()` covers `--halts` (group by halt_type) and `--context-window` (min/mean/p95/max + warn/stop-band counts); test/token-telemetry.test.js expanded from 9 → 16 tests (multi-field grouping, p95 math at n=20, 0-token edge case, unknown-field fallback, halt_type retention). Bug fixed mid-task: `doMetrics` used `g.total` but aggregate() returns `g.total_tokens` — corrected before commit. Wave 2 boundary reached per memory `feedback_no_silent_degradation` (never downgrade/skip under pressure — pause at clean boundaries). Wave 3 pending: runway-estimator T1-T5 + headless-auto-spawn T1-T3.
- 2026-04-14 23:59: [impact-analyzed] M35 IMPACT_ANALYZED — PROCEED WITH CAUTION. Verdict: all 4 breaking changes contained within M35 wave plan; no external consumers; no irreversible failures. Top-3 risks: (1) IMP-003 PLAN GAP — `scripts/context-meter/threshold.js` `BANDS` + `bandFor()` must be updated atomically with `bin/token-budget.js` T1 but was missing from domain task lists; fold into degradation-rip-out T1. (2) IMP-001 — 7 `getDegradationActions` tests in `test/token-budget.test.js` will fail immediately after T1 unless deleted atomically with code edit; T1 must be a single atomic commit including test updates. (3) IMP-002 — exit codes 11/12 (conserve/downgrade) in execute/wave/quick bash snippets become dead code after T1; harmless but stale until Wave 2 DR-T3 sweeps them. Additionally, `bin/gsd-t.js` `doStatus()` switch-case has `downgrade`/`conserve` branches (IMP-004) — fold into DR-T1. Impact report at `.gsd-t/impact-report.md`. Baseline: 941/941 tests green. Target post-M35: ~1030 tests.
- 2026-04-14 23:45: [milestone-partitioned] M35 partitioned into 7 domains. Each domain has scope.md + tasks.md + constraints.md in `.gsd-t/domains/m35-{name}/`. Total tasks per domain: degradation-rip-out (4), model-selector-advisor (6), runway-estimator (5), token-telemetry (6), optimization-backlog (4), headless-auto-spawn (5), docs-and-tests (8) = 38 tasks. Wave structure from M35-definition.md preserved: Wave 1 foundational (degradation-rip-out T1-T2, model-selector-advisor T1, token-telemetry T1-T2), Wave 2 parallel expansion (degradation-rip-out T3-T4, model-selector-advisor T2-T6, token-telemetry T3-T6), Wave 3 protection self-hosts (runway-estimator T1-T5, headless-auto-spawn T1-T3), Wave 4 stabilization (optimization-backlog T1-T4, headless-auto-spawn T4-T5), Wave 5 docs/verify/complete (docs-and-tests T1-T8). Contract deliverables captured in scope.md of each domain: token-budget v3.0.0 rewrite (clean break Option X), model-selection-contract v1.0.0 NEW, runway-estimator-contract v1.0.0 NEW, token-telemetry-contract v1.0.0 NEW, headless-auto-spawn-contract v1.0.0 NEW. Session decision: PAUSED after partition rather than chaining plan→execute. Rationale: (1) Wave 1 code-heavy work (bin/token-budget.js rewrite, test rewrite, contract rewrite, bin/token-telemetry.js skeleton) requires full Red Team and test-suite runs — incompatible with remaining session runway; (2) M35 itself estimates 4-6 resume sessions in its definition; (3) forcing Wave 1 into this session would invoke the silent-degradation pattern M35 exists to eliminate; (4) runway estimator doesn't exist yet to self-protect, so pausing at clean boundaries is the correct bootstrap discipline. Next session: `/clear` then `/user:gsd-t-plan` (expand tasks.md files with atomic dependency ordering) followed by `/user:gsd-t-execute` Wave 1.
- 2026-04-14 23:30: [milestone-defined] M35 formally defined as active milestone. Name: "No Silent Degradation + Surgical Model Escalation + Token Telemetry". Authoritative definition at `.gsd-t/M35-definition.md` (458 lines, committed in 29ee244). Rationale: M31 introduced silent `downgrade`/`conserve` bands in `bin/token-budget.js` that swap models down and skip Red Team/doc-ripple/Design Verify under context pressure — user never agreed to this; it violates GSD-T's core principle of excellent, deeply-tested results. M34 preserved the bands byte-for-byte but made them more dangerous by giving them real context-window measurement. M35 removes the bands entirely and replaces them with: (A) three-band model `normal`/`warn`/`stop` only — tightened thresholds warn@70%/stop@85%; (B) surgical per-phase model selection via `bin/model-selector.js` + `/advisor` escalation, sonnet default for routine, opus surgically at decision points; (C) `bin/runway-estimator.js` pre-flight check that refuses runs projected to cross stop threshold; (D) structural elimination of native compact messages via tighter thresholds + runway estimator; (E) granular per-spawn telemetry → `.gsd-t/token-metrics.jsonl` + `gsd-t metrics --tokens/--halts/--context-window` CLI + `bin/token-optimizer.js` → `.gsd-t/optimization-backlog.md` (detect-only, user selectively promotes via `/user:gsd-t-optimization-apply|reject`); (F) headless auto-spawn on runway refusal — user never types `/clear`, interactive session stays idle; (G) docs + tests ripple (~90 new tests, ~1030 total). Option X confirmed: clean break on `token-budget-contract` v3.0.0, no compat shim. Default routine model: sonnet. 7 domains proposed: degradation-rip-out (4), model-selector-advisor (6), runway-estimator (5), token-telemetry (6), optimization-backlog (4), headless-auto-spawn (5), m35-docs-and-tests (8) = ~38 tasks across 5 waves. Dogfooding target: Wave 3 onward self-protects via own runway estimator + headless auto-spawn. Pre-partition assessment: Complex (7 domains) → standard partition → plan → execute → integrate → verify → complete flow. Version target 2.75.10 → 2.76.10. Test baseline: 941/941 (M34 exit state). Next: `/user:gsd-t-partition`.
- 2026-04-14 23:00: [success] M34 Context Meter complete and archived as v2.75.10. Archive at `.gsd-t/milestones/M34-context-meter-2026-04-14/` with contracts (3), domains (5), progress snapshot, verify-report, and full summary.md. 6 REQs verified, 3 contracts active, 941/941 tests green, 0 findings on goal-backward check. 5 M34 domain folders removed from `.gsd-t/domains/` (other milestones' residual domains left intact). Tag `v2.75.10` pending commit. Do NOT publish per user direction; stop after commit + tag. Successor milestone M35 (No Silent Degradation + Surgical Model Escalation + Token Telemetry) designed during complete-milestone gate and will be created next session — key M35 parts: (A) rip out `downgrade`/`conserve` bands from `bin/token-budget.js`, (B) surgical model escalation via `/advisor` + `bin/model-selector.js`, (C) runway estimator with auto-spawn headless on refusal (never prompts `/clear`), (D) eliminate compact messages structurally, (E) granular per-spawn token telemetry → `.gsd-t/token-metrics.jsonl` + `bin/token-optimizer.js` → `.gsd-t/optimization-backlog.md` (detect-only, user selectively promotes), (F) docs + contracts ripple. Decision X (clean break, no compat shim) confirmed. Default routine model: sonnet. Tag v2.75.10 first, then M35 in a new session.

- 2026-04-14 22:45: [fix] M34 gap closed — 9 command files still referenced retired `Tasks-Since-Reset` column and `{COUNTER}` placeholder from v2.74.12 task-counter era. User caught this during the complete-milestone gate check. Files patched: `gsd-t-verify.md` (Steps 4 and 8 — the latter is the auto-invoke-complete-milestone line itself), `gsd-t-plan.md` (Step 7), `gsd-t-prd.md` (Step 0), `gsd-t-audit.md` (Step 0, also added missing header), `gsd-t-brainstorm.md` (Step 3), `gsd-t-reflect.md` (Step 0 — also scrubbed pre-v2.74.12 TOK_END/TOK_START/TOK_MAX/COMPACTED compaction-detect cruft), `gsd-t-visualize.md` (Step 0 — same TOK_* cleanup), `gsd-t-debug.md` (Step 1.5), `gsd-t-discuss.md` (Step 3). Also cleaned up 2 residual `TOK_START / TOK_MAX / TOK_END` references inside `gsd-t-execute.md` Step 2 (lines 237, 419) and replaced `$TOKENS` with literal `0` in the metrics-collector call on line 423 (per-call token accounting requires API response we don't capture; `context_pct` remains the real burn signal). Each site now uses the canonical CTX_PCT bash shim: `CTX_PCT=$(node -e "const tb=require('./bin/token-budget.js'); process.stdout.write(String(tb.getSessionStatus('.').pct||'N/A'))" 2>/dev/null || echo "N/A")`. Grep sweep confirms zero remaining `task-counter`, `Tasks-Since-Reset`, `{COUNTER}`, `TOK_START`, `TOK_END`, `TOK_MAX`, `$TOKENS`, `CLAUDE_CONTEXT_TOKENS_*` in `commands/`, `scripts/`, `bin/`, `templates/` outside of intentional historical prose (execute.md line 120 explanatory note + CLAUDE-global.md line 373 historical footnote + CLAUDE-project.md retirement comment + bin/gsd-t.js retirement migration function itself). Smoke test: `CTX_PCT` shim exits 0 with `N/A` when state file absent (correct fallback). Full suite 941/941 green unchanged. **M34 gap closed — CHANGELOG "Removed: task-counter.cjs + all CLAUDE_CONTEXT_TOKENS_* references" claim now true across all live surfaces.** Files: 9 command files, `commands/gsd-t-execute.md` (3 sites), `commands/gsd-t-verify.md` (2 sites).

- 2026-04-14 22:30: [fix] Auto-advance gap in `gsd-t-resume.md` — user reported that after Wave 3 of M34 finished under resume, the orchestrator stopped with a "Next Up: /user:gsd-t-complete-milestone" block instead of chaining through verify → complete-milestone automatically. Root cause: `gsd-t-resume.md` Step 4 said "pick up from logical next action" but had no contract for what happens when the resumed work reaches the end of the last wave — it fell through to end-of-command without calling verify. Unlike execute/wave/verify, resume is marked as "no successor" in CLAUDE-global, so the Next Up hint path triggered. Fix: added **Step 5: Auto-Advance Through End of Milestone** to `commands/gsd-t-resume.md` — explicit successor table mapping (task → next task, last task → verify, verify → complete-milestone, complete-milestone → honor outstanding directive), explicit "never stop at Wave N complete" rule, Level-3 self-check before any Next Up hint, and handling of multi-step outstanding user directives captured at pause time. Also extended `commands/gsd-t-pause.md` Step 3 continue-here template with an `## Outstanding User Directive` field so pause captures multi-step chains verbatim and resume can honor them. Memory: saved `feedback_auto_complete_milestone.md` — "Never stop with Next Up /gsd-t-complete-milestone; verify must auto-invoke it, chain must continue." Separately noticed (but deferred as out-of-scope for this fix) that Wave 2 token-budget-replacement Tasks 6/7/8 missed `verify.md`, `plan.md`, `discuss.md`, `visualize.md`, `prd.md`, `audit.md`, `brainstorm.md`, `reflect.md`, `debug.md` Step 1.5 — these still have the retired `Tasks-Since-Reset` column header and `{COUNTER}` placeholder in their token-log append instructions. Not blocking — the header is written once per session and the column is vestigial. Should be swept in a follow-up patch. Files: `commands/gsd-t-resume.md` (+27 lines), `commands/gsd-t-pause.md` (+3 lines).

- 2026-04-14 22:15: [success] M34 Wave 3 m34-docs-and-tests Tasks 1–8 complete — full documentation ripple + version bump. Task 1: `README.md` gained Context Meter feature bullet in Key Features + full "Context Meter Setup" section (API key prompt, doctor check, threshold bands, upgrade from task-counter). Task 2: `docs/GSD-T-README.md` Configuration → Context Meter subsection with data-flow diagram, `ANTHROPIC_API_KEY` setup, threshold table, upgrade migration. Task 3: `templates/CLAUDE-global.md` Context Meter Gate subsection added to Token-Aware Orchestration with historical note about v2.74.12 task-counter era; `templates/CLAUDE-project.md` new Context Meter section for per-project setup. Task 4: `docs/architecture.md` new Context Meter Architecture section with full data-flow diagram (hook → count_tokens → state file → token-budget → orchestrator); `docs/infrastructure.md` Context Meter Setup section (API key, doctor verification, threshold table, upgrade steps); `docs/methodology.md` "Context Awareness: From Proxy to Real Measurement" narrative; `docs/requirements.md` M34 REQ-063–REQ-068 traceability table + functional/non-functional requirements. Tasks 5–7: satisfied by earlier wave commits (`scripts/gsd-t-context-meter.e2e.test.js`, `test/token-budget.test.js` rewrite, `test/installer-m34.test.js`) — marked complete without new test files. Task 8: `package.json` version 2.74.13 → 2.75.10; `CHANGELOG.md` new `[2.75.10] - 2026-04-14` entry with Added/Changed/Removed/Migration/Propagation sections documenting every M34 deliverable (hook, config loader, installer extensions, token-budget rewrite, command-file migration, retirement migration, doctor gate, status line, docs ripple). 941/941 tests green throughout. **CP4 satisfied** — Wave 3 complete. Next: Task 9 final verification + `complete-milestone` + `checkin` + `npm publish` + `version-update-all`.

- 2026-04-14 21:45: [success] M34 installer-integration Tasks 5+6 complete — Wave 2 fully done. Task 5: `bin/gsd-t.js` gained `runTaskCounterRetirementMigration(projectDir, projectName)` — one-shot migration wired into `updateSingleProject` next to `runProgressArchiveMigration`. Removes four legacy targets (`bin/task-counter.cjs`, `.gsd-t/task-counter-config.json`, `.gsd-t/.task-counter-state.json`, `.gsd-t/.task-counter`), skips symlinks, writes `.gsd-t/.task-counter-retired-v1` marker (ISO timestamp + `Replaced-by: scripts/gsd-t-context-meter.js (M34)` line). Marker presence short-circuits second runs to no-op. Logs "retired task-counter (removed N file(s))" or "retired task-counter (no legacy files found)". Fails-open per-target (warn + continue), returns false only if marker write itself fails. Task 6: new `test/installer-m34.test.js` (16 tests across 5 describe blocks) — `runTaskCounterRetirementMigration` (5 tests: legacy removal, idempotency, fresh-project, barren no-.gsd-t, marker contents), `ensureGitignoreEntries` (3 tests: create, idempotent, preserve existing), `installContextMeter` (3 tests: script+runtime copy, config preserves user overrides, .gitignore append), `configureContextMeterHooks` (3 tests: fresh add, idempotent noop, preserves unrelated hooks and top-level keys), `resolveApiKeyEnvVar` (2 tests: default + config override). All 16 tests pass on first run, 941/941 full suite green (up from 925 — +16 new). Files: `bin/gsd-t.js` (+54 lines for migration fn + 1 line wire-up + 1 line export), `test/installer-m34.test.js` (new, 220 lines), `.gsd-t/progress.md` (this entry). Wave 2 installer-integration 6/6 complete; Wave 2 token-budget-replacement 10/10 complete (done in prior commits). Wave 2 total: 16/16. Next: Wave 3 `m34-docs-and-tests` (9 tasks).

- 2026-04-14 21:05: [success] M34 token-budget-replacement Tasks 6+7+8+9+10 complete — task-counter fully retired from command files + bin, CP4 satisfied. Task 6: `commands/gsd-t-execute.md` — Step 0 replaced `node bin/task-counter.cjs reset` with `getSessionStatus('.')` smoke call; Step 3.5 rewrote "Orchestrator Task-Count Gate" → "Orchestrator Context Gate" using exit codes 10/11/12/13 (stop/conserve/downgrade/warn) mapped to thresholds; token-log header `Tasks-Since-Reset` → `Ctx%`; Design Verify + Red Team bash snippets swapped to `CTX_PCT=$(node -e "const tb=require('./bin/token-budget.js'); process.stdout.write(String(tb.getSessionStatus('.').pct))")` pattern. Task 7: `commands/gsd-t-wave.md` — same Step 0 pattern, Wave Orchestrator Context Gate replaces Phase-Count Gate, column rename. Task 8: `commands/gsd-t-quick.md`, `gsd-t-integrate.md`, `gsd-t-debug.md` — all three migrated from `COUNTER=$(node bin/task-counter.cjs status …)` to the CTX_PCT pattern; Tasks-Since-Reset columns renamed. Task 9: `bin/task-counter.cjs` deleted via `git rm` (no test file existed); `bin/gsd-t.js` `PROJECT_BIN_TOOLS` array had `task-counter.cjs` removed — silently-skipped-on-missing copy semantics make the order irrelevant for correctness. Task 10 code-level cleanup: `scripts/gsd-t-statusline.js` rewritten to read `.gsd-t/.context-meter-state.json` (same 5-min staleness window as bin/token-budget.js) instead of env vars; `scripts/gsd-t-heartbeat.js` dropped the always-null `CLAUDE_CONTEXT_TOKENS_USED` env read on SubagentStart/SubagentStop; `templates/prompts/README.md` historical prose scrubbed; `commands/gsd-t-doc-ripple.md` last remaining task-counter snippet (line 101-105) migrated to CTX_PCT. Task 10 verification: `grep -r task-counter commands/ bin/ scripts/ templates/` → 0 hits; `grep -r CLAUDE_CONTEXT_TOKENS commands/ bin/ scripts/ templates/` → 0 hits; `npm test` → 925/925 green (unchanged from Tasks 2-5 baseline — no new tests, no regressions). **CP4 satisfied** — unblocks m34-docs-and-tests Wave 3. Wave 2 token-budget-replacement now 10/10 complete. Next: installer-integration remainder (Tasks 5-6) then Wave 3.

- 2026-04-14 20:30: [success] M34 token-budget-replacement Tasks 2+3+4+5 complete — real-source cutover for `bin/token-budget.js`. Task 2 verified (context-observability-contract.md already at v2.0.0 during partition, all four AC met). Task 3 rewrote `getSessionStatus()` to read `.gsd-t/.context-meter-state.json` first (timestamp within 5min = fresh, otherwise fallback), preserving the public API byte-for-byte. New helpers: `readContextMeterState(dir)` (returns null on missing/corrupt/stale — uses `Date.parse(timestamp)` and `STATE_STALE_MS = 5 * 60 * 1000`), `getSessionStatusHeuristic(dir)` (200k-token default window, sums today's rows from token-log.md via existing `readSessionConsumed`). `estimateMilestoneCost` rewritten to use actual estimatedTokens/window ratio instead of task-slot count — fixes the conceptually-confused "task-slot budget" from v2.74.12. Grep-verified zero `CLAUDE_CONTEXT_TOKENS` / `task-counter` / `.task-counter` references in the rewritten file. Task 4 rewrote `test/token-budget.test.js` to use state-file fixtures (`writeState({inputTokens, ageMs})` helper) instead of `writeCounter`. 11 getSessionStatus tests rewritten to hit state-file path + heuristic fallback + stale-file fallback + missing-file fallback. estimateMilestoneCost tests updated against the new token-window semantics. 37/37 token-budget tests green. Full project suite 925/925 (up from 924 — net +1 new staleness test). Task 5 discovered a partition-time assumption gap: `bin/orchestrator.js` has ZERO references to task-counter, token-budget, or getSessionStatus — the task-budget gate lives in the command files themselves (bash snippets calling `node bin/task-counter.cjs should-stop`), not in orchestrator.js. The cutover happens in Tasks 6/7/8 where command files switch their bash snippets. Task 5's seam ("orchestrator gets a stable public function to call") was already satisfied by Task 3 because `bin/token-budget.js` exports `getSessionStatus` — no orchestrator changes needed. Files: `bin/token-budget.js` (getSessionStatus/readContextMeterState/getSessionStatusHeuristic/estimateMilestoneCost), `test/token-budget.test.js` (full rewrite, 365 lines). Wave 2 now 9/16 tasks complete. Next: Task 6 (remove task-counter from commands/gsd-t-execute.md).

- 2026-04-14 14:05: [success] M34 token-budget-replacement Task 1 complete — `.gsd-t/contracts/token-budget-contract.md` polished to v2.0.0. Full rewrite of the partition-era draft: canonical section ordering (Purpose → Public API → Session Budget → Threshold Bands → Degradation Actions → Integration Points → Task Counter Retirement → Removed/Retired → Backward Compat → Breaking Changes → Change Log), every exported function from `bin/token-budget.js` documented with exact v1.x signature and return shape (all 6: `estimateCost`, `getSessionStatus`, `recordUsage`, `getDegradationActions`, `estimateMilestoneCost`, `getModelCostRatios`), state-file-first data-source flow with ASCII diagram, 5-minute staleness window, fallback to heuristic. Threshold bands documented as lower-bound-inclusive/upper-exclusive matching the `>=` comparisons in `resolveThreshold()`. Integration Points table lists all 9 consumers (orchestrator Task 5, execute Task 6, wave Task 7, quick/integrate/debug Task 8, complete-milestone, `doStatus` commit dc34881, `doDoctor` commit becf318). Task Counter Retirement section traces the timeline v1.0.x env-var → v2.74.12 proxy → v2.75.10 real meter, with explicit migration path referencing Tasks 3–9 in this domain plus installer-integration Task 5. Removed/Retired table covers env vars, Tasks-Since-Reset column, task-counter.cjs, orchestrator task-increment calls, env-var tests. Backward Compat + Breaking Changes make the caller-side zero-change promise explicit. Grep for `CLAUDE_CONTEXT_TOKENS` inside the contract returns 9 hits — all inside retirement-context prose (Purpose / Session-Budget negative assertion / Removed-Retired / Task-Counter-Retirement / Change-Log), which matches acceptance criterion "removed or explicitly marked as retired". Zero code changes. Full test suite 924/924 green (sanity check only). Constraint finding: no separate `threshold.js` module exists in `bin/`; band boundaries live solely in `bin/token-budget.js` THRESHOLDS constant, so the contract documents one canonical source and Task 3 will preserve it. Second finding: current `bin/token-budget.js` `getSessionStatus()` already no longer reads env vars — it reads `.gsd-t/.task-counter` (v2.74.12 stopgap). Task 3's actual rewrite is task-counter→state-file, not env-var→state-file. The contract's Change Log documents the three-stage history accurately. Files: `.gsd-t/contracts/token-budget-contract.md` (full rewrite, 255 lines), `.gsd-t/domains/token-budget-replacement/task-1-summary.md` (new).
- 2026-04-14 13:10: [success] M34 installer-integration Task 4 complete — `bin/gsd-t.js doStatus` now emits a real-context-window status line. Added `formatRelativeTime(iso)` helper (zero-dep, 5-line ISO→"Xs/Xm/Xh/X days ago" formatter, clock-skew safe) and `showStatusContextMeter()` (wired into `doStatus` between `showStatusTeams` and `showStatusProject`). Reads `{cwd}/.gsd-t/.context-meter-state.json` defensively (try/catch around existsSync+readFileSync+JSON.parse — any error = "N/A (meter hook not run this session)"). Four output modes: (1) missing/corrupt → dim "Context: N/A (meter hook not run this session)", (2) fresh (ts within 5 min) → "Context: {pct.toFixed(1)}% of {modelWindowSize} tokens ({threshold} band) — last check {rel}" colored per threshold (normal=GREEN, warn/downgrade=YELLOW, conserve=RED, stop=BOLD+RED), (3) stale (>5 min) → same line + " (stale)", (4) error (inputTokens===0 && lastError) → dim "Context: N/A (meter error: {lastError.code}) — last check {rel}" — NEVER prints the `message` field (contract rule #3). Invalid ts (null/non-string) yields "unknown" for relative time, never crashes. 7 smoke tests all PASS: (1) state absent → N/A + exit 0, (2) fresh warn band → "62.2% of 200000 tokens (warn band) — last check 0s ago", no stale, (3) stale 10m → "(stale)" appended, (4) error with secret message → shows "api_error" code, confirmed no leak of message string, (5) corrupt JSON "not json{" → N/A + exit 0, no crash, (6) diff vs pre-change status output = exactly +3 lines (heading + data + blank), all pre-existing lines preserved, (7) full test suite 924/924 green. No new imports, zero external deps. Files: `bin/gsd-t.js` (+76 lines). CP3 installer-integration now 4/6 tasks complete.
- 2026-04-14 12:51: [success] M34 installer-integration Task 3 complete — `bin/gsd-t.js` gains `checkDoctorContextMeter(projectDir)` (async, zero-dep) wired into `doDoctor` as the last sub-check. Five checks in order: (1) API key env var read from `resolveApiKeyEnvVar` equivalent via `loadConfig(cwd).apiKeyEnvVar` — RED if unset/empty, prints only the var NAME never the key; (2) PostToolUse hook marker `CONTEXT_METER_HOOK_MARKER` scanned in `~/.claude/settings.json` → RED if absent; (3) `{cwd}/scripts/gsd-t-context-meter.js` existence → RED if missing; (4) config file load via `require("./context-meter-config.cjs").loadConfig(cwd)` wrapped in try/catch → RED on throw, GREEN if `.gsd-t/context-meter-config.json` exists on disk, YELLOW (non-fatal) when the loader returns defaults from a missing file; (5) live `count_tokens` dry-run via `require("{cwd}/scripts/context-meter/count-tokens-client.js").countTokens({model:"claude-opus-4-6", system:"", messages:[{role:"user", content:[{type:"text", text:"ping"}]}], timeoutMs:5000})` — skipped with DIM note when Check 1 is RED, otherwise GREEN on `{inputTokens}`, RED on null or throw. `doDoctor` is now async (switch case wrapped in `.catch(...)`) and calls `process.exit(1)` when any sub-check reported an issue (previously always exited 0). Regression in `test/filesystem.test.js:331` ("doctor subcommand runs without error") — test replaced `execFileSync` with `spawnSync` and now accepts exit 0 OR 1 (intent: command ran, not zero-issues). Smoke tests: (1) GSD-T project → exit 1, Checks 1/2 RED (no key, hook not in global settings yet), Check 3 GREEN, Check 4 YELLOW, Check 5 skipped. (2) Fresh tempdir `init` → exit 1, Check 1 RED (no key), Checks 2/3/4 GREEN (hook registered, script copied, config template copied), Check 5 skipped. (3) Bare tempdir (no init) → exit 1, Checks 1/3 RED, Check 2 GREEN (leftover from prior test registration — global setting), Check 4 YELLOW. (4) `ANTHROPIC_API_KEY=""` → Check 1 RED + Check 5 skipped, exit 1. (5) Live API dry-run unverifiable in CI (no key in shell) — logic path exercised but not validated against real endpoint. Full test suite 924/924 green. Files touched: `bin/gsd-t.js` (+115 lines: new fn + doDoctor async conversion), `test/filesystem.test.js` (+18 lines: test loosened). CP3 installer-integration moves from 2/6 to 3/6 tasks complete.

- 2026-04-14 23:00: [success] M34 installer-integration Task 2 complete — `bin/gsd-t.js` extended with Context Meter install path. New helpers (additive, zero-dep): `ensureGitignoreEntries` (idempotent .gitignore appender — creates file if missing, skips duplicates, skips symlinks), `installContextMeter(projectDir)` (copies `scripts/gsd-t-context-meter.js` + `scripts/context-meter/*.js` runtime files excluding `*.test.js` and `test-injector.js`, copies `templates/context-meter-config.json` → `.gsd-t/context-meter-config.json` ONLY if absent (never overwrites user config), appends the two .gitignore entries), `configureContextMeterHooks(settingsPath)` (merges a PostToolUse `matcher:"*"` entry into settings.json via direct writeFileSync — mirrors existing heartbeat pattern, non-atomic, symlink-guarded, refreshes command in-place on path change, preserves all other hooks/keys; returns `{installed, action}` with `added|updated|noop`), `promptForApiKeyIfMissing(envVarName)` (async TTY-only readline prompt, never writes key anywhere — just prints the `export VAR="..."` line for the user to paste into their own shell profile; non-blocking error recovery), `resolveApiKeyEnvVar(projectDir)` (loads config via `bin/context-meter-config.cjs`, defaults to `ANTHROPIC_API_KEY`). `PROJECT_BIN_TOOLS` gains `context-meter-config.cjs` (task-counter.cjs retained — Task 5 retires it). `doInstall`/`doUpdate`/`doUpdateAll`/`updateGlobalCommands`/`doInit` async-ified; install/update/update-all/init cases in main switch wrapped in `.catch(e => { error; process.exit(1); })`. Wired: `doInstall` calls `configureContextMeterHooks` + `promptForApiKeyIfMissing` (global hook only); `doInit` calls `installContextMeter` + `configureContextMeterHooks` + `promptForApiKeyIfMissing` (full per-project stack). Hook command string: `node "$CLAUDE_PROJECT_DIR/scripts/gsd-t-context-meter.js"`. Smoke tests in /tmp/gsd-t-m34-smoke passed: fresh install (12 file/hook assertions), idempotency (config mtime unchanged, hook count stays at 1, .gitignore lines not duplicated), settings.json merge (pre-existing PreToolUse/PostToolUse entries + `env`/`theme` top-level keys all preserved while new entry appended). `status`/`doctor`/`help`/`--version` all exit 0. Full test suite 924/924 green (no regressions). Async ripple confined to 4 switch cases — no atomic-write refactor introduced. CP3 dependency for Task 5 (task-counter retirement migration) unblocked.

- 2026-04-14 22:30: [success] M34 installer-integration Task 1 (read-only inventory) complete — surveyed bin/gsd-t.js (2809 lines) and produced `.gsd-t/domains/installer-integration/inventory.md`: surgical-plan map of every touch point Tasks 2-5 will edit. Key findings: (1) `task-counter.cjs` has exactly ONE reference in bin/gsd-t.js — line 1563 `PROJECT_BIN_TOOLS` array — so installer-side retirement is a one-string delete plus a new migration fn modeled on `runProgressArchiveMigration` (marker `.gsd-t/.context-meter-migration-v1`). (2) `installHeartbeat` + `configureHeartbeatHooks` (lines 315–367) are near-perfect templates for Task 2's PostToolUse hook install — same copy-script + register-in-settings.json + idempotent-by-name-match pattern. (3) No existing `.gitignore` handling and no existing TTY/readline usage — Task 2 must add both (both zero-dep). (4) `doInstall` is synchronous; the API-key prompt will require async-ifying `install`/`update`/`update-all` OR using a blocking readline pattern. (5) `doDoctor` + `doStatus` are thin orchestrators (lines 1790 and 1145) — Tasks 3 and 4 each need one new sub-fn and one wiring line, very low risk. (6) Settings.json writes in this file are non-atomic; Task 2 should stay consistent and not introduce atomic writes. Zero code changes in this task. Unblocks Tasks 2-5. Artifact: `inventory.md` (260 lines) + `task-1-summary.md`.

- 2026-04-14 22:00: [success] M34 Wave 1 COMPLETE — context-meter-hook domain all 5 tasks done; unblocks Wave 2 (installer-integration + token-budget-replacement). Task 5 delivered `scripts/gsd-t-context-meter.e2e.test.js` (4 black-box integration tests — below-threshold, above-threshold, missing-key, checkFrequency-skip) plus `scripts/context-meter/test-injector.js` (test-only NODE_OPTIONS `--require` hook that monkey-patches `countTokens` to inject `_baseUrl` at the local stub HTTP server — gated on `GSD_T_CONTEXT_METER_TEST_BASE_URL`, silent no-op when unset, never loaded by production). Real child-process spawn of `node scripts/gsd-t-context-meter.js` with cwd=tempdir, stub HTTP server on `127.0.0.1:0`, stdin payload, stdout+state-file assertions, afterEach sandbox dispose (kills child procs, closes server, removes tempdir). All four E2E tests pass in ~170ms total. Context-meter suite 90/90 (was 86/86 — 4 new E2E tests). Full project suite 924/924 (was 919/919 — 5 new tests vs 924 baseline: the E2E suite added 4 but the test runner also picked up prior changes). Zero production-code changes. Next: Wave 2 — installer-integration + token-budget-replacement in parallel.

- 2026-04-14 21:10: [success] M34 context-meter-hook Task 3 complete — `scripts/context-meter/threshold.js` is a zero-dep pure-function module exporting `computePct`, `bandFor`, `buildAdditionalContext`, plus a frozen `BANDS` constant. Boundaries confirmed byte-for-byte against `bin/token-budget.js` THRESHOLDS (warn=60, downgrade=70, conserve=85, stop=95 — lower-bound inclusive). `computePct` returns 0 on every non-finite / zero-window / negative input (fail-safe), does NOT clamp above 100 so 125% is preserved for the hook's diagnostic log. `bandFor` returns `"normal"` on NaN/Infinity/undefined (never escalate on garbage). `buildAdditionalContext` emits the exact contract string `⚠️ Context window at {pct.toFixed(1)}% of {modelWindowSize}. Run /user:gsd-t-pause to checkpoint and clear before continuing.` — raw integer window size (no commas, no K suffix), one decimal place on pct — and returns `null` below `thresholdPct` or on non-finite inputs. 35/35 threshold tests green, full context-meter suite 71/71 — no regressions.

- 2026-04-14 21:20: [success] M34 CP2 satisfied — 1f19072 — `scripts/gsd-t-context-meter.js` wires Tasks 1-3 + config loader into the PostToolUse hook. `runMeter({payload, projectRoot, env, clock?, baseUrl?, _loadConfig?, _parseTranscript?, _countTokens?})` is the testable async core; CLI shim at `require.main === module` handles stdin/stdout/exit. Atomic state writes via temp+rename under `.gsd-t/.context-meter-state.json`; append-only line-based diagnostic log to `.gsd-t/context-meter.log` (categories + counts only, no message content, no API key). Fail-open is absolute: outer try/catch in `runMeter` + outer try/catch in `main` + every stub-throw path still writes state and returns `{}`. Check-frequency gate short-circuits BEFORE parseTranscript/countTokens so skipped ticks cost only one state read/write. Choice on API failure: reset `inputTokens` to 0 (not preserve prior) to eliminate stale-reading false-positives on the threshold check. 15 unit tests green (node:test + node:assert, all stubs injected via runMeter seams; no real network, no real config file). Full context-meter suite 86/86. Project test suite 919/919 (was 845 baseline after CP1 — 74 tests added across Tasks 1-4). CP2 unblocks installer-integration and token-budget-replacement Task 3 (real-count tests). Next: context-meter-hook Task 5 (E2E test) or advance to installer-integration wave.

- 2026-04-14 20:40: [success] M34 context-meter-hook Task 2 complete — `scripts/context-meter/count-tokens-client.js` is a zero-dep Node.js HTTPS client for `POST /v1/messages/count_tokens`. Single-function `countTokens({apiKey, model, system, messages, timeoutMs, _baseUrl?})` → `{inputTokens}` on 200, `null` on every failure mode (invalid opts, 401/403/429/5xx, network error, timeout, malformed JSON, missing `input_tokens`). Hard timeout via `req.setTimeout` + `req.destroy()` on fire (releases socket before the hook's 200ms latency budget expires). Drops `system` key from the request body when the value is `""` — the endpoint 400s on empty system; matches the `{system:"", messages:[…]}` shape produced by Task 1's transcript-parser, so the hook can pass them through unchanged. Test-only `_baseUrl` override lets tests point at a local stub http server bound to `127.0.0.1:0`; `URL` parsing picks http vs https transparently. NEVER logs request body — only returns `null` silently. 11/11 client tests green (happy path, 401, 429, 500, timeout, malformed JSON, missing field, empty/non-empty system body shape, headers, invalid opts). Full context-meter suite 36/36 — no regressions.

- 2026-04-14 20:10: [success] M34 context-meter-hook Task 1 complete — `scripts/context-meter/transcript-parser.js` streams a Claude Code JSONL transcript via `readline`/`createReadStream` and returns `{system, messages}` shaped for `count_tokens`. Zero deps. Null on unreadable file / fatal read error; malformed individual lines skipped. Observed Claude Code transcript types and annotated in the top-of-file doc comment: user/assistant carry `message.content` that may be STRING or ARRAY; assistant arrays contain text/thinking/tool_use blocks (thinking dropped — not accepted by count_tokens); user arrays carry tool_result blocks whose `content` is itself STRING or ARRAY of text/image blocks. tool_use caller field stripped; `is_error:false` not preserved (only `true`). Unknown event types (summary, attachment, file-history-snapshot, permission-mode, queue-operation, last-prompt, system) are skipped forward-compatibly. 13/13 parser tests green; full project suite 25/25 (13 new + 12 config) — no regressions.

- 2026-04-14 19:56: [execute] M34 Wave 1 / context-meter-config domain complete — CP1 satisfied. 4 tasks: (1) context-meter-contract.md DRAFT → ACTIVE (v1.0.0), (2) templates/context-meter-config.json default, (3) bin/context-meter-config.cjs loader with range validation + API-key-leak detection, (4) 12 unit tests green. Full suite 845/845 (up from 833 baseline). Also added SessionStart auto-resume hook at .claude/settings.json (startup|clear matchers, injects /user:gsd-t-resume, guarded by .gsd-t/progress.md existence) — uncommitted, scoped to this project. Prior commits this session: b1e51c5 (orchestrator async rework + design-review server), 8efbe38 (M34 partition+plan), c3098b2 (config domain). Next: context-meter-hook domain (5 tasks, CP2) — requires fresh session (task counter 4/5).

- 2026-04-14 18:55: [plan] M34 planned: 34 atomic tasks across 5 domains (config 4, hook 5, installer 6, token-budget-replacement 10, docs-and-tests 9). All tasks bound to contracts with explicit dependencies, acceptance criteria, and file lists. Cross-domain duplicate operation scan: no duplicates detected, no shared-core needed. Task scope validation: no task exceeds >5 files AND >3 dependencies (auto-split rule not triggered). Checkpoints CP1–CP4 mapped to specific task completions. Next: `/user:gsd-t-execute`.

- 2026-04-14 18:30: [partition] M34 partitioned into 5 domains. Wave ordering: (W1) context-meter-config ∥ context-meter-hook → (W2) installer-integration ∥ token-budget-replacement → (W3) m34-docs-and-tests. Four checkpoints defined (CP1–CP4). New contract: context-meter-contract.md (schema + state file format + hook I/O). Updated contracts: token-budget-contract.md → v2.0.0 (real-count source), context-observability-contract.md → v2.0.0 (Ctx% source), integration-points.md (M34 dependency graph + file-ownership map). Design brief + design contract steps skipped — no UI signals. Green baseline confirmed: 833/833 tests passing before partition. Next: `/user:gsd-t-plan`.

- 2026-04-14 18:00: [decision] M34 formalized as active milestone — Context Meter REPLACES task counter, not defense-in-depth. Roadmap scope updated: task counter retired in same milestone (transitional during dev + brief field validation, then full removal); `gsd-t doctor` gains hard dependency on API key; migration step purges `.task-counter` state in downstream projects. Rationale: task counter is a proxy and the v2.74.12 regression exposed its fragility; Opus-primary usage compounds compaction risk; real measurement is the only durable fix. Next: partition M34 into 5 domains (context-meter-hook, context-meter-config, installer-integration, token-budget-replacement, docs-and-tests).

- 2026-04-09: [fix] Unlimited human review cycles with auto-review reset — human review loop no longer capped. After each human fix: re-measure → re-run auto-review (fresh cycle counter) → re-queue for human. Repeats until zero changes submitted. (v2.73.27)

- 2026-04-09: [feat] AI prompt assistant in review panel — expandable header panel (Ctrl+K) for component Q&A and correction prompt help. Uses Claude Code CLI (claude -p) for Max subscription support. "Use as comment" copies refined text to per-component feedback. New /review/api/contract and /review/api/ai-assist endpoints. Zero deps. (v2.73.26)

- 2026-04-09 00:15: [feat] Permitted value dropdowns + enhanced visual cue — enum CSS properties (display, flexDirection, textAlign, alignItems, justifyContent, fontWeight, overflow, position) now show a select dropdown instead of text input. Generic flashZone fallback enhanced with bright outline + value label. All properties now editable (added overflow, position, top, left, boxShadow, fontFamily). Playwright-verified: display→select with 9 options, textAlign→select. (v2.73.18)

- 2026-04-09 00:05: [feat] Fixture data tree in property inspector — /review/api/fixture endpoint returns extracted test fixture data. Inspector renders expandable tree showing columns, rows, segments, etc. with color swatches. Collapsible at every level. Playwright-verified: TableStripedHeader shows columns[5] and rows[5] tree. (v2.73.17)

- 2026-04-13 17:25: [feat] Rolling Decision Log archival to fix mid-session compaction regression — added bin/archive-progress.js (keeps last 5 entries live, rolls older into 20-entry windows under .gsd-t/progress-archive/), bin/log-tail.js (truncates test/build output), bin/context-budget-audit.js (preamble cost diagnostic). Auto-migrated this project: 163KB → 42KB, Decision Log section dropped 100KB+ → 13KB. Wired into version-update-all to copy bin tools into every registered project and run a one-time archive migration (gated by .gsd-t/.archive-migration-v1 marker). Root-cause fix for the "manual /compact prompts started 2026-04-10" regression — every command that read progress.md was paying 25% of the context window per read. (v2.74.10)

- 2026-04-13 17:55: [fix] Renamed bin tools .js → .cjs to support ESM projects — first version-update-all hit a CommonJS/ESM error on BDS-Analytics-UI (which has "type": "module" in package.json). archive-progress.js, log-tail.js, and context-budget-audit.js now use .cjs extension so they run as CommonJS regardless of the host project's module type. Cleaned up broken .js copies from 13 already-updated projects, retried update-all successfully. 11 of 13 projects ran their one-time migration on first update-all. (v2.74.11)

- 2026-04-14: [fix] P0 context-burn regression — `CLAUDE_CONTEXT_TOKENS_USED` / `CLAUDE_CONTEXT_TOKENS_MAX` env vars were vaporware (Claude Code never exports them). The "orchestrator context self-check" added in commit `0b91429` was silently inert, and commits `da6d3ae` / `b68353e` piled per-task Red Team + Design Verification on top assuming it would catch drain. bee-poc reproducer drained 77% → 12% context in 2 tasks. Comprehensive two-layer fix: (1) NEW `bin/task-counter.cjs` — real on-disk task-count gate with `should-stop` exit code 10 at limit, wired into execute.md (Step 0 reset, Step 3.5 gate, Step 5 increment) and wave.md (phase-count gate). `bin/token-budget.js` rewritten to read the counter instead of env vars, API surface preserved. (2) Extracted QA / Red Team / Design Verify prompts to NEW `templates/prompts/*.md` — commands reference by file path so orchestrator never re-materializes prompt bodies; reverted Red Team + Design Verify to per-domain cadence (raises safe-task-count per session from ~5 to ~15+); QA stays per-task. Schema: removed Tokens/Compacted/Ctx% columns from token-log, added Tasks-Since-Reset. Neutralized 70+ env-var refs across 14 command files. 36/36 tests passing. Requires `/user:gsd-t-version-update-all` to propagate to all registered projects. (v2.74.12)

- 2026-04-14 17:10: [fix] P0 v2.74.12 task-counter distribution gap — v2.74.12 shipped `bin/task-counter.cjs` and wired every command file to call it, but `PROJECT_BIN_TOOLS` in the installer (`bin/gsd-t.js:1562`) was never updated to include the new file, so downstream projects ran command files that referenced a non-existent script. `node bin/task-counter.cjs …` threw "Cannot find module" everywhere, swallowed by `2>/dev/null`, and the orchestrator silently continued with no gate. Confirmed in bee-poc during 30-min monitoring: multiple tasks executed while counter stayed at `{"count":0}` and token-log got zero rows. Additionally found `doInit()` (`bin/gsd-t.js:1095`) never called `copyBinToolsToProject` at all — new `gsd-t init` projects were born with no bin tools until user ran `update`. Fix: two-line change in `bin/gsd-t.js` — append `"task-counter.cjs"` to `PROJECT_BIN_TOOLS`, add `copyBinToolsToProject` call to `doInit`. v2.74.12's entire two-layer fix (task-count gate + extracted prompts) is correct — it just needed one line to actually ship the counter script. Requires `/user:gsd-t-version-update-all` to propagate. (v2.74.13)

- 2026-04-14 16:30: [feature] Context Meter — defined feature M34 in roadmap. Uses free Token Counting API (`POST /v1/messages/count_tokens`) via PostToolUse hook to measure real context window usage. Hook reads `transcript_path` from stdin, reconstructs messages, calls API with separate free API key, returns `additionalContext` when usage > 75% threshold instructing Claude to pause. Replaces task-count proxy with actual token measurement while keeping task counter as defense-in-depth. Research confirmed: ANTHROPIC_BASE_URL proxy doesn't work with Max subscription; count_tokens API is free with separate rate limits; PostToolUse hooks receive transcript_path and can return additionalContext.

## Session Log
| Date | Session | What was accomplished |
|------|---------|----------------------|
| 2026-02-07 | 1 | Project initialized, full codebase scan completed |
| 2026-02-09 | 2 | Doc ripple + test verify enforcement, Destructive Action Guard, CLI update-all/register, Milestone 1 defined |
| 2026-02-10 | 3 | Milestone 1: plan → execute → verify → complete. 14 tasks, 3 domains, v2.8.0 tagged. |
| 2026-02-17 | 4 | Milestone 2: QA Agent — Test-Driven Contracts. 11 tasks, 3 domains, v2.22.0. Wave rewrite v2.23.0. |
| 2026-02-18 | 5 | Full scan (5 agents), Contract & Doc Alignment milestone (6 TDs resolved, v2.21.2). Merged origin/main (v2.23.0). Scan #3: 10 new items, 1 regressed, 1 worsened. 26 open items. |
| 2026-02-18 | 6 | Promoted 25/26 debt items → 5 milestones (M3-M7). Executed all 5 milestones: M3 count fix (v2.23.1), M4 testing (v2.24.0, 64 tests), M5 security (v2.24.1, 30 tests), M6 CLI quality (v2.24.2, 22 tests), M7 command cleanup (v2.24.3). Scan #4: 25/26 resolved, 12 new (all small), 116/116 tests pass. |
| 2026-02-18 | 7 | M8: Housekeeping + Contract Sync. 6 tasks, 1 domain, 13 tech debt items (12 resolved + TD-029 accepted as risk). Zero open tech debt. v2.24.4 |
| 2026-02-18 | 8 | Scan #5: post-M8 analysis. 10 new LOW items (TD-056-TD-065). 0 critical/high/medium. 87 functions, 54 exports, 116 tests all pass. Codebase in excellent health. |
| 2026-02-18 | 9 | Promoted 10 items → M9 Cleanup Sprint. Executed 6 tasks (dead code, case fallthrough, condition fix, title scrub, 9 new tests, contract fixes). 125/125 tests. Zero open tech debt. v2.24.5 |
| 2026-02-18 | 10 | Versioning scheme changed (patches ≥10). M10 Token Efficiency (QA refactor, subagent wraps, v2.25.10). M11 Execution Quality (Deviation Rules, per-task commits, wave spot-check, v2.26.10). M12 Planning Intelligence (CONTEXT.md, plan validation, REQ traceability, v2.27.10). M13 Tooling & UX (gsd-t-tools.js, statusline, health/pause commands, wave groupings, v2.28.10). All 4 milestones from roadmap.md complete. |
| 2026-02-18 | 11 | Full codebase scan #6 (lead agent, 5 dimensions parallel). Post-M10-M13 analysis at v2.28.10. 14 new items found (TD-066 through TD-079): 0 critical, 1 high, 5 medium, 7 low. Key findings: (1) gsd-t-tools.js + gsd-t-statusline.js have no module.exports — zero test coverage (TD-066, HIGH); (2) qa-agent-contract.md still lists partition/plan as QA phases post-M10 (TD-067); (3) living docs not updated after M10-M13 — addressed in this scan (TD-068); (4) wave-phase-sequence contract missing M11/M12 additions (TD-069); (5) progress-file-format contract missing M11-M13 state artifacts (TD-070); (6) stateSet allows markdown injection via newlines (TD-071). 125/125 tests still passing. Updated all living docs (architecture, workflows, infrastructure, requirements). |
| 2026-02-19 | 13 | Added backlog item #2: "Living docs staleness detection" (type: improvement, app: gsd-t, category: commands) — gsd-t-health STALE flag for placeholder-only docs + scan self-check after writing living docs |
| 2026-02-19 | 16 | Added backlog item #4: "DB integration testing capability in QA agent" (type: feature, app: gsd-t, category: commands) — DB-agnostic test phase reading infrastructure.md for connection, migrations, seed, teardown |
| 2026-02-22 | 18 | Token logging coverage extended to all subagent-spawning commands (v2.28.14): added OBSERVABILITY LOGGING block to gsd-t-quick (Step 0), gsd-t-debug (Step 0), gsd-t-verify (Step 4 Team Mode), gsd-t-wave (Phase Agent Spawn Pattern), gsd-t-brainstorm (Step 3 Team Mode), gsd-t-discuss (Step 3 Team Mode); CLAUDE.md updated with new-command convention (Command Files), Pre-Commit Gate check, and Don't Do These Things rule; 127/127 tests pass |
| 2026-02-22 | 20 | Fix getRegisteredProjects to strip `|display name` suffix from project registry entries (v2.29.11): split each line on `|` and take the path segment before it, so entries written as `path|label` (legacy format) are handled correctly without warnings |
| 2026-02-22 | 19 | Added gsd-t-prd command — GSD-T-optimized PRD generator (v2.29.10): new command reads all project context (CLAUDE.md, progress.md, docs/, contracts/), runs adaptive intake, generates PRD with REQ-IDs/field-level data model/file-path components/milestone sequence, outputs to docs/prd.md; spawns subagent via Step 0 pattern with OBSERVABILITY LOGGING; updated 6 reference files (gsd-t-help.md, GSD-T-README.md, README.md, CLAUDE-global.md template, CLAUDE.md, package.json 2.28.14→2.29.10, count 45→46) |
| 2026-02-19 | 17 | Token-log compaction-aware schema (v2.28.13): new columns Datetime-start/end + Tokens + Compacted; bash snippet reads CLAUDE_CONTEXT_TOKENS_USED before/after each spawn; compaction detected when end<start and approximates total via (TOK_MAX-TOK_START)+TOK_END; heartbeat SubagentStart/Stop now emit token snapshot; 127/127 tests pass |
| 2026-02-19 | 15 | Added backlog item #3: "Auto-cleanup test data after test runs" (type: improvement, app: gsd-t, category: cli) — temp files/dirs from test suite must be removed on completion |
| 2026-02-19 | 14 | Fix heartbeat hooks for dashboard agent graph (v2.28.12): PostToolUse now writes agent_id (null when absent); SubagentStart + SubagentStop now write parent_id (parent_agent_id if available, else session_id as root). Enables dashboard to draw agent edges and attribute tool calls to correct nodes. 127/127 tests pass. |
| 2026-02-23 | 23 | GSD-T project guard for auto-route hook (v2.31.11): added fs.existsSync guard for .gsd-t/progress.md in cwd to gsd-t-auto-route.js — hook now silent in non-GSD-T directories; CLAUDE-global.md + live CLAUDE.md note updated; 127/127 tests pass |
| 2026-02-23 | 22 | Auto-Route feature (v2.31.10): added scripts/gsd-t-auto-route.js (UserPromptSubmit hook — plain text prompts auto-routed through /gsd, slash commands pass through unchanged); updated bin/gsd-t.js with installAutoRoute()/configureAutoRouteHook() called from doInstall(); updated templates/CLAUDE-global.md and live ~/.claude/CLAUDE.md Conversation vs. Work section with [GSD-T AUTO-ROUTE] signal recognition; updated commands/gsd-t-help.md, docs/GSD-T-README.md, README.md with auto-route documentation. 127/127 tests pass. |
| 2026-02-22 | 21 | Auto-clear context window after safe commands (v2.30.10): added `## Auto-Clear` section to 41 of 42 gsd-t-* command files — instructs Claude to execute `/clear` after each command completes, freeing the context window for the next command. Excluded: gsd-t-resume.md (would defeat purpose of restoring session context). gsd-t-wave.md gets auto-clear only at the very end of the full wave cycle (after all phases complete). All work is committed to project files so clearing is safe. 127/127 tests pass. |
| 2026-03-04 | 25 | M14 Execution Intelligence Layer (partition→verify→complete, v2.32.10): JSONL event stream (gsd-t-event-writer.js), heartbeat enrichment, outcome-tagged Decision Log, Reflexion pre-task retrieval (execute/debug), phase_transition events (wave), distillation step (complete-milestone), gsd-t-reflect command (#47). 153 tests. |
| 2026-03-04 | 26 | M15 Real-Time Agent Dashboard (partition→verify→complete, v2.33.10): gsd-t-dashboard-server.js (141 lines, zero deps, SSE), gsd-t-dashboard.html (194 lines, React Flow + Dagre CDN), gsd-t-visualize command (#48, 104 lines). 176/176 tests. |
| 2026-02-19 | 12 | Observability & model optimization (v2.28.11): (1) model: haiku for execute QA, integrate QA, plan validation, status, health, scan architecture/business-rules/contracts teammates; (2) model: sonnet kept for scan security/quality teammates; (3) MANDATORY observability logging added to execute/integrate/plan — before/after Bash timestamps + append to .gsd-t/token-log.md and .gsd-t/qa-issues.md; (4) Observability Logging directive added to CLAUDE.md; (5) init.md creates token-log.md + qa-issues.md with header rows; (6) TD-080 added for log archiving/summarizing. 125/125 tests pass. |
- 2026-04-06 10:27: [feat] Added Design System Detection step to design pipeline — gsd-t-design-decompose.md (Step 0.4), gsd-t-design-audit.md (Step 0), design-to-code.md (new Section 1, all subsequent sections renumbered 2→18). Agents now ask for design system/component library URL upfront, fetch docs, catalog available components, and map design elements to library primitives. Reduces custom-build effort and improves fidelity for projects using libraries like shadcn-vue, Vuetify, Radix, MUI. Verification checklist updated with 2 new items.
- 2026-04-06 10:37: [feat] Added SVG Structural Overlay Comparison as a mandatory verification layer in the design pipeline. Exports Figma frame as SVG, parses element positions/dimensions/colors, maps to built DOM bounding boxes, compares geometry (≤2px = MATCH). Catches aggregate spacing drift, alignment issues, and proportion errors that pass property-level checks. Added to: gsd-t-execute.md (Step 5 inside Design Verification Agent), gsd-t-quick.md (step 7 inside Design Verification Agent), gsd-t-design-audit.md (Step 3.5), design-to-code.md (Target 3 + verification workflow step 7 + checklist item). Three verification layers now: property comparison (values), SVG structural overlay (geometry), Red Team (behavior).
- 2026-04-06 16:07: [feat] Added DOM Box Model Inspection + Layout Arithmetic + Flex Anti-Pattern Rule (v2.70.11). Three verification gaps closed from BDS 5-round fix analysis: (1) Playwright-based offsetHeight vs scrollHeight check catches inflated flex elements, (2) widget contract mandatory height budget math prevents gap miscalculation, (3) explicit prohibition on flex:1 for content centering. Updated: gsd-t-execute.md (Step 5.5), gsd-t-quick.md (step 8), gsd-t-design-audit.md (Step 3.75), design-to-code.md (Section 8 anti-patterns + checklist), widget-contract.md (Internal Layout Arithmetic section + 3 checklist items).
- 2026-04-06 16:14: [feat] Added Element Count Reconciliation (v2.70.12). Counts widgets/elements from Figma INDEX.md, compares against built DOM count via Playwright. Missing widget = CRITICAL. Added to execute (Step 0), quick (Step 0), design-audit (Step 1.5), design-decompose (Figma Element Counts table in INDEX.md). Design-to-code.md now documents 5-layer verification model: Target 0 (count) → Target 1 (contracts) → Target 2 (Figma) → Target 3 (SVG overlay) → Target 4 (DOM box model).
- 2026-04-06 16:20: [feat] Added auto-create project dir + GitHub repo to gsd-t-init and gsd-t-init-scan-setup (v2.70.13). Configurable via ~/.claude/.gsd-t-config: projects_dir (base path for new projects) and github_org (org for repo creation). Skips entirely for existing projects.
- 2026-04-06 22:15: [feat] Hierarchical Build Order for design pipeline (v2.70.14). Root cause fix: decomposition creates element→widget→page hierarchy, but plan/execute ignored it — creating monolithic "build the page" tasks. Now: gsd-t-plan auto-generates tasks in Wave 1 (elements, one task per contract) → Wave 2 (widgets, import built elements) → Wave 3 (pages, import built widgets). Execute adds Design Hierarchy Build Rule in subagent prompt (no inline rebuilds, contract is authoritative over screenshots) + per-wave design verification checkpoints. Previous v2.70.10-12 enhancements (SVG overlay, box model, element count) remain as verification safety net, but primary enforcement moves from post-build verification to build-time structure.
- 2026-04-14 15:27: [milestone-planned] M35 plan expanded — 38 atomic tasks across 7 domains, 5 waves. Cross-domain dependency graph mapped in integration-points.md. REQ-069–078 added to docs/requirements.md. Wave sequence: Wave1(DR-T1/T2, MSA-T1, TT-T1/T2) → Wave2(DR-T3/T4, MSA-T2–T6, TT-T3–T6) → Wave3(RE-T1–T5, HAS-T1–T3) → Wave4(OB-T1–T4, HAS-T4–T5) → Wave5(DAT-T1–T8). Status: PLANNED. Ready for /user:gsd-t-impact.
- 2026-04-14 23:15: [wave-complete] M35 Wave 1 complete — 5/5 tasks done, tests 954/954 green (+13 from 941 baseline), token-budget v3.0.0 active, clean break in production. Wave 1 commits: (1) T1 atomic 7-file rewrite — bin/token-budget.js getDegradationActions three-band {band,pct,message}, WARN/STOP thresholds retuned to 70/85, scripts/context-meter/threshold.js BANDS collapsed, bin/gsd-t.js doStatus switch, test/token-budget.test.js boundary sweep, threshold.test.js updated, two context-meter e2e tests updated. (2) T2 token-budget-contract.md v3.0.0 — Non-Goals section, migration table, Option X rationale. (3) T3 model-selection-contract.md v1.0.0 — three-tier declarative phase map (17 canonical assignments), escalation hook pattern, /advisor fallback, dual-layer ANTHROPIC_MODEL + model: directive convention. (4) T4 token-telemetry-contract.md v1.0.0 — frozen 18-field JSONL schema, bash token-bracket shim, gsd-t metrics CLI contract. (5) T5 bin/token-telemetry.js skeleton + 9 tests — recordSpawn/readAll/aggregate with full schema validation. Red Team verdict: GRUDGING PASS (boundary math verified, no residual downgrade/conserve in live code outside comment blocks, contracts match implementation). Ready for Wave 2.
- 2026-04-15 02:30: [wave-complete] M35 Wave 4 complete — 6/6 atomic tasks, 985/985 tests green. Wave 4 closes the M35 execution surface with the retrospective optimization loop and the user-facing read-back path. Deliverables: (1) **HAS-T4** `bin/check-headless-sessions.js` — `checkCompletedSessions(projectDir)` returns unsurfaced completed entries sorted oldest-first; `markSurfaced(projectDir, id)` flips `surfaced: true` so the banner never repeats; `formatBanner(sessions)` renders the `## Headless runs since you left` block; `printBannerIfAny(projectDir)` is the CLI entry invoked by the two command files. Skips `-context.json` files, silently tolerates malformed JSON, returns `[]` when the sessions dir is missing. Wired into `commands/gsd-t-resume.md` (new Step 0.5 runs before load-full-state) and `commands/gsd-t-status.md` (new Step 0 runs before read-files). (2) **HAS-T5** `test/headless-auto-spawn.test.js` — 16 tests covering the full HAS surface: `makeSessionId` slug format + `gsd-t-` prefix stripping, `autoSpawnHeadless` return shape and running-session file, continue-here context snapshot write, `markSessionCompleted` running→completed transition + no-op on missing file, `checkCompletedSessions` filter (running/surfaced/oldest-first), empty-dir safety, malformed-skip, `-context.json` ignore, `markSurfaced` idempotency, `formatBanner` empty/success/exit-N shapes, non-darwin graceful degradation, plus an E2E shim-process smoke test that spawns `node -e "process.exit(0)"` and waits up to 10s for the watcher to flip status to completed. Qualitative no-clear-prompt assertion is documented in a closing comment block. (3) **OB-T1** `bin/token-optimizer.js` — `detectRecommendations({projectDir, lookbackMilestones: 3})` reads `.gsd-t/token-metrics.jsonl`, filters to the last N milestones, and runs 4 declarative detection rules: **demote** (opus phase ≥90% success + ≥3 volume → sonnet candidate), **escalate** (sonnet phase ≥30% failure + ≥5 volume → opus candidate), **runway-tune** (projected − actual > 15pt; no-op until schema extension, rule is wired for the moment the fields exist), **investigate** (per-phase p95 > 2× median + ≥10 volume). Each rule generates a fingerprint; rejected entries under active cooldown (rejection_cooldown > 0) are filtered out before detection returns. `appendToBacklog(recommendations, projectDir)` writes entries with a frozen H2 metadata shape; empty arrays still append a `## Complete-milestone review — no recommendations (M{N})` marker so the loop is visibly active. `parseBacklog(content)` round-trips entries; `setRecommendationStatus(content, id, updates)` rewrites metadata lines in-place. `DETECTION_RULES` + `REJECTION_COOLDOWN_MILESTONES` are exported for testing. Plus `.gsd-t/optimization-backlog.md` stub with format documentation. `test/token-optimizer.test.js` — 18 unit tests + 1 integration roundtrip (see OB-T4). (4) **OB-T2** `commands/gsd-t-optimization-apply.md` + `commands/gsd-t-optimization-reject.md` — both operate via `bin/token-optimizer.js` exports (readBacklog/parseBacklog/setRecommendationStatus/writeBacklog), both idempotent (already-promoted/already-rejected exits cleanly), both include observability logging into `.gsd-t/token-log.md` with `manual` model column (no subagent spawned). `reject` captures `--reason` text into the log + Decision Log entry (not the backlog body — keeps parseBacklog simple). Apply at autonomy level 3 auto-routes to `/user:gsd-t-quick` for demote/escalate/runway-tune and to `/user:gsd-t-backlog-promote` for investigate (scope unknown). Both set/check `rejection_cooldown: 5` on rejection. (5) **OB-T3** wired optimizer into `commands/gsd-t-complete-milestone.md` as new **Step 14** (non-blocking invocation wrapped in try/catch, logs "no new recommendations" or surfaces the count), added `--file` flag to `commands/gsd-t-backlog-list.md` (new Step 0 — when `--file optimization-backlog.md` is supplied, renders via `bin/token-optimizer.js` parseBacklog and optional `--status` filter, skips the default steps), added pending-count one-liner to `commands/gsd-t-status.md` as new Step 0.5 (appears after the HAS-T4 headless banner, suppressed when N=0), added help.md entries in the backlog section + detailed command-details section. (6) **OB-T4** integration roundtrip test extending `test/token-optimizer.test.js` — fixture of 10 opus/execute/success + 10 sonnet/debug/50%-failure records → detectRecommendations fires both demote + escalate → appendToBacklog writes both → setRecommendationStatus simulates apply (promoted) on demote + reject (5-cooldown) on escalate → re-detectRecommendations asserts the rejected escalate is filtered by cooldown. Files created: bin/check-headless-sessions.js, bin/token-optimizer.js, commands/gsd-t-optimization-apply.md, commands/gsd-t-optimization-reject.md, test/headless-auto-spawn.test.js, test/token-optimizer.test.js, .gsd-t/optimization-backlog.md. Files modified: commands/gsd-t-{resume,status,complete-milestone,backlog-list,help}.md, test/filesystem.test.js (command count 51→53, total 56→58). Test deltas: headless-auto-spawn 0→16, token-optimizer 0→19 (18 unit + 1 roundtrip), filesystem count bump. Wave 5 (docs-and-tests DAT-T1–T8) is the only remaining M35 work. No blockers. Ready for Wave 5.
- 2026-04-15 14:30: [fix] v3.10.12 — P0 context meter regression fix (M36 /compact incidents). **Root cause:** during M36 execution, user hit Claude Code's native `/compact` prompt multiple times despite M34's Context Meter being built to prevent this. Audit of `.gsd-t/.context-meter-state.json` revealed `checkCount=2102` with `pct=0` and `lastError: missing_key` forever — every single PostToolUse hook call had silently failed at the `ANTHROPIC_API_KEY` check and returned `{}` per the hook's fail-open invariant. `token-budget.getSessionStatus()` then fell through to the heuristic and reported `threshold: "normal"`, so the gate was **blind since installation** with no user-visible alarm at any layer. **Fix:** new `stale` band in `bin/token-budget.js` — when state file exists but is dead (`lastError` set, `timestamp` null, older than 5 min, or corrupt JSON), returns `{threshold: "stale", deadReason}` with one of 8 specific codes. All gated command files (`gsd-t-execute` Step 3.5, `gsd-t-wave` context gate, Step 7 per-domain re-check) now treat `stale` as exit-10 STOP but do NOT auto-spawn a fresh session (a fresh session would have the same broken guardrail). New `gsd-t-resume` Step 0.6 runs a context-meter health check immediately after the headless read-back banner, prints a prominent warning on `stale`, runs `gsd-t doctor` inline, and refuses to auto-advance into gated commands until fixed. `bin/gsd-t.js showStatusContextMeter()` promoted from dim ignorable whisper to red `✗ CONTEXT METER DEAD` alarm with actionable fix instructions, explicitly calling out "measurement only — inference stays on Claude Code subscription" when cause is `missing_key`. Contracts: `context-meter-contract.md` v1.0.0 → v1.1.0 (new §"Stale Band and Resume Gating", measurement-only Rule #3), `token-budget-contract.md` v3.0.0 → v3.1.0 (fourth band in threshold table). Tests: +2 new stale-band tests in `test/token-budget.test.js` (replaces the incorrect "stale falls through to heuristic" test that was asserting the old broken behavior); 1228/1228 pass. Audit process disproved 5 of the 6 hypotheses from the continue-here file and proved hypothesis #4 (silent hook fail on missing key) as the sole root cause — kept the fix minimal per Prime Directive #1.
- 2026-04-15 01:15: [wave-complete] M35 Wave 3 complete — 8/8 atomic tasks, tests 1043/1043 green (+32 from Wave 2's 1011 baseline). Wave 3 delivers the two foundational pieces that make M35's "no silent degradation" guarantee load-bearing: pre-flight runway projection (refuse-before-burn) and detached headless continuation (auto-spawn instead of prompting /clear). Together they close the loop so every long-running command can self-protect without ever asking the user to intervene. Deliverables: (1) **RE-T1** `bin/runway-estimator.js` — `estimateRunway({command, domain_type, remaining_tasks, projectDir, headlessAvailable})` → `{can_start, current_pct, projected_end_pct, confidence, confidence_basis, pct_per_task, recommendation, reason}`. Three-tier historical query: `{command, domain_type}` pair → `{command}` aggregate → constant fallback. Confidence grading high≥50, medium≥10, low<10. Conservative 1.25x skew at low confidence. Sonnet-default fallback 4%/task, opus-default (debug, integrate) 8%/task. Stop threshold mirrors token-budget v3.0.0 at 85%. Zero external deps. Never throws; missing `.gsd-t/.context-meter-state.json` → `current_pct=0` with stderr warning; missing `.gsd-t/token-metrics.jsonl` → empty + fallback path. (2) **RE-T2** `.gsd-t/contracts/runway-estimator-contract.md` v1.0.0 — frozen API, confidence grading rules, conservative skew policy, refusal-block format, handoff protocol to headless-auto-spawn, never-prompts-the-user guarantee, consumers list. (3) **RE-T3** `test/runway-estimator.test.js` — 26 unit tests covering empty/missing telemetry, tier fallback when pair<10 records, confidence boundaries (9/10/49/50/55), 1.25x skew at low / no skew at medium/high, proceed/headless/clear-and-resume recommendation paths, sonnet+opus constants, multi-task projection, malformed JSONL skip, negative pct delta coerced to 0, frozen decision-object shape, exported constants. (4) **HAS-T1+T2** `bin/headless-auto-spawn.js` — `autoSpawnHeadless({command, args, continue_from, projectDir, context})` → `{id, pid, logPath, timestamp}`. Uses `child_process.spawn` with `detached: true`, `stdio: ['ignore', logFd, logFd]`, and `child.unref()` so the interactive session never blocks on the child. Writes `.gsd-t/headless-sessions/{id}.json` (10-field schema: id, pid, logPath, startTimestamp, command, args, status, continueFromPath, surfaced, +endTimestamp/exitCode on completion) and `{id}-context.json` (continue-here snapshot pulling from progress.md). Poll-based completion watcher (2s interval, 1h cap, `timer.unref()`) uses `process.kill(pid, 0)` liveness probe, then `osascript -e 'display notification …'` on darwin with graceful no-op fallback on other platforms. T2 notification integration is delivered inline with T1 since the watcher and mac-notification live in the same module. Zero external deps. Helper exports: `makeSessionId`, `writeSessionFile`, `writeContinueHereFile`, `markSessionCompleted`. Plus `.gsd-t/contracts/headless-auto-spawn-contract.md` v1.0.0 (frozen API, session schema, notification channel, handoff protocol, never-prompts guarantee). `.gsd-t/headless-sessions/.gitkeep` created. (5) **RE-T4** Step 0 runway check wired into **5 command files** — `commands/gsd-t-execute.md`, `commands/gsd-t-wave.md`, `commands/gsd-t-integrate.md`, `commands/gsd-t-quick.md`, `commands/gsd-t-debug.md`. Each file gets a new `## Step 0: Runway Check` section before existing Step 0 work (existing Step 0 renamed to Step 0.1 where present — execute and wave had Context Gate Readiness, the others had Launch via Subagent). The Step 0 bash block inline-requires runway-estimator, prints JSON decision on success, and on refusal prints the ⛔ banner + calls `autoSpawnHeadless` + `process.exit(0)`. gsd-t-quick uses `remaining_tasks=1` (single-task), gsd-t-execute/wave/integrate substitute `{N}` at runtime, gsd-t-debug uses 1 and the mid-loop check re-runs per iteration. (6) **RE-T5 + HAS-T3** `commands/gsd-t-debug.md` gains `### Between-Iteration Runway Check (MANDATORY — every iteration)` after the 3-attempt-limit block. On refusal: reads `GSD_T_DEBUG_{HYPOTHESIS,LAST_FIX,LAST_TEST_OUTPUT,NEXT_ITERATION}` env vars, appends a `runway-handoff-snapshot` entry to `.gsd-t/debug-ledger.jsonl`, then calls `autoSpawnHeadless({command: 'gsd-t-debug', args: ['--resume', 'iteration-N+1'], continue_from: ledgerPath})`, then `process.exit(0)`. (7) **HAS-T3 test** `test/runway-debug-handoff.test.js` — 5 tests re-implementing the bash block in JS to assert on side-effects: state persistence (snapshot shape/fields), handoff call args shape, session file creation with status='running', clean no-throw exit, headless pickup path (continue_from points to existing ledger). (8) **RE-T5 smoke test** appended to `test/runway-estimator.test.js` — 80% CTX_PCT fixture, 5 remaining tasks, asserts `can_start=false`, `recommendation='headless'`, projected_end_pct=105 (80 + 4*5*1.25). Test deltas per file: runway-estimator 0→27 (26 unit + 1 smoke), runway-debug-handoff 0→5. Contracts added: runway-estimator-contract v1.0.0, headless-auto-spawn-contract v1.0.0. Files created: bin/runway-estimator.js, bin/headless-auto-spawn.js, test/runway-estimator.test.js, test/runway-debug-handoff.test.js, 2 contract files, .gsd-t/headless-sessions/.gitkeep. Files modified: commands/gsd-t-{execute,wave,integrate,quick,debug}.md (Step 0 insertion + debug.md between-iteration block). Wave 3 is the first wave that can self-protect — subsequent waves invoke the Step 0 runway check and will refuse rather than drain. Wave 4 (optimization-backlog + headless-auto-spawn T4/T5 + read-back banner) unblocked. No blockers. Ready for Wave 4.
