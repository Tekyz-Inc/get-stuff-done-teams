# Tasks: m36-cross-platform

Empty — tasks will be filled during `/user:gsd-t-plan`.

See `scope.md` for responsibility and `constraints.md` for rules. Contract: `.gsd-t/contracts/unattended-supervisor-contract.md` v1.0.0.
