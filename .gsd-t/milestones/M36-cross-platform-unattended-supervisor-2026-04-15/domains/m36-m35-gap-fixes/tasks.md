# Tasks: m36-m35-gap-fixes

Empty — tasks will be filled during `/user:gsd-t-plan`.

See `scope.md` for responsibility and `constraints.md` for rules. Contract: `.gsd-t/contracts/unattended-supervisor-contract.md` v1.0.0 + `headless-auto-spawn-contract.md` v1.0.0 (read-only).
