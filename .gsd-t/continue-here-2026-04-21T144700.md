# Continue Here — 2026-04-21 14:47

## Standing Directive

**Execute M43 (Token Attribution & Always-Headless Inversion) all the way to COMPLETED, then tag v3.17.10 and run `/gsd-t-version-update-all`. Do not stop or ask. Survive compaction.**

User authorized full unattended execution. Pause only for: VERIFY-FAILED, destructive actions, unrecoverable errors after debug-loop exit 4, or COMPLETED reached.

## Current Position

- **Milestone**: M43 — Token Attribution & Always-Headless Inversion (status: DEFINED)
- **Target version**: v3.17.10 (from current 3.16.10)
- **Domains** (6): m43-d1-in-session-usage-capture, m43-d2-per-tool-attribution, m43-d3-sink-unification-backfill, m43-d4-default-headless-inversion, m43-d5-compaction-pressure-circuit-breaker (renamed in body to "dialog-channel-meter"), m43-d6-transcript-viewer-primary-surface
- **Test baseline**: 1561/1561 pass

## Next Action

`/gsd-t-partition M43` — refresh the partition with the revised D4/D5/D6 scope (committed `ca1b588`).

## CRITICAL Constraints (read before partitioning)

1. **D4 MUST land in the FINAL wave, ALONE.** D4 deletes the spawn-mode branching from the very command files the orchestrator routes work through. If D4 lands mid-run, every subsequent spawn changes shape under the running orchestrator. This is documented in `.gsd-t/domains/m43-d4-default-headless-inversion/scope.md` § Dependencies. Override the original "Wave 2 parallel" placement.

2. **D5 has collapsed scope.** Originally a compaction-pressure circuit breaker; now a router-only dialog-channel growth meter. Pure warn, never refuses, never reroutes. No new module — small extension to `bin/runway-estimator.cjs`. No new contract — drops `compaction-pressure-contract.md` from scope. See `.gsd-t/domains/m43-d5-compaction-pressure-circuit-breaker/scope.md` for the full revised spec.

3. **Channel-separation invariant** (this is the core of M43 Part B): the in-session channel is reserved for human↔Claude dialog. All tool-using work spawns. The visualizer is the watching surface. No `--in-session` flag, no `--headless` flag, no low-water mark. Documented in `.gsd-t/progress.md` M43 def + decision-log entry 2026-04-21 14:43.

4. **D1 probe hook is wired** (this session, 2026-04-21 14:46). `~/.claude/settings.json` now invokes `~/.claude/scripts/gsd-t-in-session-probe.js` on Stop / SessionEnd / PostToolUse, writing payloads to `.gsd-t/.hook-probe/`. After a few real Stop events, inspect that directory: if `usage` is present in the payload → Branch A locked (hook-based capture); else Branch B (extend M42 transcript tee to interactive sessions).

5. **Project-hashed dashboard port** (`df34eb2`): `scripts/gsd-t-dashboard-server.js` now exports `projectScopedDefaultPort(projectDir)` returning a deterministic port in `[7433, 7532]`. D6 reuses this for auto-launch — do not reimplement.

6. **Supervisor PID fingerprint** (`df34eb2`): `bin/supervisor-pid-fingerprint.cjs` exports `writePidFile`/`readPidFile`/`verifyFingerprint`. Already wired into `bin/gsd-t-unattended.cjs` write site. Resume Step 0 uses the helper. Don't touch this code unless D4/D6 needs the helper signature changed.

## Recent Commits (do not revert)

- `ca1b588` — M43 scope revision (channel separation, doc-only)
- `df34eb2` — multi-project isolation (dashboard port + PID fingerprint)
- `82cc3b8` — transcript SSE waiting/empty/end semantics
- `cf77578` — M43 heartbeat watchdog (1h backstop, 5min stale)
- `f94857b` — M43 D1-T1 probe (already shipped, hook now wired)

## Multi-Step Chain (after M43 COMPLETED)

1. `/gsd-t-complete-milestone` (auto-invoked by verify)
2. `/gsd-t-version-update-all` to propagate v3.17.10 to registered projects
3. Stop. Done.

## Pre-Flight Status

- ✅ D4-last constraint added to scope
- ✅ Probe hook wired (Stop, SessionEnd, PostToolUse)
- ✅ `.gsd-t/.hook-probe/` directory exists
- ✅ Supervisor heartbeat watchdog at 5min stale + 1h backstop
- ✅ Tests baseline 1561/1561 green
- ✅ Prior supervisor state archived (`.gsd-t/.unattended/archive/state-2026-04-21T194417-stopped-iter4.json`)
- ✅ Stop sentinel removed
