Produced cold — only ./protocol.md and ./requirements.md were read before this document was written.

## Decided without you

| Seq | Setup / date | Action | Expected result | Effect on saved data | Source |
|---|---|---|---|---|---|
| F1-7 | Two entries exist for user U on 2026-09-01 and 2026-09-02, both 8h | A third entry of 9h is logged for U on 2026-09-01, taking that day to 17h | The overtime alert fires for 2026-09-01 only, and a single email is sent to U for that day | Third entry saved; no change to the two existing entries; the in-memory dedup Map gains one key for U+2026-09-01 | DECIDED-WITHOUT-YOU: F8 says "one per user per day" and SCAN-TEST-20 names the Map key as per-user-per-day, so I treated the dedup key as (user, date) rather than (user, calendar day the email was sent). The requirements never state which date the key uses. |
| F1-11 | Config "Max Hours Per Day" shows 12 in Settings and was never persisted (SCAN-FE-07 / SCAN2-DQ-04) | A 13h entry is logged for one day | Refused or warned per the configured max | none if refused; entry saved if only warned | DECIDED-WITHOUT-YOU: I treated the overtime threshold as the "Max Hours Per Day" config value because both appear in Settings and no other threshold source is named. The requirements never state what value overtime detection compares against, nor whether the unpersisted input feeds it. |
| F5-6 | A local account exists for e@x.com; the 2FA gate is active (SCAN-CLI-03 names a `/auth/login` → `/auth/verify-2fa` two-step) | Correct password POSTed to `/auth/login` | 200 with `twoFactorRequired`; NO session cookie set | none | DECIDED-WITHOUT-YOU: F5 lists "Local + Google OAuth login" and never mentions 2FA at all; only the CLI findings (SCAN-CLI-03, REQ-V125-R16) reveal that a 2FA gate ships server-side. I treated 2FA as active for all local logins because the CLI findings say the gate "defers session creation to `/auth/verify-2fa`" unconditionally. |
| F9-3 | Two live tokens are impossible per REQ-V125-R09; user U holds one live token | U mints a second token | Refused | none | DECIDED-WITHOUT-YOU: R09 says "one live token per person" but never names the refusal shape. I treated it as a refusal of the new mint rather than a silent replacement of the old one, because R14 says the value is shown once and a silent replace would revoke a token the user still believes is live. |
| F10-4 | A client column on the Rate Ledger has no override for user U; U is billable | Invoice revenue is resolved for U's hours on that client | A dash is rendered and the hours are counted into the visible "no rate set" total | none | DECIDED-WITHOUT-YOU: REQ-A10 states the dash and the "no rate set" total, but not whether the *billing rate* (non-client-specific) is used as a fallback when a client override is absent. I treated absence of a client override as "no rate set" (no fallback), because REQ-A7 describes the per-client dash as its own display state. |

## Table: F1 — Time Entry (REQ-TIME-*)

| Seq | Setup / date | Action | Expected result | Effect on saved data | Source |
|---|---|---|---|---|---|
| 1 | User U is assigned to project P; no entries exist; today is 2026-09-03 | U logs 4h on P dated 2026-09-03 with a 20-character description | Entry saved; `hourly_rate_snapshot` and role captured at insert | One new time-entry row; snapshot columns frozen at insert values | sourced: requirements.md F1 "Log time per day per project" (`server/src/index.ts:888`), "Snapshot rate + role at insert" (`index.ts:880`) |
| 2 | Continues from Seq 1 (one entry already saved for U on P, 2026-09-03) | U logs a SECOND entry, 3h, on the same project and same date | Both entries coexist; the day totals 7h | Second row saved; first row unchanged | GAP: F1 says "Log time per day per project" without stating whether a second entry for the same (user, project, date) is permitted, merged, or refused. No uniqueness constraint is named for `tb_time_entries` (contrast `tb_leave`'s explicit `UNIQUE(user_id,leave_date,half_day)` in REQ-V120-R01). |
| 3 | Two users U1 and U2 are both assigned to project P | U2 logs 4h on P on the same date U1 already logged 4h | Both entries saved independently; no interaction | Two rows, one per user; neither alters the other | sourced: requirements.md F1 (per-user entries; RBAC F6 gives each user their own entries) |
| 4 | U's live hourly rate is changed from 100 to 150 after Seq 1's entry was saved | The dashboard cost for Seq 1's entry is read | Cost must use the snapshot (100), not the live rate | Saved entry unchanged; only the displayed cost is at issue | sourced: requirements.md SCAN-DATA-05 (as-should-be: "must use `hourlyRateSnapshot`"), F1 "Dashboard cost uses snapshot (not live rate)" ❌ |
| 5 | Today is 2026-09-03 | U logs 4h dated 2026-09-04 (tomorrow) | Refused — future dates cannot be logged | none | sourced: requirements.md F1 "Cannot log future dates" ✅ (`index.ts:900-902`) |
| 6 | Today is 2026-09-03; the date field's visible text-entry path is used, not the native picker | U types 2026-09-04 directly into the date box and saves | Refused client-side at commit, not only on the server round-trip | none | sourced: requirements.md SCAN2-DQ-05 (as-should-be: guard "must apply to the visible text-entry path") |
| 7 | Today is 2026-09-03, 23:30 local, in a UTC-negative timezone | The time-entry panel is opened and the default date is read | The default date is the local date 2026-09-03, not the UTC date 2026-09-04 | none | sourced: requirements.md SCAN-FE-11, Non-Functional "Internationalization" (UTC-midnight split wrong in UTC-negative timezones) |
| 8 | U is assigned to project P | U logs an entry with a 9-character description | Refused — description must be at least 10 characters | none | sourced: requirements.md F1 "Description >= 10 characters" ✅ (`index.ts:904-906`) |
| 9 | U is assigned to project P | U logs an entry with a description of exactly 10 characters | Accepted — 10 is inside the allowed range | Entry saved | sourced: requirements.md F1 "Description >= 10 characters" (the `>=` states the boundary side explicitly) |
| 10 | U is assigned to project P | U logs 0 hours | Refused — hours must be greater than 0 | none | sourced: requirements.md F1 "Hours > 0" ✅ (`index.ts:907`) |
| 11 | U is assigned to project P | U logs exactly 24 hours in one day | Accepted — 24 is at the stated upper bound and inside it | Entry saved | GAP: SCAN-DATA-07 states the bound must be "max 24" but never says whether exactly 24 is inside or outside. E7 cannot be answered from the requirements. |
| 12 | U is assigned to project P | U logs 25 hours | Refused — above the upper bound | none | sourced: requirements.md SCAN-DATA-07 (as-should-be: "Hours field must have an upper bound (max 24)") |
| 13 | U is assigned to project P | U submits hours that parse to `Infinity` (e.g. the string "Infinity" or a value overflowing `parseFloat`) | Refused — `Infinity` must be rejected | none | sourced: requirements.md SCAN-DATA-07 (as-should-be: "must reject `NaN`/`Infinity`") |
| 14 | U is assigned to project P | U submits hours that parse to `NaN` | Refused | none | sourced: requirements.md SCAN-DATA-07 |
| 15 | U is assigned to project P; Stage is not "Proposal" | U saves an entry with no module, via the normal submit path | Refused — module is required unless Stage="Proposal" | none | sourced: requirements.md F1 "Module required unless Stage='Proposal'" |
| 16 | Same as Seq 15, but the save goes through the dirty-confirm dialog (`handlePanelSave` calls `validateForm()` directly) | U saves with no module and no activity type | Refused — `validateForm()` must explicitly check `activityType` and `module`, not rely on HTML5 `required` | none | sourced: requirements.md SCAN-FE-09 (as-should-be), F1 "bypassed via handlePanelSave path" |
| 17 | Stage is "Proposal" | U saves an entry with no module | Accepted — module is not required at this Stage | Entry saved with no module | sourced: requirements.md F1 "Module required unless Stage='Proposal'" |
| 18 | U has exactly one role on project P | The time-entry panel is opened for P | The designation auto-populates to that single role | none | sourced: requirements.md F1 "Designation auto-populates if single role on project" ✅ |
| 19 | U has two roles on project P | The time-entry panel is opened for P | Stated behavior for the multi-role case | none | GAP: F1 states auto-populate only for the single-role case; the requirements never say what happens with two or more roles (no selection, first alphabetically, last used). E1 (more than one of everything) has no answer. |
| 20 | A task's module value is entered as "Backend " with a trailing space | The entry is saved | The module is trimmed to "Backend" before save, matching `taskDescription`'s treatment | Saved row stores the trimmed value; the module picker does not fragment | sourced: requirements.md SCAN2-DQ-02 (as-should-be: "Task `module` value must be trimmed before save") |
| 21 | U is a MEMBER; user V is another user on the same shared project | U POSTs a time entry with `userId` = V | Refused — a MEMBER may only log their own time | none | sourced: requirements.md SCAN-RBAC-01 (as-should-be: "MEMBER must not be able to POST time entries for other users"), F1 "MEMBER can only log own time" |
| 22 | U is a MEMBER viewing the time-entry screen | U looks for a control to select another user as the entry's owner | No such control is offered to a MEMBER | none | GAP: the requirements state the MEMBER rule at the endpoint (SCAN-RBAC-01) but never state the screen-level rule — whether the user-picker is hidden, disabled, or locked to self for a MEMBER. E4's screen half is unstated. |
| 23 | U is an ADMIN; V is a user not assigned to project P | ADMIN POSTs a time entry for V on P | Accepted — ADMIN may create an entry for any user on any project regardless of assignment | Entry saved for V | sourced: requirements.md REQ-V11111-P02 (complete 2026-05-18) |
| 24 | U is not assigned to client C or project P | U selects C in the panel | A warn dialog appears: "You are not assigned to {Client}. Continue?" with Cancel and Continue | none until Continue is taken | sourced: requirements.md REQ-V121-R02 |
| 25 | Continues from Seq 24 — Continue was taken; U then selects unassigned project P | The identical warn dialog appears for the project | none until Continue is taken | none | sourced: requirements.md REQ-V121-R03, REQ-V121-R10 (one component parameterized by kind) |
| 26 | Continues from Seq 25; U is unassigned to both C and P | U saves with `crossAssignmentOverride: true` | Accepted — the server bypasses the scope check when the flag is set | Entry saved; an audit row is written naming the recipients | sourced: requirements.md REQ-V121-R04, REQ-V121-R07 |
| 27 | U is unassigned to project P | U POSTs the same entry with the override flag ABSENT | Refused — 403 or 400; the defense-in-depth scope check runs when the flag is absent | none | sourced: requirements.md REQ-V121-R04 ("without flag → 403/400") |
| 28 | Same as Seq 26, but the save goes through the dirty-confirm dialog path (`handlePanelSave`) | U saves an unassigned-project entry after a dirty-confirm | The override flag must still be computed and included | Entry saved with the override honored, not silently dropped | sourced: requirements.md SCAN-FE-01 (as-should-be: "`handlePanelSave` must compute and include `crossAssignmentOverride`") |
| 29 | Continues from Seq 26; PMs P1 (by project) and P2 (by client) exist, and P1 is ALSO an ADMIN | The cross-assignment notification audience is computed | The audience is (PMs by project) ∪ (PMs by client) ∪ (all ADMINs), deduplicated by `user.id`; P1 appears once | Audit row's `recipients` lists each id once | sourced: requirements.md REQ-V121-R05 |
| 30 | Continues from Seq 29; the mail transport fails for one recipient | The notification email is sent | Best-effort — the failure is logged and does NOT roll back the saved entry | The time entry stays saved; no rollback | sourced: requirements.md REQ-V121-R06, F8 "Cross-assignment notification email ✅ best-effort" |
| 31 | U has a FULL-day leave recorded for 2026-09-02 | U logs any hours on 2026-09-02 | A warning is shown; the entry is NOT blocked | Entry saved despite the warning | sourced: requirements.md REQ-V120-R05 (FULL-day leave + any hours → warn; non-blocking) |
| 32 | U has a half-day (AM) leave on 2026-09-02 and already has 3h logged that day | U logs a further 2h, taking the day to 5h | A warning is shown (half-day + total > 4h); the entry still saves | Second entry saved; first unchanged | sourced: requirements.md REQ-V120-R05 (half-day + total > 4h → warn) |
| 33 | U has a half-day leave on 2026-09-02 and 4h logged that day | U reads the guard outcome for a day totalling exactly 4h | No warning — the rule fires above 4h, so exactly 4h is inside the allowed range | none | sourced: requirements.md REQ-V120-R05 (the rule is stated as "total >4h", which places exactly 4h outside the warn condition) |
| 34 | Continues from Seq 31; U re-submits with the override flag | The server re-validates that the leave still exists (force-with-lease), emits `[NOTIFICATION] LEAVE-CONFLICT`, and persists the notifier flag | Entry saved; audit marker row persisted; no email delivery this milestone | Entry saved; a notifier flag row persisted; the leave row itself unchanged | sourced: requirements.md REQ-V120-R06 |
| 35 | Continues from Seq 34, but the leave was DELETED between the warning and the override re-submit | U re-submits with the override flag | The re-validation finds no leave; stated behavior for a stale override | Unstated — whether the entry is saved or refused is exactly what is missing | GAP: REQ-V120-R06 says the server "re-validates leave exists (force-with-lease)" but never states what happens when the re-validation fails — accept silently, refuse, or warn again. |
| 36 | An entry exists for U dated 2026-09-02 with 8h | The entry's date is changed to 2026-09-01 (an earlier date that already holds 8h) | Overtime and leave-guard checks must evaluate BOTH the original date and the new date | Existing 2026-09-02 row is updated to 2026-09-01; the other 2026-09-01 entry is unchanged; that day now totals 16h and must trigger the guard | sourced: requirements.md SCAN-DATA-06 (as-should-be: "must use original entry date when date is changed") |
| 37 | An entry exists for U dated 2026-09-01 with 8h; a second entry exists on 2026-09-02 | The 2026-09-01 entry is edited to the same date as the 2026-09-02 entry | Both entries now sit on 2026-09-02; stated behavior for the same-date collision | Unstated — whether the edited row replaces, merges with, or coexists alongside the existing 2026-09-02 row is what is missing | GAP: no same-date rule is stated for time entries (see Seq 2). E2's same-date-replace ordering has no answer for this table. |
| 38 | An entry was logged with `hourly_rate_snapshot` = 100; the entry's hours are then edited from 4 to 6 | The entry is saved | The snapshot rate stays 100 — the snapshot is frozen at insert, not re-derived on edit | Row's hours change; snapshot columns unchanged | sourced: requirements.md F1 "Snapshot rate + role at insert (immutable history)" |
| 39 | A task is marked `billable = false` (e.g. an internal meeting logged against a client project) | The Invoice Sheet total and the Dashboard cost/hours KPIs are read | Both must EXCLUDE the non-billable task's hours and cost | Saved rows unchanged; only the aggregates are at issue | sourced: requirements.md SCAN-DATA-11 / SCAN2-AUTHZ-01 (🔴 EXTREME, as-should-be: "must exclude tasks with `billable = false`") |
| 40 | A task on the internal project is created | The `billable` flag is read | It is forced false for the internal project (default is true elsewhere) | Row stores `billable = false` | sourced: requirements.md F1 "Per-task `billable` flag exists and is stored ✅ (defaults true; forced false for the internal project)" |
| 41 | Two tasks on one entry: one billable, one not | The entry's invoiced total is computed | Only the billable task's hours are summed | Rows unchanged | sourced: requirements.md SCAN-DATA-11 (per-task flag, so a mixed entry must split) |
| 42 | U is a MEMBER; the Dashboard is rendered | The Dashboard's entry set is inspected | It receives `visibleEntries` (RBAC-filtered), matching what TimeSheet receives | none | sourced: requirements.md SCAN-FE-19 (as-should-be) |
| 43 | U is authenticated; no rate limit is applied to the AI endpoint | U calls `POST /api/ai/improve` many times in quick succession | Refused past a rate limit, to prevent cost amplification | none | sourced: requirements.md SCAN-SEC-07 (as-should-be: "must have rate limiting"), F1 "AI endpoint must have rate limiting ❌" |
| 44 | U is NOT authenticated | U calls `POST /api/ai/improve` | Refused — `isAuth` is required | none | sourced: requirements.md SCAN-SEC-07 ("`isAuth` only" is the current state, i.e. auth is present) |
| 45 | The AI rate limit's exact allowance | A caller reaches the limit boundary exactly | Which request number is refused | none | GAP: SCAN-SEC-07 requires "rate limiting" but names no window or count (contrast login's explicit 5/IP/15min). E7's boundary cannot be stated. |
| 46 | U is a MEMBER; an entry belonging to another user V exists | U DELETEs V's entry | Refused — the handler special-cases MEMBER on `user_id` match | none | sourced: requirements.md SCAN2-AUTHZ-02 (current state names the MEMBER `user_id` check as present) |
| 47 | U is a MANAGER; an entry exists on a project/client entirely outside U's assignment | U DELETEs that entry | Refused — DELETE must scope MANAGER the same way GET does (own entries OR entries for assigned projects) | none | sourced: requirements.md SCAN2-AUTHZ-02 (🟠 HIGH, as-should-be) |
| 48 | U is a MANAGER; an entry exists on a project U IS assigned to, owned by another user | U DELETEs that entry | Accepted — inside the MANAGER scope predicate | Row deleted; audit row written | sourced: requirements.md SCAN2-AUTHZ-02 (as-should-be mirrors GET's "own entries OR entries for assigned projects") |
| 49 | U is a MANAGER on the timesheet screen, viewing an out-of-scope entry | U looks for the delete control on that row | The control is hidden or disabled for an out-of-scope row | none | GAP: SCAN2-AUTHZ-02 states the endpoint rule only. The screen-level half of E4 — whether the delete button is shown for an entry a MANAGER may not delete — is unstated. |
| 50 | Entries exist for a withdrawn/deleted project | A project with time entries is deleted | Refused — a project with time entries cannot be deleted | none | sourced: requirements.md F2 "Cannot delete project with time entries ✅" (`index.ts:1308-1311`) |

## Table: F2 / F3 — Project and Client Management (REQ-PROJ-*, REQ-CLIENT-*)

| Seq | Setup / date | Action | Expected result | Effect on saved data | Source |
|---|---|---|---|---|---|
| 51 | An ADMIN is signed in; client C exists | ADMIN creates project P with start 2026-09-01, end 2026-09-30, one module, one assignment | Accepted | New project row; assignment row; module link row | sourced: requirements.md F2 "Create / edit / delete project (ADMIN only per docs)" |
| 52 | Same as Seq 51 | Project created with end date 2026-09-01 and start date 2026-09-01 (equal) | Refused — end date must be strictly greater than start date, so equal dates fall outside | none | sourced: requirements.md F2 "End date > start date ✅" (`index.ts:1140–1142`) — the `>` states the boundary side |
| 53 | Same as Seq 51 | Project created with zero modules | Refused — at least one module required | none | sourced: requirements.md F2 "≥ 1 module ✅" (`index.ts:1145–1147`) |
| 54 | Same as Seq 51 | Project created with exactly one module | Accepted — one is inside the range | Project + one module link saved | sourced: requirements.md F2 "≥ 1 module" (the `≥` states the boundary side) |
| 55 | Same as Seq 51 | Project created with zero user assignments | Refused — at least one assignment required | none | sourced: requirements.md F2 "≥ 1 user assignment ✅" (`index.ts:1150–1152`) |
| 56 | A MANAGER is signed in | MANAGER creates a project | Accepted for an assigned client; MANAGER is auto-assigned to the project they create | Project row + an assignment row for the MANAGER | sourced: requirements.md F2 "Manager auto-assigned to projects they create ✅" (`index.ts:1165-1171`), F6 (MANAGER "Create/edit projects for assigned clients") |
| 57 | A MANAGER is signed in; project P belongs to a client the MANAGER is NOT assigned to | MANAGER edits P | Refused — MANAGER is restricted to assigned clients | none | sourced: requirements.md F3 "MANAGER/MEMBER restricted to `assignedClientIds`" (as-should-be; ❌ never queried, TD-001), F6 |
| 58 | A MANAGER is signed in | MANAGER DELETEs a project | Refused — MANAGER cannot delete projects | none | sourced: requirements.md F2 "MANAGER cannot delete projects ❌ not enforced (TD-001/TD-010)", F6 |
| 59 | A MANAGER is signed in, on the Projects screen | MANAGER looks for the project delete control | The control is hidden or disabled for a MANAGER | none | GAP: F2/F6 state the MANAGER delete rule as a server rule only. Whether the screen hides the delete button for a MANAGER is never stated. (Contrast SCAN2-AUTHZ-04, which does state the screen/server mismatch for the Team screen.) |
| 60 | A MEMBER is signed in | MEMBER creates a project | Refused — a MEMBER's documented scope is own time entries and own profile only | none | sourced: requirements.md F6 (MEMBER: "Own time entries; own profile; no financials"), TD-003 |
| 61 | The Internal project exists | Any role DELETEs the Internal project | Refused | none | sourced: requirements.md F2 "Cannot delete Internal project ✅" (`index.ts:1303-1305`) |
| 62 | The Internal client exists | Any role DELETEs the Internal client | Refused | none | sourced: requirements.md F3 "Cannot delete Internal client ✅" |
| 63 | Client C has two projects | C is deleted | Refused — a client with projects cannot be deleted | none | sourced: requirements.md F3 "Cannot delete client with projects ✅" |
| 64 | Client C has exactly one project, and that project has zero time entries | C is deleted | Refused — the project count is what blocks, and it is 1, not 0 | none | sourced: requirements.md F3 "Cannot delete client with projects" (any project count above zero blocks) |
| 65 | Client C is the ONLY client, and every project and every member routes through it | C's last project is deleted, then C is deleted | Stated refusal that prevents stranding the system with no client to register projects against | Unstated — whether the client row is deleted or retained is what is missing | GAP: E8's "last of a kind" case. The requirements name the Internal-client and has-projects refusals but never say whether the system must always retain at least one client (or whether Internal is guaranteed undeletable and therefore covers it). |
| 66 | A Super Admin exists; project P is created by another user | P is created | Super Admin is auto-assigned to all projects | An assignment row for the Super Admin | sourced: requirements.md F2 "Super Admin auto-assigned to all projects ✅" (`index.ts:1173-1182`) |
| 67 | Same as Seq 66, but v1.11.11 shipped | P is created | REQ-V11111-D05 removed super-admin auto-assign from POST/PUT /api/projects — no auto-assignment row | No auto-assignment row written | GAP:CONTRADICTION: F2 "Super Admin auto-assigned to all projects ✅ `index.ts:1173–1182, 1250–1259`" contradicts REQ-V11111-D05 "Super-admin auto-assign removed from POST/PUT /api/projects — complete 2026-05-18". Both are in this file and disagree about whether the row is written. |
| 68 | Project P has start 2026-10-01 (future), today is 2026-09-03 | P's status is read | Status auto-calculates to "Future" | none | sourced: requirements.md F2 "Project status auto-calc (Future/Open/Closed) ❌ not in schema; client-side only (TD-027)" |
| 69 | Project P's start date is exactly today, 2026-09-03 | P's status is read | Stated side of the boundary — "Future" or "Open" on the start date itself | none | GAP: F2 names three statuses but never states which side a date exactly ON the start (or end) date falls. E7 has no answer. |
| 70 | Project P is "Closed" (end date passed) | A time entry is logged against P dated after the end date | Stated behavior — accepted, warned, or refused | Unstated — whether a time-entry row is written against a Closed project is what is missing | GAP: the requirements never state whether a Closed project accepts new time entries, nor whether re-opening a Closed project is possible. E6 (way out of the state, and back in) is unanswered. |
| 71 | Project P exists with name "Alpha" | P is edited to have an empty name | Refused — name must be non-empty | none | sourced: requirements.md SCAN-FE-16 (as-should-be: "must validate `name` (non-empty)") |
| 72 | Project P is created with no `clientId` selected | Save is attempted | Refused with a validation error, not a 500 from an FK violation | none | sourced: requirements.md SCAN-FE-16 (as-should-be: "must validate ... `clientId` (selected)"; current state: "missing `clientId` causes 500 on FK violation") |
| 73 | Project P already has a sprint named "S1" | A second sprint named "S1" is added | Refused client-side AND server-side with a 400 and a user-facing explanation, not a generic 500 | none | sourced: requirements.md SCAN-FE-17 (as-should-be) |
| 74 | Project P has sprint S1, not Done | The sprint's Done toggle is clicked once | S1 is marked Done, `projectSnapshot` is updated, and Cancel afterwards does NOT raise the dirty dialog | Sprint row's completed state persisted | sourced: requirements.md SCAN-FE-14 (as-should-be) |
| 75 | Continues from Seq 74 | The same Done toggle is double-clicked rapidly | A per-row in-flight guard prevents the second PATCH; DB and frontend sprint status stay consistent | Exactly one state change persisted | sourced: requirements.md SCAN-FE-15 (as-should-be) |
| 76 | Sprint S1 is Done | The time-entry sprint picker is opened | Done sprints are hidden from the picker | none | sourced: requirements.md REQ-V110-R11 |
| 77 | Sprint S1 is Done | S1's Done toggle is clicked again (leaving the Done state) | S1 becomes not-Done and reappears in the time-entry picker | Sprint row's completed state cleared | GAP: REQ-V110-R11 documents entering Done and the picker consequence, but never states whether Done can be un-set, who may un-set it, or what happens to entries logged while it was hidden. E6's way-out is unstated. |
| 78 | Two clients exist, "Acme" (clientId `z9`) and "Zenith" (clientId `a1`) | The Projects grid "Client" column header is clicked to sort | Rows sort by the displayed client NAME (Acme before Zenith), not by the raw `clientId` | none | sourced: requirements.md SCAN2-DQ-03 (as-should-be) |
| 79 | The Edit Client panel was opened for client C1, its name edited, then closed; the panel is not remounted between opens | The panel is opened for client C2 | C2's real name is shown, not C1's previously-edited name | none until save; a save must not overwrite the wrong client's name | sourced: requirements.md SCAN-FE-03 / SCAN2-DQ-01 (as-should-be: "must be a controlled input (`value=`)") |
| 80 | The TimeSheet has filterClient = C1 and filterProject = P1 (a project of C1) | filterClient is changed to C2 | The project filter resets; the grid does not silently show zero rows with no feedback | none | sourced: requirements.md SCAN-FE-08 (as-should-be: "client filter change must reset the project filter") |

## Table: F5 / F6 — Authentication, Session, and RBAC (REQ-AUTH-*, REQ-ACCESS-*)

| Seq | Setup / date | Action | Expected result | Effect on saved data | Source |
|---|---|---|---|---|---|
| 81 | No account exists for e@x.com | A login is attempted with e@x.com and any password | Refused with a GENERIC message ("Invalid email or password.") that does not reveal whether the account exists | none | sourced: requirements.md SCAN-SEC-03 (as-should-be: "must be generic ... to prevent user enumeration") |
| 82 | An account exists for e@x.com and is Google-federated (no local password) | A local login is attempted with e@x.com | Refused with the SAME generic message as Seq 81 — the auth method must not be revealed | none | sourced: requirements.md SCAN-SEC-03 (current state names "This account uses Google Login." as a leak) |
| 83 | An account exists for e@x.com with a local password | A login succeeds | The response is a safe DTO — `password_hash`, `reset_token`, `reset_token_expires`, `auth_id` are absent | Session created (24h maxAge) | sourced: requirements.md SCAN-SEC-02 (as-should-be: "must return a safe DTO, not raw DB rows"; `/auth/me` named as the model), F5 "Session 24h ✅" |
| 84 | No caller is authenticated | `GET /api/auth/debug` is called | Refused — the route requires authentication | none | sourced: requirements.md SCAN2-SEC-01 (🔴 EXTREME, as-should-be: "must require authentication and strip sensitive columns") |
| 85 | A caller IS authenticated | `GET /api/auth/debug` is called | Answered, but with `password_hash`, `reset_token`, `reset_token_expires`, `auth_id` stripped | none | sourced: requirements.md SCAN-SEC-01 / SCAN2-SEC-01 (as-should-be names both halves: auth required AND columns stripped) |
| 86 | An unauthenticated visitor is on the login screen | The visitor looks for the "Troubleshoot Auth" button | No public UI renders that endpoint's raw response | none | sourced: requirements.md SCAN2-SEC-01 (as-should-be: "no public UI should render its raw response"; `components/Login.tsx:54-62,586-597`) |
| 87 | A password of exactly 8 characters is submitted at registration | The account is created | Accepted — 8 is inside the allowed range | New user row | sourced: requirements.md F5 "Password >= 8 chars ✅" (the `>=` states the boundary side) |
| 88 | A password of 7 characters is submitted | Registration is attempted | Refused | none | sourced: requirements.md F5 "Password >= 8 chars" |
| 89 | An account already exists for e@x.com | `POST /api/auth/register` is called with e@x.com | Refused with a GENERIC response that does not confirm the address is registered nor that it is Google-federated | none | sourced: requirements.md SCAN-SEC-04 (as-should-be: "must return generic response on duplicate email") |
| 90 | Registration has no rate limiter today | Registration is called repeatedly from one IP | Refused past a rate limit | none | GAP: SCAN-SEC-04 requires rate limiting on `POST /api/auth/register` but names no allowance or window (contrast login's explicit 5/IP/15min and reset's 3/email/1h). E7's boundary and the refusal threshold are unstated. |
| 91 | 4 failed logins have been made from one IP inside a 15-minute window | A 5th login attempt is made from that IP | Accepted (attempt processed) — the limit is 5 per IP per 15 minutes, so the 5th is inside the allowance | none | sourced: requirements.md F5 / Technical Requirements "Login: 5 attempts per IP per 15-minute window" (`rate-limit.ts:56`) |
| 92 | Continues from Seq 91 — 5 attempts already consumed inside the window | A 6th login attempt is made from that IP | Refused with 429 | none | sourced: requirements.md Technical Requirements "Login: 5 attempts per IP per 15-minute window", REQ-V109-10 ("production-path 429 still fires") |
| 93 | The caller's user-agent is Playwright and the host is a NON-production host | 6 login attempts are made | The limiter exempts the Playwright UA on non-production hosts, so the 6th is not refused | none | sourced: requirements.md Technical Requirements "Playwright UA bypass active on non-production hosts only (TD-045)", REQ-V109-10 |
| 94 | The caller's user-agent is Playwright and the host IS production (`timesheet.tekyz.com`) | 6 login attempts are made | Refused with 429 — the bypass does not apply on production | none | sourced: requirements.md REQ-V109-10 ("Cloud Run prod + custom domain unaffected"), Technical Requirements (bypass "on non-production hosts only") |

## HALT — case-space bound reached at case 94

Case 94 is the bound stated in the protocol (`test-plan-first-contract.md`). The requirements
area is NOT finished. Enumeration stopped here rather than narrowing scope silently.

**What was enumerated:** F1 Time Entry (E1–E8 applied in full, Seq 1–50); F2/F3 Project and
Client Management (Seq 51–80); F5/F6 Authentication, Session and RBAC — partially, covering
login-error genericity, the debug endpoint, password and rate-limit boundaries only (Seq 81–94).

**What was left un-enumerated, and roughly how many further cases each implies:**

| Region left out | Rules not yet applied | Estimated further cases |
|---|---|---|
| F5/F6 remainder — session expiry at exactly 24h, reset token at exactly 1h, setup token at exactly 24h, `reset_token_expires` TIMESTAMPTZ timezone comparison (SCAN-DATA-08), the two-step 2FA flow, Google-OAuth auto-promotion to ADMIN (SCAN2-AUTHZ-03, incl. the "sole admin later deleted" re-entry path and the concurrent-first-login race), weak cookie key fallback (TD-007), SCHEMA_SQL auto-recovery on error code 42P01 vs substring match (SCAN-SEC-06), CORS allowed-origin enumeration | E1, E2, E4, E6, E7, E8 | ~35 |
| F6 RBAC permission matrix in full — 3 roles × 2 surfaces (screen + endpoint) × every guarded resource (users, clients, projects, time entries, leave, meta/settings, config, admin tools, reports), plus the stated screen/server divergence on the Team screen (SCAN2-AUTHZ-04) and the rate-hiding rule (REQ-V11111-P01) checked on EVERY returned row, not just the first (SCAN2-TEST-05) | E4 (the doubling rule), E1, E8 | ~70 |
| F4 Team / User Management — cannot delete self, cannot delete the last ADMIN (E8's last-of-a-kind), email uniqueness, welcome email + 24h setup token, first-Google-user-becomes-ADMIN, ADMIN-only user creation (TD-002), email verification before activation (TD-023), multiple designations per member (V110-R07), hourly cost vs hourly rate (V110-R08), member deactivation and reactivation (v1.27, E6 both directions) | E1–E8 | ~45 |
| F7 Audit — every CREATE/UPDATE/DELETE on each key table, JSONB before/after, sensitive-column stripping before storage (SCAN-SEC-05), audit rows for password resets (TD-034), the "audit log is sacrosanct" rule vs the legacy bulk-delete path that deletes audit rows (SCAN2-SEC-02, a contradiction to record), agent-token writes committing audit inside the transaction (REQ-V125-R06) vs human writes committing after (REQ-V125-R07) | E3, E5, E8 | ~30 |
| F8 Notifications — overtime alert dedup across a restart (SCAN-TEST-20), one-per-user-per-day boundary, cross-assignment email dedup, leave-conflict notifier flag | E1, E2, E7 | ~15 |
| v1.20 Leave Tracking — `UNIQUE(user_id,leave_date,half_day)` collisions (E1/E2 same-date-replace), AM vs PM half-day pairing on one date, the four leave types, approval columns left nullable (E6: no approval path documented), `ON DELETE RESTRICT` on `user_id` (E8), the changed-vacation audit highlight, calendar load cancellation (SCAN-FE-02), non-409 save/delete error surfacing (SCAN-FE-12/13), enum validation returning 400 (SCAN-FE-18), `x-test-mode` propagation to `is_test` (SCAN-DATA-04) | E1–E8 | ~50 |
| v1.20.11 / SCAN2-SEC-02 Delete Test Data — the preview/confirm/typed-"DELETE" flow, FK-safe child→parent order in one transaction, `tb_firms` omitted from the legacy path, `tb_leave` omitted from `DELETE /api/test-cleanup` (SCAN-API-04), and the two-bulk-delete-paths contradiction | E3, E4, E8 | ~25 |
| v1.25 Agent API Tokens — mint/one-live-token/revoke (E6 both directions), Bearer-token auth resolving to the same `req.user`, ownership NOT widened by an ADMIN's role, DELETE always refused for agents, case-SENSITIVE comparison (the documented exception to the project default), `TT_API_TOKEN` overriding a saved session, the stop-with-neither rule, and the un-run migration 18 | E1–E8 | ~40 |
| v1.27 Rate Ledger + Deactivation — dated rate resolution, back-dated rate insert-before, replacing an unstarted future rate, the last-rate-for-kind refusal, never-billed greying, per-client override absence, Show Inactive local state, deactivation and reactivation | E2 (all three orderings), E6, E7, E8 | ~45 |
| CLI (`tt`) — login 2FA two-step, `--google` one-time phrase (expires in two minutes: E7), wrapped response unwrapping, required `activityType`, `tt last` per-task description, session file mode 0600, `tt report` ranges | E1–E8 | ~35 |
| Invoice Sheet — CSV formula-injection neutralization of `=`/`+`/`-`/`@` (SCAN2-SEC-03), revenue resolved server-side with no client arithmetic (REQ-A10) | E7, E8 | ~12 |
| Cross-cutting infrastructure — transaction safety on the four multi-statement blocks (SCAN-DATA-01), migration fail-fast preventing startup on a partial schema (SCAN-DATA-02), fresh-boot vs upgraded schema divergence (SCAN-DATA-03), `clientScopeFilter`/`idColumn` allowlist validation (SCAN-SEC-08/09), concurrent `fetchData()` deduplication (SCAN-FE-05), double-submit guard on ConfirmModal (SCAN-FE-06), Settings config fetch base URL (SCAN-FE-04), Max Hours Per Day persistence (SCAN-FE-07) | E3, E5, E8 | ~40 |

**Estimated further cases implied by the un-enumerated region: ~440.**

Handing back as un-finished, in the manner of `blocked-needs-human`: the regions above are
named, not guessed past. No subset of them has been selected and presented as a complete plan.
