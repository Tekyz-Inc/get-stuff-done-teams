# Progress Archive 020

**Date range**: 2026-04-21 → 2026-04-22
**Entries**: 20
**Generated**: 2026-04-23 by archive-progress.js

---

## Decision Log

- 2026-04-21 13:50: [quick] Transcript SSE route — added `event: status` (waiting/empty) and `event: end` (finished spawn) semantics + 4 unit tests. `scripts/gsd-t-dashboard-server.js` `handleTranscriptStream` now: (1) on missing `.gsd-t/transcripts/{spawn-id}.ndjson` emits `event: status\ndata:{"status":"waiting",...}` and holds the stream open with keepalive (no fs.watchFile since the file doesn't exist); (2) on 0-byte file emits `event: status\ndata:{"status":"empty",...}` then still attaches fs.watchFile so tail engages when the producer starts writing; (3) reads `.gsd-t/transcripts/.index.json` via new `readIndexEntry(projectDir, spawnId)` helper and, if `status` is `done`/`failed`/`stopped`, replays all lines + emits `event: end\ndata:{"status":"<s>","endedAt":"<iso>"}` + `res.end()` (no tail). Running-spawn path preserves existing replay + byte-offset fs.watchFile dedupe. `test/m42-transcript-sse-replay.test.js` (new, 4/4 pass) covers all four branches via real http.createServer + tmpdir fixture; full suite 1547/1547 pass (baseline 1543 + 4). Producer-side zero-byte bug in `bin/gsd-t-unattended.cjs` (post-hoc tee without `--output-format stream-json`) is explicitly out of scope for this quick and untouched per parent-agent directive.

- 2026-04-21 13:30: [quick] Liveness heartbeat watchdog for unattended supervisor (M43 out-of-band — shipped as a focused quick because M43-D1 probe work uncovered the 270s wall-clock guillotine was too aggressive for legitimate long-running iters). **bin/gsd-t-unattended-heartbeat.cjs** (new, ~180 lines, zero deps): pure `checkHeartbeat({projectDir, workerStartedAt, staleHeartbeatMs, now, fsShim})` staleness checker against `.gsd-t/events/YYYY-MM-DD.jsonl` mtime — grace window for fresh workers (events file not yet created, absent-but-under-threshold = healthy), `max(mtime, workerStartedAt)` reference prevents killing on first poll when events file is carried over from prior iter. Exports `eventsPathFor`, `DEFAULT_HEARTBEAT_POLL_MS = 60_000`, `DEFAULT_STALE_HEARTBEAT_MS = 300_000`. **bin/gsd-t-unattended-platform.cjs**: `spawnWorker(cwd, timeout, opts)` now accepts `onHeartbeatCheck` + `heartbeatPollMs` + `onHeartbeatSample` + `_spawnImpl` — when `onHeartbeatCheck` is provided, returns a Promise that resolves with `{staleHeartbeat, heartbeatReason, ...}` (SIGTERM on stale via `setInterval`); legacy spawnSync path unchanged when no heartbeat wiring. **bin/gsd-t-unattended.cjs**: `CONTRACT_VERSION` `1.1.0` → `1.4.0` (catching up from long-stale constant to match contract v1.3.1→v1.4.0 bump); `DEFAULT_WORKER_TIMEOUT_MS` `270_000` → `3_600_000` (absolute backstop only); new `DEFAULT_STALE_HEARTBEAT_MS` + `DEFAULT_HEARTBEAT_POLL_MS` constants exported; `doUnattended` + `runMainLoop` converted to `async`; config merge picks up `staleHeartbeatMs`; main loop wires `onHeartbeatCheck` into `spawnWorker` + awaits result when Promise, maps `res.staleHeartbeat===true` → `exitCode=125` + `lastExitReason="stale_heartbeat"` (heartbeat beats wall-clock on ties), writes `[stale_heartbeat]` marker to run.log parallel to existing `[worker_timeout]` marker, emits retry event with `reason:"stale_heartbeat"`. `_spawnWorker` factored into sync-or-async via `finalize()` helper so callers pass-through untouched when no heartbeat wiring. **bin/gsd-t-unattended-safety.cjs**: DEFAULTS `workerTimeoutMs` 270_000 → 3_600_000 + added `staleHeartbeatMs: 300_000`; `cloneDefaults` includes new key. **.gsd-t/.unattended/config.json**: `workerTimeoutMs` 270_000 → 3_600_000, added `staleHeartbeatMs: 300_000`. **test/m43-heartbeat-watchdog.test.js** (new, 14/14 pass): 8 `checkHeartbeat` pure tests (healthy, stale, grace, absent-post-grace, carried-over events file, invalid inputs, `eventsPathFor` path shape, default constants); 5 `platform.spawnWorker` heartbeat path tests (Promise return, kills on stale + SIGTERM, clean exit, legacy sync fallthrough, spawn error surfacing); 1 supervisor main loop test asserting `state.lastExit===125` + `state.lastExitReason==="stale_heartbeat"` via captured cap-check hook. **test/unattended-cache-warm-pacing.test.js** rewritten — asserts new 1h backstop, 5min stale, 1min poll defaults + ≤5s inter-iter sleep invariant (§16 cache-pacing retained) + rationale comment mentions "heartbeat" or "backstop". **test/unattended-supervisor.test.js** + **test/unattended-triple-fix-v3-13-11.test.js**: 24 `it(…)` blocks that called `sup.doUnattended(…)` synchronously converted to `async () =>` + `await sup.doUnattended(…)` via systematic bulk patch (async conversion required because `runMainLoop` now awaits the heartbeat-polling spawn Promise). **.gsd-t/contracts/unattended-supervisor-contract.md** v1.3.1 → v1.4.0: added §16a "Heartbeat Watchdog" (design, grace window, poll cadence, kill path, run-log diagnostic, config table, state.json fields, testability), §16 renamed "Cache-Warm Pacing (v1.3.0, superseded-as-primary by §16a in v1.4.0)" — invariant retained as inter-iter sleep guard, §5 exit table adds code 125 stale-heartbeat + rewords 124 as worker-timeout absolute backstop + notes `lastExitReason` field, §6 CLI adds `--stale-heartbeat-ms` flag and retitles `--worker-timeout` default to 3_600_000, §13 config.json example updated, version history row added. Full suite: **1543/1543 pass** (was 1540 before patches, +3 net — added 14 heartbeat tests, preserved 29 M42 tests through the sync-vs-Promise `_spawnWorker` refactor). Per user directive: **PAUSE after heartbeat is done — DO NOT relaunch supervisor**. Supervisor was not restarted; users ready to relaunch `gsd-t unattended` will pick up the new 1h backstop + 5min heartbeat automatically (config already on disk).

- 2026-04-21 12:56: [execute M43 D1-T1] In-session usage capture probe installed. **scripts/hooks/gsd-t-in-session-probe.js** (new, ~60 lines, zero deps, silent-fail): reads Claude Code hook stdin JSON (Stop / SessionEnd / PostToolUse), writes raw payload to `.gsd-t/.hook-probe/{event}-{ts}-{sid}.json` when that directory exists (creating the dir is the on/off switch); rotates to MAX_PER_EVENT=10 files per event; blocks path traversal by resolving paths back inside the probe dir; 1MB stdin cap; never throws (never interferes with Claude Code). Copied to `~/.claude/scripts/gsd-t-in-session-probe.js` for cross-session installability. **test/m43-in-session-usage-probe.test.js** (new, 5/5 pass): happy path captures usage-bearing payload, off-switch (no dir = no writes), malformed-stdin no-op, cwd-traversal guard, MAX_PER_EVENT=10 rotation. Suite: **1525/1527 pass** (2 pre-existing fails in `watch-progress-writer.test.js` are env-leak from `GSD_T_CLAUDE_AGENT_ID=supervisor-iter-3` set by unattended worker — the test asserts `/^shell-\\d+-\\d+\\.json$/` expecting no env-injected id; bug is in the test, not this commit). **Branch-decision gate pending**: settings.json write to mount the probe hook was blocked by permissions (both `~/.claude/settings.json` and `.claude/settings.json` denied in this unattended iteration). Next worker (or interactive Claude) must add the Stop + SessionEnd + PostToolUse handler entries — probe script is already in place at `~/.claude/scripts/gsd-t-in-session-probe.js` and `.gsd-t/.hook-probe/` exists and has been verified with a fabricated Stop payload (`.gsd-t/.hook-probe/Stop-2026-04-21T19-55-18-366Z-probe-test-0.json` contains the expected usage fields). Once the hook is wired and one real Stop fires, D1-T1 Branch A/B decision is direct inspection of that first probe artifact: **if `usage` is present → Branch A locked (hook-based)**, else Branch B (interactive transcript tee extension of M42 D1 infrastructure). Per worker-iteration budget (270s), landing the probe + tests + decision-gate documentation here lets the next worker ship the branch commit quickly.

- 2026-04-21 12:30: [milestone] M43 Token Attribution & Headless-Default Inversion DEFINED — target v3.17.10, 6 domains across 2 themes. **Part A — Universal Token Attribution**: D1 per-turn in-session usage capture (hook-based if Claude Code exposes `usage` on Stop/SessionEnd, else extend M42 transcript tee to interactive sessions; writes `.gsd-t/metrics/token-usage.jsonl` with `turn_id`+`session_id`); D2 per-tool attribution (`bin/gsd-t-tool-attribution.cjs` joins events stream + token-usage JSONL by `turn_id`, attributes turn usage across tool_calls by output-byte ratio; ships `gsd-t tool-cost --group-by tool|command|domain`); D3 sink unification + backfill (canonical schema v2, single producer path from M36 heartbeat + M40 aggregator + D1 hook + D2 joiner, `.gsd-t/token-log.md` becomes regenerated view via `gsd-t tokens --regenerate-log`, backfill existing 64 Tokens=0 rows from `.gsd-t/events/*.jsonl` + `.gsd-t/headless-*.log` — M41 D3 backfiller existed but was never run against historical log). **Part B — Default-Headless Inversion**: D4 flip ~14 command files (execute/wave/integrate/quick/debug/verify/complete-milestone/test-sync/scan/gap-analysis/populate/feature/project/partition) from "headless only over context band" to "headless unless `--in-session` or session-context below ~15% low-water mark"; D5 compaction-pressure circuit breaker extending `bin/runway-estimator.cjs` with turn-over-turn usage trajectory signal predicting next `/compact` within N turns, trips in-session commands to refuse-and-reroute-to-headless; D6 transcript viewer (M42 `:7433`) as primary surface — router prints dashboard URL hint at every spawn, `--in-session` mode prints live transcript URL. Rationale: A measures the cost, B reduces it — they ship together so the inversion can be validated against attributed data immediately rather than guessed at. Motivated by bee-poc compaction pathology (agent `b94ca63f`, 3 compactions in one workday, interval collapsed 51min → 11min) showing Bash-heavy + Read-heavy in-session workloads need both visibility AND default-headless. Contracts touched: `metrics-schema-contract.md` v1→v2 (adds turn_id, session_id, tool_attribution[], compaction_pressure), `stream-json-sink-contract.md` v1.1.0→v1.2.0 (formalize in-session hook entry-point), new `tool-attribution-contract.md` (output-byte ratio algorithm + tie-breaker + missing-data handling), `headless-default-contract.md` v1.0.0→v2.0.0 (flip default + `--in-session` opt-out + low-water mark). Success criteria: (1) every spawn writes per-turn row with usage parsed never 0/N/A; (2) `gsd-t tool-cost --group-by tool` shows non-zero attribution for Bash/Read/Edit/Grep/Task over last 30d after backfill; (3) `.gsd-t/token-log.md` byte-identical on delete+regenerate; (4) default `/gsd-t-execute|wave|quick` spawns headless, `--in-session` round-trips through router; (5) circuit breaker fires on synthetic high-growth fixture and reroutes without silent `/compact`. Assessment: COMPLEX (6 domains, 2 themes). Recommended flow: `partition` → `discuss` (for D1 hook-vs-tee branch + D5 compaction-pressure heuristic) → `plan` → `execute` (waves) → `integrate` → `verify` → `complete-milestone` → tag v3.17.10. Sources combined per user direction "Make M43 and M44 one milestone" — previously scoped separately as M43 (Universal Token Attribution, 3 domains) and M44 (Default-Headless Inversion, 3 domains) in `.gsd-t/continue-here-2026-04-21T121612.md`. Not yet partitioned.

- 2026-04-21 17:26: [verify M43] final gate passed — all 6 domains shipped across 3 waves (W1: D1+D3, W2: D2+D5+D6 parallel, W3: D4 alone). Full test suite 1708/1710 (2 pre-existing unrelated fails: buildEventStreamEntry, writer_shim_safe_empty_agent_id_auto_mints_id). Commits: a1ec5ce (D3), 6d40e1c+811722a (D1), c4ce6b4 (D2), 1a48f9f (D5), 01f4534 (D6), 172d5b2 (D4). Channel-separation invariant locked via shouldSpawnHeadless=()=>true + headless-default-contract v2.0.0. Ready for complete-milestone → v3.17.10.

- 2026-04-21 17:27: [complete-milestone M43] COMPLETED — v3.16.12 → v3.17.10 (minor bump, completed feature milestone). All 6 domains shipped, channel-separation invariant locked, per-tool attribution available via `gsd-t tool-cost`. package.json bumped. Ready for tag v3.17.10.

- 2026-04-22 23:00: [execute M44-D7 · unattended Wave 1] M44 D7 per-CW attribution complete — 5/5 tasks shipped in sequence (T1 metrics-schema v2.1.0 + compaction-events v1.1.0 contract bumps `7c3e571` → T2 cw_id pass-through in `bin/gsd-t-token-capture.cjs` `3d784ed` → T3 calibration hook `scripts/gsd-t-calibration-hook.js` `64301ed` → T4 unit test suite `test/m44-cw-attribution.test.js` 19/19 isolated → T5 doc-ripple this commit). **Token-capture wrapper** — `recordSpawnRow` and `captureSpawn` now accept optional `cw_id` opt; `_buildJsonlRecord` serializes it only when supplied (`if (cw_id != null && cw_id !== '') rec.cw_id = String(cw_id);` — same pattern as the existing `session_id` / `turn_id` branches, so pre-D7 callers produce byte-identical rows). 15/15 existing `m41-token-capture.test.js` tests still pass with no modifications. **Calibration hook** — new SessionStart handler that runs alongside (not replacing) `scripts/gsd-t-compact-detector.js`. On `payload.source === "compact"` it reads `.gsd-t/.unattended/state.json`, derives `spawn_id` (sessionId fallback chain), `cw_id` (= spawn_id for unattended workers), `task_id` (activeTask / task_id / currentTask fallback chain), `estimatedCwPct` (D6's pre-spawn prediction if recorded), `cwCeilingTokens` (defaults to 200000), then derives `actualCwPct` from `payload.input_tokens` (or nested `compactMetadata.preTokens`) divided by the ceiling, clamped to [0, 2.0]. Appends one `{type: "compaction_post_spawn", schemaVersion: 1, ts, cw_id, task_id, spawn_id, estimatedCwPct, actualCwPct}` row to `.gsd-t/metrics/compactions.jsonl`. **Silent no-op** when payload isn't a compact source, when `.gsd-t/` is missing, when `state.json` is missing/unparseable/`status !== "running"`, when no `spawn_id` derivable, when no `input_tokens` derivable, or when `cwd` is non-absolute. Always exits 0 (throwing breaks Claude Code SessionStart). Zero ext deps, 1 MiB stdin cap, path-traversal guard mirroring the detector. **Coexistence verified by test**: pre-existing v1.0.0 detector rows (`source: "compact"`, no `type` field) remain intact when calibration row appended; consumers distinguish via `type` field (`"compaction_post_spawn"` vs absent). **Contracts bumped** in this domain (per constraints — must land in D7's commit, not Wave 2): `metrics-schema-contract.md` v2 → v2.1.0 (adds optional `cw_id` field with derivation rules — unattended: `cw_id == spawn_id`; in-session: `session_id + ":" + compaction_index` per the calibration hook's compact-fire counter), `compaction-events-contract.md` v1.0.0 → v1.1.0 (adds `compaction_post_spawn` event type, calibration hook lifecycle, guardrails, three-hook wiring matrix). **Test suite**: `test/m44-cw-attribution.test.js` — 19 tests, all green isolated: cw_id present → field serialized, cw_id numeric → coerced to string, cw_id absent/null/empty → field OMITTED (not null, not ""), captureSpawn forwards cw_id, `_buildJsonlRecord` cw_id branch matches session_id pattern, calibration hook with active spawn → calibration row written with correct task_id + estimatedCwPct + actualCwPct ~0.97 for 194000/200000, fallback to compactMetadata.preTokens, cwCeilingTokens override (95000/100000 = 0.95), no state.json → no row written, status !== running → no-op, malformed json → silent, source !== compact → no-op, missing input_tokens → no-op, empty/non-json stdin → no-op, non-absolute cwd → no-op, calibration row appends alongside v1.0.0 detector rows in same sink, zero ext deps. **Hard constraints all met** (per `.gsd-t/domains/m44-d7-per-cw-attribution/constraints.md`): backward-compatible cw_id (existing callers unchanged, absent ≠ null), silent no-op when no active spawn, zero ext deps, calibration event includes BOTH estimatedCwPct AND actualCwPct, contract bumps in D7's commit, `scripts/gsd-t-compact-detector.js` NOT modified. **Out of scope** (deferred): backfilling pre-D7 token-usage.jsonl rows with cw_id (525 historical rows; D6 falls back to per-iter median); economics logic (D6); parallel dispatch (D2); CW boundary detection algorithm beyond the spawn_id == cw_id rule. **D1 ran in parallel** (also Wave 1 Foundation per partition map: D1 + D7 both no-dep). Files: `bin/gsd-t-token-capture.cjs` (cw_id opt + serialization branch), `scripts/gsd-t-calibration-hook.js` (new, 254 lines), `.gsd-t/contracts/metrics-schema-contract.md` (v2.1.0 §added), `.gsd-t/contracts/compaction-events-contract.md` (v1.1.0 §added), `test/m44-cw-attribution.test.js` (new, 19 tests), `docs/requirements.md` (+§"M44 Per-CW Attribution" 6 REQs), `docs/architecture.md` (+§"Per-CW Attribution (M44 D7)" with diagram, hook lifecycle, contract pointers), this entry.

- 2026-04-22 16:30: [execute M44-D1 · unattended Wave 1] M44 D1 task-graph reader complete — 5/5 tasks shipped in sequence (T1 contract skeleton + module scaffold `26979df` → T2 real parser + cycle detection + scope.md fallback `57076d6` → T3 unit test suite 22/22 pass `b799e83` → T4 `gsd-t graph --output json|table` CLI `f5ee283` → T5 doc-ripple this commit). New `bin/gsd-t-task-graph.cjs` (305 lines, zero ext deps): `buildTaskGraph({projectDir})` returns `{nodes, edges, ready, byId, warnings}` after parsing every `.gsd-t/domains/*/tasks.md`; node schema `{id, domain, wave, title, status, deps, touches}`; iterative three-color DFS cycle detection throws `TaskGraphCycleError` with `.cycle: string[]`; touch-list resolution prefers task `**Touches**` / `**Files touched**`, falls back to `scope.md ## Files Owned`, falls back to `[]` + warning; status markers `[ ]/[x]/[-]/[!]` + unknown → pending + warning; only `done` deps satisfy dependents (skipped/failed do NOT). New contract `.gsd-t/contracts/task-graph-contract.md` v0.1.0 (T1 skeleton) → v1.0.0 (T2 final, downstream M44 domains may now begin implementation). New test suite `test/m44-task-graph.test.js` — 22/22 pass in 51 ms isolated, covering single-domain, multi-domain cross-domain, 3-task ring + self-loop cycle, ready-mask done/skipped/failed/unknown-dep semantics, scope.md fallback, [] fallback + warning, unknown status marker warning, parseDepList/parseFileList edge cases, missing domains dir, getReadyTasks null-safety, perf (50 domains × 5 tasks = 250 nodes parses in ~6 ms vs 200 ms budget), live-repo smoke. CLI surface in `bin/gsd-t.js doGraph`: `--output json` and `--output table` (id/domain/wave/status/ready/deps); also explicit `gsd-t graph tasks [json|table]`; pre-existing `graph index|status|query` (codebase entity graph) untouched. Live verified on this repo: 33 tasks · 36 edges · 2 ready (M44-D1-T1 + M44-D7-T1, the two no-dep Wave-1 foundation tasks) · 0 warnings · 3 ms parse. Doc-ripple landed: `docs/requirements.md` §"M44 Task-Graph Reader" with 7 REQs (REQ-M44-D1-01..07); `docs/architecture.md` §"Task-Graph Reader (M44 D1)" with consumer diagram, public API, hard rules, CLI surface. **Hard constraints all met**: zero ext deps (Node built-ins only), cycle detection mandatory + non-recoverable, read-only at all times, mode-agnostic (no in-session vs unattended awareness), perf < 200 ms (3 ms live / 6 ms synthetic), contract gate (v1.0.0 locks schema for D2/D4/D5/D6 to start). D7 running in parallel (Wave 1 Foundation per partition map: D1 + D7 both no-dep). Files: `bin/gsd-t-task-graph.cjs` (new), `test/m44-task-graph.test.js` (new), `.gsd-t/contracts/task-graph-contract.md` (new), `bin/gsd-t.js` (+graph subcommand routing), `docs/requirements.md` (+§M44 Task-Graph Reader), `docs/architecture.md` (+§Task-Graph Reader (M44 D1)), `.gsd-t/progress.md` (this entry).

- 2026-04-22 22:31: [partition M44 · unattended iter 1] M44 partitioned into 7 domains across 3 waves (Foundation: D1+D7 → Gates: D4+D5+D6 → Integration: D2→D3). Mode-aware gating math layered on M40 orchestrator (built-on, not replaced). Shared-file conflict map identifies orchestrator.js + orchestrator-config.cjs as D2/D6 split, command files as D3-owned, token-capture.cjs as D7 cw_id pass-through. Integration points: post-Wave-2 D6 estimator vs D7-tagged real slice + D4+D5 synthetic 2-task fixture; post-Wave-3 in-session multi-domain smoke (assert ≤T/2 + zero pause prompts) + unattended 5-iter chain over multi-task milestone (assert zero compactions in compactions.jsonl). Pre-reqs all landed (Q1/Q2a/Q2b/Q3, optimization report, adaptive maxParallel). No known blockers. Calibration corpus: 525 turn rows + 72 compaction events. Next: /gsd-t-plan for M44 to populate per-domain tasks.md task bodies (or proceed straight to /gsd-t-execute Wave 1 if tasks.md already concrete).

- 2026-04-22 23:55: [quick] transcript renderer — per-frame arrival timestamp pill. User feedback during M44 unattended run: *"Each message and tool call should include a time stamp so I can easily see from the stream if something got stuck."* Added a small monospace HH:MM:SS pill (with ISO `title` tooltip) prepended to every frame in `scripts/gsd-t-transcript.html`. Single chokepoint: `appendFrame(el, arrivedAt)` now inserts a `<span class="ts">` as the first child of every frame div. SSE `onmessage` captures `arrivedAt = new Date()` once per message and threads it through `renderFrame` → all 9 per-type render helpers (`renderSystem`, `renderUserMessage`, `renderAssistantText`, `renderThinking`, `renderToolUse`, `renderToolResult`, `renderBoundary`, `renderRaw`, `renderCompactMarker`). Stuck-stream detection is now visual: two adjacent frames with identical HH:MM:SS = nothing arrived between them; a long gap before a new timestamp = the stream paused. Tests: `test/m44-transcript-timestamp.test.js` — 7 asserts (CSS rule present + appendFrame signature + fmtTs/pad2 helpers + SSE captures + renderFrame threading + script syntactically valid + sandboxed-eval unit test of `fmtTs(Date)` → `"09:05:07"`). All 14 transcript renderer tests pass (7 new + 7 prior). Renderer-only change — no contract, domain, or server impact.

- 2026-04-22 23:30: [quick] supervisor self-healing — stream-json spawn args + JSON-or-integer pid parser. Two bugs surfaced during the M44 unattended launch: (1) supervisor's explicit `args:` to `claude -p` omitted `--output-format stream-json --verbose`, so the worker buffered all output until exit and the M42 transcript NDJSON stayed at 0 bytes mid-iter; (2) `supervisor.pid` is now JSON `{pid, projectDir, startedAt}` (per `bin/supervisor-pid-fingerprint.cjs writePidFile`) but three command-file readers parsed it with `parseInt(...trim(), 10)` — getting `NaN`, treating the supervisor as dead while it was running fine. Fix scope: spawn-args added to both the supervisor's explicit args (`bin/gsd-t-unattended.cjs:1381`) and the platform default fallback (`bin/gsd-t-unattended-platform.cjs:154`); PID readers in `commands/gsd-t-unattended-watch.md` (Step 2 PID branch), `commands/gsd-t-unattended.md` (Step 1b pre-flight + Step 3 liveness loop) now require the canonical `readPidFile()` from `bin/supervisor-pid-fingerprint.cjs` which transparently handles both JSON and legacy bare-integer forms. `commands/gsd-t-resume.md` already used `readPidFile()` — no change needed. Two new regression tests: `test/m44-supervisor-stream-json-args.test.js` (3 asserts: explicit-args site + default fallback both contain `--output-format stream-json --verbose --dangerously-skip-permissions`) and `test/m44-pid-parser-canonical.test.js` (6 asserts across three command files: no `parseInt(readFileSync(PID_FILE))` pattern + each uses `readPidFile`; plus a unit test that `readPidFile` returns `{form:"json"}` and `{form:"legacy"}` correctly). Full suite **1767/1769** — exactly the prior 1758/1760 baseline plus 9 new tests. Same 2 unrelated pre-existing failures (flaky dashboard port test + M43-progress-md hardcoded expectation now stale post-M44 promotion). This commit is shipped per the user's standing self-healing directive: *"if you see a problem and you've identified a fix for it, just stop unattended, implement your fix, and restart it without asking me."* Files changed: `bin/gsd-t-unattended.cjs`, `bin/gsd-t-unattended-platform.cjs`, `commands/gsd-t-unattended-watch.md`, `commands/gsd-t-unattended.md`, `test/m44-supervisor-stream-json-args.test.js` (new), `test/m44-pid-parser-canonical.test.js` (new). Next: relaunch `/gsd-t-unattended M44`.

- 2026-04-22 22:50: [bootstrap] Promoted M44 from backlog #14 to `## Current Milestone` (status IN PROGRESS, target v3.18.10) ahead of `/gsd-t-unattended` launch. Status header flipped from `M43 COMPLETED` to `M44 IN PROGRESS`. Added M44 row to Milestones table above the M43 COMPLETED row. M43 reference DEFINED block dropped from Current Milestone (full M43 record preserved in the Completed Milestones table at line 70). Body summary lifted from backlog #14 (mode contracts NON-NEGOTIABLE, three invariants, two delivery layers, pre-reqs landed status, calibration corpus pointers, 7 proposed domains, success criteria, recommended flow → `partition` → `discuss` → `plan` → `execute` → `integrate` → `verify` → `complete-milestone` → tag v3.18.10). Bootstrap done because progress.md showed M43 COMPLETED but no successor; supervisor's resume worker keys off `## Current Milestone` so the supervisor would have had no target.

- 2026-04-22 22:30: [quick] Q2b — compact_marker frame in transcript NDJSON + visualizer timeline badge. Extended `scripts/gsd-t-compact-detector.js` with a second write path: after appending the compactions.jsonl row, the hook now also appends a `{type:"compact_marker", ts, source, session_id, prior_session_id, trigger?, preTokens?, postTokens?}` frame to the most-recently-modified `.ndjson` in `<cwd>/.gsd-t/transcripts/`. Active transcript discovery (`findActiveTranscript`) reads the transcripts dir, filters `*.ndjson`, picks the file with the highest mtime — no-ops silently when the dir is absent or empty. Identical guardrails as the Q2a write: path-traversal guard (resolved transcript path must stay under `.gsd-t/transcripts/`), off-switch (no `.gsd-t/` → silent no-op), fail-open (transcript append failure swallowed so compactions.jsonl write always completes). Updated `scripts/gsd-t-transcript.html`: new CSS `.frame.compact-marker` (orange/yellow horizontal rule with centered badge), new `renderCompactMarker(frame)` function (exports as `window.__gsdtRenderCompactMarker`), badge label "⚡ CW boundary", tooltip = `ts + trigger + prior_session_id + session_id + preTokens + postTokens` — only fields present in the frame. `renderFrame()` dispatches `type === "compact_marker"` before all other frame types. New `test/m44-compact-marker-frame.test.js` — **11/11 pass**: schema required fields; optional fields included/omitted correctly; appends without overwriting prior content; most-recently-modified transcript chosen; no-op when `.gsd-t/` absent; no-op when transcripts dir absent; no-op when dir has no `.ndjson`; hook exits 0 even when transcript write fails (compactions.jsonl still written); renderer exports `renderCompactMarker`; badge has `cm-badge` class + "CW boundary" text + tooltip with trigger/tokens; no throw on legacy frame. Full suite **1758/1760** — same 2 pre-existing unrelated fails. Files changed: `scripts/gsd-t-compact-detector.js` (+76 lines: `findActiveTranscript` + `writeTranscriptMarker` + inline call), `scripts/gsd-t-transcript.html` (+39 lines: CSS + `renderCompactMarker` + `renderFrame` dispatch), `test/m44-compact-marker-frame.test.js` (new, 235 lines).

- 2026-04-22 22:15: [backlog] M44 entry added to `.gsd-t/backlog.md` (#14) with mode-aware framing — **in-session and unattended are equally first-class** delivery targets, not primary + secondary. The continue-here file from earlier in the session claimed M44 was already in the backlog — it was not; grep came back empty. Now landed. Objectives per mode: [in-session] speed + minimize-compaction (hard rule: NEVER throw interactive pause/resume prompts); [unattended] zero compaction across autonomous M1→M10 run. Three pre-existing invariants (file-disjointness, 100% auto-merges, pre-spawn economics) apply to both modes identically — they're correctness, not mode. Mode-specific gating math: [in-session] orchestrator-CW headroom (worker summaries land in user-facing CW; bound fan-out width AND result-envelope size) vs [unattended] per-worker-CW headroom (supervisor is Node process with no CW; bind per-`claude -p` worker CW ceiling). Two delivery layers (L1: parallel `claude -p` worker spawns, L2: parallel tasks within one worker) are mode-agnostic. **Token-cost honesty in the entry**: parallel is approximately token-neutral in steady state (total input/output same, wall-clock is the primary win; `max` instead of `sum`), and token-positive only in the case where sequential would have compacted (compaction-induced re-input drops to zero). Rate-limit pressure is explicitly NOT a con — hitting the 5-hour ceiling because 20× more work got done is the goal. 7 proposed domains (D1 task-graph reader, D2 `gsd-t parallel` CLI, D3 command-file integration, D4 dep-graph validation, D5 file-disjointness prover, D6 pre-spawn economics + cost estimator consuming the 525-row calibration corpus, D7 per-CW attribution + compaction-event integration). Pre-reqs already landed this session: Q1 token-log regen (`7eefd2c`), Q2a compaction detector + scanner (`940e5a8` + `f7de324`), Q3 turn→tool join (`8f4588b`), optimization report (`b5edff2`), adaptive maxParallel (`969462a`). Companion memory `feedback_parallelism_two_modes.md` written; prior `feedback_unattended_overnight_only.md` superseded — time-of-day framing retired, mode is now chosen by *intent* (interrupt-ability vs zero-touch autonomy) not clock. User framing that drove this: "In session, there's two clear objectives, speed and reduce compaction as much as is possible" + "Unattended must run M1 → M10 end-to-end with zero human involvement and zero compaction. This is critical, but not for all builds. Many I will want to run in session because I don't have all the requirements defined or I want to be able to interrupt." Success criteria: [in-session] T/2 wall-clock reduction with N workers and zero pause prompts; [unattended] zero compaction events logged during a measured autonomous chain (baseline: 72 compactions over 18 days of mixed-mode work from Q2a backfill).

- 2026-04-22 21:50: [quick] token-usage optimization report generator — `gsd-t report tokens [--date YYYY-MM-DD] [--out PATH]` writes a markdown report at `.gsd-t/reports/token-usage-{DATE}.md` organized per the canonical 5-level hierarchy `Run → Iter → CW → Turn → Tool`, with CW (context window) as the primary optimization unit (peer of Iter, not a partition of it). New `bin/gsd-t-report-tokens.cjs` (430 lines) exports `generateReport`, `groupIntoCWs`, `rollupCW`, `topNExpensiveTurns`, `groupCompactionEvents`, `renderMarkdown`. CW grouping rule: each distinct `session_id` in `.gsd-t/metrics/token-usage.jsonl` = one CW; compactions in `.gsd-t/metrics/compactions.jsonl` whose `prior_session_id` matches a CW mark `endedBy: compaction (auto|manual)` (manual when `trigger=manual`, auto otherwise; missing trigger defaults to auto with a footnote dagger). CWs without a following compaction get `iter end` (if a later CW exists) or `run end` (if it's the last in the data). Live + backfill compaction rows are deduped by `(ts, session_id, prior_session_id)` with live `source=compact` winning over `compact-backfill` when both exist. Report sections: **A** per-CW rollup (PRIMARY) — `CW# | Iter | Start | Turns | In | Out | CacheR | CacheC | Avg Out/turn | Peak Ctx% | Ended-by` with totals + average-turns-per-CW + compaction rate; **B** tool-tokens rollup via `bin/gsd-t-tool-attribution.cjs::joinTurnsAndEvents` + `aggregateByTool` — B.1 by-tool table + B.2 tool×command cross-tab (top 10 tools × top 5 commands by total tokens); **C** top-20 expensive turns sorted by `input + output` descending (input-heavy = context bloat, output-heavy = generation cost — both columns visible); **D** all compaction events with `ts | source | iter | trigger | preTokens | postTokens | durationMs | active command | active domain/task` enriched with the last turn's command/domain/task before the compaction `ts`. **No cost columns anywhere** (Max subscription = $0); **idempotent** (same date overwrites same path; output deterministic modulo the `Generated:` ISO line); **never crashes on missing data** (missing `compactions.jsonl` → "no compaction events recorded"; failed tool attribution → "Tool attribution unavailable"). CLI dispatch: new `case "report"` block in `bin/gsd-t.js` introduces nested-subcommand pattern (`gsd-t report tokens …`); help text bumped. Tests: new `test/m44-report-tokens.test.js` — **13/13 pass**: groupIntoCWs (0/2 compactions, live+backfill dedup, missing-trigger flag), rollupCW (sums + avg + peak + null ctxPct), topNExpensiveTurns (≥N + <N), groupCompactionEvents activity enrichment, generateReport end-to-end (4 sections + correct CW count + run-end + missing-compactions survival + idempotence). Full suite **1748/1749 pass** — 1 fail is pre-existing unrelated (`m43-milestone-complete-detection` asserts on live `progress.md` content — stale fixture). **Live report on this repo** (`.gsd-t/reports/token-usage-2026-04-22.md`): 1 CW with all 523 turn rows of the current session_id, top tool `no-tool` (285 of 286 attributed turns landed there because most turns have zero matched tool events) followed by Bash/Edit/Read/ScheduleWakeup. Section D shows the full 72-event compaction history (61 auto + 11 manual, all backfill) — the historical record M44's pre-req scanner produced. **Surprise**: only 1 CW in the per-CW rollup for the entire dataset because every token-usage row shares one session_id (`a5ee3b8e-1a77`); the 72 compactions are all _ancestor_ events (their `prior_session_id` doesn't match any in our turn set), so they show only in Section D and not as CW boundaries. This is correct given the data shape (all 523 rows landed in one Stop-hook capture window). V2 deferred: roll up across `.gsd-t/transcripts/s-*.ndjson` `result` envelopes for run-level (cross-session) totals; HTML/dashboard widget; per-CW tool breakdown sub-table. Red Team skipped per quick spec — V1 analyst tool, not user-facing critical path. Files: `bin/gsd-t-report-tokens.cjs` (new, 430 lines), `bin/gsd-t.js` (+59 lines: report dispatch + help line), `test/m44-report-tokens.test.js` (new, 280 lines), `.gsd-t/reports/token-usage-2026-04-22.md` (live output, 156 lines).

- 2026-04-22 21:30: [quick] orchestrator default `--max-parallel` is now adaptive — `floor(freemem / 2GB)` clamped to floor 3, ceiling 15. Reverts the static `10` from `67c520a` which was calibrated for the maintainer's high-RAM Mac and would risk OOM on smaller machines (10 × ~1.5–2 GB resident per `claude -p` worker). Static `DEFAULTS.maxParallel` drops back to `3` (the safe floor). New `computeAdaptiveMaxParallel(freeBytes)` in `bin/gsd-t-orchestrator-config.cjs` is invoked when no project `orchestrator.config.json` sets `maxParallel`, no `--max-parallel` flag, and no `GSD_T_MAX_PARALLEL` env. Precedence chain: `default(3) < adaptive(floor(free/2GB), [3..15]) < config-file < CLI < env`. New `loadConfig` return field `maxParallelSource` exposes which level decided ('adaptive' | 'config-file' | 'cli' | 'env') for observability. Help text updated; wave-join contract updated to "default: adaptive — floor(freemem / 2GB) clamped to floor 3 / ceiling 15". Tests: 4 new in `test/m40-orchestrator-config.test.js` covering adaptive math at 0/1/4/8/16/20/64 GB free + NaN guard, plus a regression test asserting `DEFAULTS.maxParallel === 3` (the safe floor). All 15/15 config tests + 58/58 across the full M40 orchestrator suite pass. Existing `.gsd-t/orchestrator.config.json` overrides still take precedence — power users (incl. this maintainer) can pin `maxParallel: 10` per-project without touching defaults.

- 2026-04-22 20:25: [quick] compaction historical scanner from ~/.claude/projects/ (M44 pre-req, part 2 of 2). New `scripts/gsd-t-compaction-scanner.js` scans every `.jsonl` under `~/.claude/projects/<slug>/` (slug derived from `projectDir` — `/` → `-`, matching Claude Code's encoding), filters rows where `type === "system" && subtype === "compact_boundary"`, converts them to canonical compaction rows with `source: "compact-backfill"`, dedups against existing `.gsd-t/metrics/compactions.jsonl` on `(ts, session_id)`, sorts by ts ascending, and — only when `--write` is passed — appends. **Default is DRY-RUN**; the `--write` flag is mandatory for mutation. Optional flags: `--project-dir DIR`, `--sessions-root DIR` (override), `--limit N` (diagnostic cap), `-h/--help`. Exports `run`, `scanSessionFile`, `boundaryToRow`, `deriveSessionsRoot`, `loadExistingKeys`, `rowKey` for testability. Row preserves optional fields from `compactMetadata`: `trigger` (auto|manual), `preTokens`, `postTokens`, `durationMs` — but older Claude Code archives do not consistently emit `postTokens`, so missing optional fields are simply omitted rather than guessed (documented in contract as a consumer invariant). Backfill rows use `logicalParentUuid` as the `prior_session_id` — it's the last pre-compact message UUID, the closest stable boundary anchor the archive exposes for in-place compactions (documented explicitly). 4 new tests (13/13 pass total across M44 test file): scanner dry-run no-writes, scanner --write + dedup on re-run, missing sessions root exits cleanly, slug derivation matches Claude Code convention. **Live backfill run**: 112 GSD-T session files scanned, **72 historical compaction boundaries** extracted spanning 2026-04-04 through 2026-04-21, written to `.gsd-t/metrics/compactions.jsonl`. This is the first persistent record of how many times Claude Code auto-compacted mid-run in this repo — 72 events over 18 days confirms the M43 debrief observation that CW transitions were the dominant invisible boundary in the token-accounting pipeline. Contract `.gsd-t/contracts/compaction-events-contract.md` updated with the 72-event empirical note + the `prior_session_id`-on-backfill semantic.

- 2026-04-22 20:10: [quick] compaction detector hook + contract + tests (M44 pre-req). New `scripts/gsd-t-compact-detector.js` is a `SessionStart` hook that reads the stdin JSON payload, and — only when `source === "compact"` — appends one NDJSON row to `<cwd>/.gsd-t/metrics/compactions.jsonl`. startup / resume are no-ops. Fail-open contract: exits 0 on every code path (throwing here would break Claude Code session startup). 1 MiB stdin cap + path-traversal guard (resolved output path must stay under `<cwd>/.gsd-t/metrics/`) + off-switch (no `<cwd>/.gsd-t/` → silent no-op; the hook never creates that directory — opt-in). Row schema v1: `{ts, schemaVersion, session_id, prior_session_id, source, cwd, hook}`; `source` is `"compact"` for live captures and reserved `"compact-backfill"` for the historical scanner (landing in a follow-up commit). Wired into `~/.claude/settings.json` as a fourth `SessionStart` matcher alongside the existing `gsd-t-update-check.js` and `gsd-t-heartbeat.js` hooks (neither touched); reads the installed package via `$(npm root -g)/@tekyzinc/gsd-t/scripts/gsd-t-compact-detector.js` with a presence guard so sessions in non-GSD-T projects short-circuit silently. New contract `.gsd-t/contracts/compaction-events-contract.md` v1.0.0: rationale (CW boundaries invisible today → Run→Iter→CW→Turn→Tool hierarchy silently collapses), row schema, hook lifecycle, guardrails, backfill semantics, wiring pointer. New `test/m44-compaction-detector.test.js` — 9 tests all pass: source=compact writes row; source=startup no-op; source=resume no-op; malformed stdin no-throw; 1 MiB cap triggers abort; path-traversal cwd rejected (relative paths + fallback), off-switch holds when no `.gsd-t/`; empty stdin still exits 0; three appends → three rows (append-only, no overwrite). Named `m44-*` because M44 (Generalized Cross-Domain Parallel Task Orchestrator) consumes these rows for CW-scoped attribution. Scanner (historical backfill from `~/.claude/projects/<slug>/*.jsonl`) lands in a sibling commit.

- 2026-04-22 18:55: [quick] fix D2 tool attribution join — add `turn_id` to `tool_call` events. The existing `bin/gsd-t-tool-attribution.cjs` joiner bucketed 285 of 286 turns into `no-tool` because it relied on a timestamp-window matcher against the row `ts` field, and D1 writes every turn row with the same `ts` (the Stop-hook fire instant — 523 rows landed in a 5-second window even though the real turns spanned ~an hour). Fix: `scripts/gsd-t-heartbeat.js::buildEventStreamEntry` now calls `resolveTurnIdFromTranscript(transcript_path, tool_use_id)` on PostToolUse and stamps `turn_id` + `tool_use_id` onto the emitted `tool_call` event (transcript tail-scan, ~0ms in practice). `bin/gsd-t-tool-attribution.cjs::joinTurnsAndEvents` prefers the direct `(session_id, turn_id)` join when events carry `turn_id`; events without it (pre-v3.17.11) still bind via the original timestamp-window matcher (back-compat, documented as a migration gap). Synthetic verification on the 2026-04-21 dataset (enriching the 458 real tool_uses with their transcript-derived `turn_id`s) shows `no-tool` drops from 285/286 → 20/286 and Bash turns go from 1 → 180, Read 1 → 37, Edit 1 → 30 — realistic attribution. New test file `test/m43-tool-attribution-join.test.js` (9 tests: direct-join bypasses timestamps, aggregation spreads across tools, back-compat window matcher still works for unflagged events, unknown turn_id falls through, `resolveTurnIdFromTranscript` happy/sad paths, `buildEventStreamEntry` PostToolUse carries both new fields incl. null-transcript safety). All 31 existing attribution tests pass. Contract `.gsd-t/contracts/event-schema-contract.md` extended with an "Event-type-specific optional fields" section documenting `tool_call.turn_id` + `tool_use_id` (additive since v3.17.11) and the legacy-events migration gap. Full suite 1726/1728 (same 2 pre-existing unrelated fails: dashboard-autostart port race, M43 milestone-complete fixture drift against live progress.md).

- 2026-04-22 12:08: [quick] regenerate token-log.md from D1 source-of-truth `.gsd-t/metrics/token-usage.jsonl` (523 rows) via `node bin/gsd-t.js tokens --regenerate-log` — first run against the post-M43 dataset. Old 12-column variant (Notes before Tokens + extra Compacted column) replaced with the canonical 11-column header (`| Datetime-start | ... | Tokens | Notes | Domain | Task | Ctx% |`) via `bin/gsd-t-token-regenerate-log.cjs`'s rewrite-from-jsonl path. 523 rows now render with `in=N out=N cr=N cc=N $X.XX` — the in-session D1 rows correctly show `—` for cost because the hook stores `costUSD: null` (assistant-frame envelopes don't carry cost; only result-frame envelopes do). Two legacy 10-column supervisor rows (2026-04-21 23:31 + 23:42, `gsd-t-resume supervisor-iter-1`) were NOT in the jsonl, so the regenerate wiped them; re-appended post-regen: row #1 (exit-4 426s) → Tokens `—` (no transcript survives; all 11 `.gsd-t/transcripts/*.ndjson` pre-date the 23:31 run), row #2 (exit-1 2800s, the 47-min M43 worker) → backfilled `in=44 out=27346 cr=2929058 cc=149739 $30.98` from `result` frame in `.gsd-t/transcripts/s-8b329c31.ndjson`. Final: **525 data rows** (523 regen + 2 appended); **524 with canonical `in=...` cells**; **1 dashed** (row #1 per spec — em-dash, not `N/A` or `0`). Token tests: **70/70 pass** (`test/m41-token-*.test.js` + `test/m40-token-aggregator.test.js` + `test/m43-schema-v2.test.js` + `test/m43-in-session-usage.test.js` incl. 5 regenerate-log tests — idempotent, deterministic-sort, missing-usage-renders-dash, empty-jsonl, schemaVersion=1 back-compat). Full suite 1717/1719 (2 pre-existing unrelated fails: M43 isMilestoneComplete reads live progress.md vs test fixture; M44 compaction-detector scaffolding — Q2a not shipped). **Contract observation**: D1 hook records ~466 in-session turns against a single `session_id`, confirming metrics-schema-contract v2 §Derived Artifact — `token-log.md` is a regenerated view, not a primary record. The dash-vs-number distinction held up: cost is `—` where the envelope lacks it, never silently `0`.
